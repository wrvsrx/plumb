`= title|plumb export playground
`= author|plumb
`= created|2026-07-20T00:00:00+08:00
`= keywords

 `+ strict markup
 `+ pandoc

`= project

 `= name|plumb
 `= stage|prototype

This document exercises the successfully exportable semantic profile.

`# Inline content

This paragraph contains `*[emphasis with `![strong content]], `==[marked content],
`~[obsolete content], `^[superscript], and `_[subscript],
inline verbatim `"cargo test", a `kbd[Ctrl+C|+[shortcut]] element, and a
`->[link back to the index|index.plumb].

An identical label and target can use an Autolink without duplication:
`->["https://example.test/a%20b"|@[external]|+[reference]|=[rel|nofollow]].

A relative file Autolink uses the same form: `->"static/plumb-pipeline.svg".

Images use parsed alt content and an absolute URI or raw relative src target:

`img[plumb processing pipeline|=[src|static/plumb-pipeline.svg]|@[pipeline]|+[diagram]|=[loading|lazy]]

Transparent `span[inline containers|+[notice]] and TeX `$"x^2 + y^2" use standard profiles.

The exporter preserves generic inline kinds as Pandoc spans. The official
citation profile currently accepts one plain id, such as `cite[smith2004].

`# Lists and definitions

`- first item
`- second item

 `- nested item

`- attributed item

 `+ example

 `== key value

`-
 `- compact nested item
 `- compact sibling

`. first ordered item
`. second ordered item

 `. nested ordered item

`: term

 First definition.

`: another term

 `+ example

 Second definition with attributes.

`# Generic blocks and raw content

`aside Generic marked blocks become Pandoc Div values.

 `+ notice

`div Standard div containers omit the generic marker pair.

 `@ transparent

 `+ notice

`> A quote head becomes its first paragraph.

 `@ quoted

 `+ source

 `== cite smith2004

 Quote children remain inside the Pandoc BlockQuote.

 `> Nested quotes use the same marker.

`rust

 `@ hello-rust

|"
  fn main() {
      println!("hello from plumb-export");
  }

`$

 `@ energy

|"
  E = mc^2

`# Tasks in exported content

Task semantics can coexist with list-item structure and attributes:

`- Inspect multiline task attributes

 `+ task

 `@ export-example

 `== created 2026-07-20T09:00:00+08:00

`# Conclusion

 `@ export-target

This heading is a target for navigation tests from the
`->[language server playground|plumb-ls.plumb#export-target].
