`marker head {`-[aside] `@[intro] `-[note] `:[key value]} with `->[link]{`:[to guide.plumb]}

Paragraph with `text"raw text" and ``escaped introducer.

Autolink `->"inbox.plumb".

`rust"
  fn main() {}

`$"
  E = mc^2

Broken `kind[inline
