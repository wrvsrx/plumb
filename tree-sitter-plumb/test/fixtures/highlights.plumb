`marker{aside #intro .note key="value"} head with `->[link]{to="guide.plumb"}

Paragraph with `[raw text]{language=text} and ``escaped introducer.

`{language=rust}
  fn main() {}

Broken `kind[inline
