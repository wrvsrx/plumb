`#{#event-planning-and-actuals} Event planning and actual records

本文是 event、planned event、clock operations 与 task 边界的设计草案，不是当前 standard extension
reference。实现并迁移现有行为后，稳定语义应进入
`->[standard extensions]{to="docs/reference/standard-extensions.plumb"}。

`# 1. 决策摘要

Event 是统一的 point 或 interval temporal record：

`- 普通 `[.event] 默认表示 actual record，即用户确认发生的时间点或时间区间。
`- 带 `[.planned] facet 的 event 表示计划。
`- Clock 不使用独立 marker 或 record type；Start、Stop 与 Record time 是创建和关闭 actual interval
   event 的 operations。
`- 当前不引入 Transaction record。Task attrs 继续保存当前 workflow facts，Git、undo 与普通文本历史
   承担更完整的修改追踪。

Planned/actual role 不根据 `[now] 推断。未来 actual、过去 planned 都保持 source 中显式表达的 role；
时间经过不会自动移除 `[.planned]，也不会自动创建 actual event。

`# 2. Task boundary

Task 表示希望完成的工作及其当前 workflow state。Task 本身不分为 planned task 和 actual task：

`- `[due]、`[wait]、`[priority] 与 `[recur] 是 planning constraints 或当前意图。
`- `[created]、`[done] 与 `[canceled] 是已经发生的 workflow facts。
`- Task derived state 继续由 task fields、时间与 dependencies 计算，不从 event 自动推断。

`[due] 是任务级 deadline，不等于 planned event 的 start/end。一个 task 可以有多个 planned work
sessions，也可以在没有具体时间安排时只有 due。

Complete task 只写入 `[done]；它不证明 planned event 已发生，也不隐式关闭正在运行的 clock。
Cancel task 只写入 `[canceled]，不同于取消一次 planned event。Stop clock 也不代表 task 已完成。

`# 3. Actual events

普通 `[.event] 默认是 actual。它可以是不关联 task 的生活流水：

`{language=plumb}
  `-{
     .event kind=sleep start="2026-07-31T03:10:00+08:00"
     end="2026-07-31T10:30:00+08:00"
    } Sleep

也可以是关联 task 的实际工作区间：

`{language=plumb}
  `-{
     .event kind=work tasks="#write-parser" start="2026-07-31T14:25:00+08:00"
     end="2026-07-31T14:45:00+08:00"
    } Implement parser

Actual event 只有 `[at] 时表示已经发生的 point event。具有 `[start] 和 `[end] 时表示 closed actual
interval，只有 `[start] 时表示 running actual interval。`[at] 与 `[start/end] 互斥，`[end] 不能单独
存在。所有 closed actual intervals 都可参与 activity duration；只有关联 task 的 closed actual intervals
参与 task work-duration aggregation。

`# 4. Planned events

计划通过 `[.planned] facet 显式表达：

`{language=plumb}
  `-{
     .event .planned #parser-session tasks="#write-parser"
     start="2026-08-01T14:00:00+08:00" end="2026-08-01T16:00:00+08:00"
    } Implement parser

Planned event 可以用 `[at] 表示 point，或用完整 `[start/end] 表示 interval；planned start-only shape
非法，因为 running 是 actual operation state。时间过去后，它仍是 planned；如果没有 actual 或明确 outcome，
UI 可以把它显示为 unreconciled，但不能据此断言 occurred 或 missed。

Planned event 不参与 actual duration 或 task work-duration aggregation。Calendar、Agenda 与 query 可以
同时显示 planned 和 actual，并必须暴露 role 以便分层、筛选和比较。

`# 5. Clock operations

Clock 是围绕 actual interval event 的 operation protocol，不是独立 extension。

Start 创建只有 `[start] 的 actual interval：

`{language=plumb}
  `-{
     .event kind=work tasks="#write-parser"
     start="2026-08-01T14:20:00+08:00"
    } Implement parser

Stop 写入 `[end]：

`{language=plumb}
  `-{
     .event kind=work tasks="#write-parser" start="2026-08-01T14:20:00+08:00"
     end="2026-08-01T16:35:00+08:00"
    } Implement parser

独立的 `[at] 解决缺失 `[end] 的歧义：

`- 只有 `[at] 是 point event。
`- 只有 `[start] 是 running interval。
`- `[start/end] 是 closed interval。
`- Stop 必须验证目标仍是同一 revision 中的 running event，并要求 end 晚于 start。

同一 workspace 是否允许多个 running events 是 clock operation policy，不改变普通 event parsing。
Record time 可以直接创建 closed actual interval；Correct time 只修正 actual event，不修改关联 plan。

`# 6. Plan references and inheritance

Actual event 可以通过 `[plan] 引用具有 explicit id 的 planned event。一个只含 `[plan] 的 actual event
是“按计划发生”的最短表达：

`{language=plumb}
  `-{
     .event .planned #parser-session tasks="#write-parser" kind=work
     start="2026-08-01T14:00:00+08:00" end="2026-08-01T16:00:00+08:00"
    } Implement parser

  `-{.event plan="#parser-session"}

第二个 event 是独立 actual record。它从 plan 继承 title、完整 interval、tasks 与 kind，因此参与 actual
activity 和 task duration projection，但 source 不重复这些 fields。

`## 6.1 Inheritance rules

`- `[plan] 必须解析到 `[.planned] event；planned event 自身不能携带 `[plan]。
`- 一个 actual 只能引用一个 plan；一个 plan 可以被零到多个 actual events 引用。
`- 禁止 plan chains、self reference 与 cycles。
`- 可继承 fields 初版固定为 title、time shape、tasks 与 kind；`[#id]、`[uid]、`[.planned]、outcome 和
   custom attributes 不继承。
`- Actual 上的显式 title、tasks 或 kind 覆盖对应 inherited value。
`- Time shape 作为整体继承：actual 没有 at/start/end 时继承 planned point 或完整 interval；显式 at
   覆盖为 actual point；显式 start 与 optional end 覆盖为 running 或 closed actual interval。
`- Actual 一旦显式提供任一 time field，就不再继承 plan 的其他 time fields。

继承发生在 semantic projection 中，不向 source 写入重复 fields。Hover、detail、CEL 与 export 必须区分
explicit value 和 inherited value，并能导航到提供 inherited value 的 planned event。

`## 6.2 Referenced plan stability

Sparse inheritance 只有在 plan 是稳定历史基线时才可靠。Planned event 一旦被 actual 引用：

`- 普通 Reschedule 不得原地修改它的 title、interval、tasks 或 kind。
`- 删除仍被引用的 plan 必须被拒绝。
`- Reschedule 应保留旧 plan，并创建新的 planned event。
`- Correct plan 用于修正录入错误；它是显式 operation，并明确允许所有继承该值的 sparse actual
   projections 一起改变。

如果产品不能接受 referenced plan 的稳定性约束，就必须在创建 actual 时复制快照，不能同时采用动态
inheritance 和可任意改写的 plan。

`# 7. Operations

UI 和 protocol adapters 提交 typed intent，不提供一个可以任意混改 planned、actual 与 task fields 的
通用 mutation。

`## 7.1 Plan operations

`- Schedule：创建 `[.planned] event，并可选关联 tasks。
`- Reschedule：修改尚未被 actual 引用的 plan；已引用时保留旧 plan 并创建新 plan。
`- Cancel schedule：取消一次安排，不等于 Cancel task。
`- Mark missed：明确记录计划未发生；不能仅根据 end 早于 now 推导 missed。

`## 7.2 Actual and clock operations

`- Start：创建只有 `[start] 的 actual event；可选携带 `[plan]，并使用 now 作为显式 actual start。
`- Stop：为 running actual 写入 end；不自动 Complete task。
`- Record time：补录 closed actual interval。
`- Record as planned：创建只含 `[plan] 的 sparse actual event。
`- Correct actual：添加或修改 actual overrides，不反向修改 planned event。

如果 Start 后实际 start/end 与 plan 完全一致，Stop 可以在保证语义不变时规范化为只含 `[plan] 的最短
actual source。只要存在时间或其他 override，就必须保留显式 actual fields。

组合行为必须使用显式命名且 revision-guarded 的原子 operation，例如 Stop and complete；不能把组合
副作用隐藏在 Stop 或 Complete 中。

`# 8. Reconciliation

Planned event 的展示状态由时间、显式 outcome 和关联 actual records 派生：

`- upcoming：计划尚未开始，且没有 actual。
`- in-progress：存在引用它的 running actual。
`- occurred：存在一个或多个引用它的 closed actual records。
`- unreconciled：计划区间已过去，但既没有 actual，也没有明确 outcome。
`- missed：用户明确确认未发生。
`- canceled：用户明确取消计划。

`[unreconciled] 不能自动提升为 occurred 或 missed。计划和实际同时存在时，projection 可以计算 start、
end 与 duration variance；偏差不写回 source fields。Sparse actual 继承完整 interval 时 variance 为零。

`# 9. Calendar and projections

Calendar 是通用 temporal visualization，同时消费 planned 和 actual records：

`- Planned layer 显示安排、未核对计划和取消结果。
`- Actual layer 显示生活流水、工作区间与 point events。
`- Comparison layer 可以把 plan 与引用它的 actual 组合显示。

Reminder 默认只消费 planned。Activity timeline 默认消费 actual。Task duration 只消费关联 task 的 actual
intervals。Agenda 可以组合两类 records，并提供明确 role filter。

iCalendar 是 projection policy，不决定 EventRecord 语义。默认 planned feed 与 optional actual activity
feed 应分开，使用户可以独立订阅；不得把所有 sleep、phone 等 actual records 无条件混入 planned feed。

`# 10. UI behavior

创建入口表达默认 role：

`- Calendar Schedule 创建 planned event。
`- Activity Record 创建 actual point 或 closed interval。
`- Clock Start 创建 running actual interval。
`- 通用 event editor 显式提供 Planned toggle。

Task detail 可以组合 task、plans、actual sessions 与 variance，但操作保持分开：

`- Task actions：Schedule、Start、Record time、Complete、Cancel task。
`- Planned actions：Start、Record as planned、Reschedule、Missed、Cancel schedule。
`- Actual actions：Stop、Correct time、Edit classification、Add note。

按计划发生的 sparse actual 与 plan 可以折叠显示为一项，但底层仍是两个 records，并分别拥有 source
identity。UI 必须显示 inherited fields 的来源，编辑 inherited value 时引导用户选择 Correct plan 或在
actual 上创建 override。

`# 11. Mutation safety

所有 operations 使用 indexed identity、expected document revision、owned syntax mutation 与
format-aware finalization。Frontend 不拼接 plumb source。

跨 plan/actual 的 operation 先在同一 revision snapshot 上验证引用、owner、interval、inheritance 与
preconditions，再原子提交 guarded edit。Revision conflict、invalid document、引用失效或 plan stability
violation 时不得部分写入。

`# 12. Migration

当前 standard event 没有 `[.planned]，迁移不能只根据记录在过去还是未来分类：

`- 明确来自 Calendar、预约或排程的数据补充 `[.planned]。
`- 来自 `[task-utils list-event] 的 sleep、phone、work、用药等时间流水保持普通 `[.event]。
`- 旧 start-only point records 迁移为 `[at]；已知由 Start 产生且尚未结束的记录保留 `[start]。
`- 无法可靠判断的 records 要求用户或 importer 选择，不静默改变含义。

迁移不伪造 actual records，也不把过去的 planned records视为已经发生。兼容层可以按 source/provider
使用临时默认值，但 query、Web UI 与 export 必须区分 explicit role 和 compatibility assumption。
