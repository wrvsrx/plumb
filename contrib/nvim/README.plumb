`meta
 `: title

    plumb Neovim Lua modules

`# User guide

完整的 Neovim 0.12、LSP、tree-sitter、workspace 与 authoring setup 见
`->[editor integration 指南]{to="../../docs/guide/editor-integration.plumb"}。本文只记录 packaged Lua
modules 的调用接口。

`# Module setup

把该 directory 加入 Neovim `[runtimepath] 后调用唯一 entry point：

`{language=lua}
  require('plumb').setup()

它默认启用 LSP、LSP folding、tree-sitter、CodeLens、search 与 Web command，不创建 keymaps。重复调用是幂等的；
`[lsp]、`[treesitter]、`[codelens]、`[search] 与 `[web] 都可用 `[enabled = false] 单独关闭：

`{language=lua}
  require('plumb').setup({
    command = 'plumb',
    lsp = { folding = true, root_markers = { '.plumb', '.git' } },
    treesitter = { parser_path = '/path/to/plumb.so' },
    codelens = { picker = 'snacks' },
    search = { picker = 'snacks', task_filter = 'state == "ready"' },
  })

  vim.keymap.set('n', '<leader>pn', function()
    require('plumb.search').search_notes()
  end)
  vim.keymap.set('n', '<leader>pt', function()
    require('plumb.search').search_tasks({ filter = 'actionable' })
  end)

modules 要求 Neovim 0.12。`[plumb.search] 还要求 attached plumb LSP 声明
`[experimental.plumb.search] schema version 2。native search 使用 `[vim.ui.input/select]；`[live_search]
接受 picker adapter，并负责 debounce、request cancellation、stale-result suppression、locations 和
incomplete-result state。

LSP attach 后 plugin 使用 `[vim.lsp.foldexpr()] 与 `[vim.lsp.foldtext()]，初始 `[foldlevel] 是 99；
`[lsp.folding = false] 保留用户自己的 window-local fold settings。fold 范围覆盖 heading sections 与跨行
marked/verbatim blocks；task fold text 显示 READY、WAITING、DONE、CANCELED 或 INVALID 与 title。未连接
LSP 时仍可自行使用 bundled tree-sitter `[folds.scm]。Nested task label 保留 source block indentation，
让 indent guide 占用缩进区域而不覆盖状态文本。

plugin 默认把 `[task + completed] 与 `[task + canceled] semantic tokens 链接到 `[Comment]，淡化完整
closed task block。自定义 `[@lsp.typemod.task.completed.plumb] 与
`[@lsp.typemod.task.canceled.plumb] highlight groups 可以覆盖该默认值。

`# Installation

Nix 提供 `[packages.plumb-nvim] 与 `[plumb.passthru.neovim-plugin]，可直接加入
`[programs.neovim.plugins]；主 package 也把源码安装在 `[share/plumb/contrib/nvim]。binary、Lua runtime
与 bundled syntax profile 来自同一 package version。`[:checkhealth plumb] 会检查 binary、parser、queries
与 active client。tree-sitter parser 由 `[plumb.passthru.tree-sitter-plumb] 提供。
plugin 与 binary 要求 exact release version match；tree-sitter 要求 ABI 15，bundled queries 必须与同一
checkout 的 grammar queries 一致。Cargo regression test 强制 `[version.lua] 跟 workspace version 同步，
query drift test 强制两份 runtime query 相同。

lazy.nvim 使用 source checkout 时：

`{language=lua}
  {
    dir = '/path/to/plumb/contrib/nvim',
    ft = 'plumb',
    config = function()
      require('plumb').setup()
    end,
  }

不用 plugin manager 时执行 `[vim.opt.runtimepath:prepend('/path/to/plumb/contrib/nvim')] 后调用 setup。
`[parser_path] 只用于 local development override；普通安装应让 Neovim 从 parser package 注册 language。
runtime 内的 `[queries/plumb] 与 `[tree-sitter-plumb/queries] 有 drift regression test。

`# Commands and API

setup 注册 `[PlumbFormat]、`[PlumbRename]、`[PlumbCodeAction]、`[PlumbTaskComplete]、
`[PlumbTaskCancel]、`[PlumbNotes]、`[PlumbTasks]、`[PlumbReferences] 与 `[PlumbWeb]。完整 public Lua
API 和 default-free keymap examples 见 editor integration guide 与 `[doc/plumb.txt]。

`# Workspace Web app

`[:PlumbWeb] starts `[plumb site serve] for the attached plumb client's workspace and selects the current
plumb document in the browser. `[:PlumbWeb!] opens the whole workspace without a current-note
selection. The browser process is independent of LSP; the integration only discovers the workspace root
and starts the unified binary. Lua callers can use `[require('plumb.web').open({ root = path })] directly.

`# Reference CodeLens

`[plumb lsp] 在顶层 metadata 上显示 `[N file references]，并在每个 explicit anchor 上显示
`[N references] CodeLens。前者统计其他 documents 中指向当前文件的 reference occurrences。
command id 是 `[plumb.showReferences]，arguments 是 target URI、target position 和完整 reference locations。
`[plumb.codelens.setup({ picker = 'snacks' })] 为 Neovim 注册 client-side command，并把 CodeLens 已
携带的 locations 直接转换为 Snacks file picker items；它不会重复发送 LSP references request。picker
提供 file preview、jump、tagstack 和 single-result auto-confirm。

默认 `[picker = 'quickfix'] 会把 locations 写入 quickfix list 并打开 quickfix window。请求 Snacks
但 module 不可用时会 warning 并回退 quickfix；0 references 时只显示提示。其他 editor 可以使用同一
arguments contract 接入自己的 references UI。
