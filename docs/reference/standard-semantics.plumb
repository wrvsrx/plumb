`= title plumb standard semantics reference

`# 1. Scope

core syntax 对 marker spelling 保持 opaque。本文定义 official semantic profile：heading、anchor、metadata、
list、definition、Link、image/file、citation、quote/style、task/event、table、math 与 Pandoc projection。

profile 是 statically composed、protocol-neutral pipeline，不是 dynamic plugin registry。syntax-valid document
才进入 authoritative analysis；recovered editor queries 只能提供本文明确允许的局部 assist。

`# 2. Direct declarations

block/inline owner 使用同一 relation projection：direct leaf-shaped `"@"、`"+"、`"=" owner 分别产生 id、
facet 与 property。block declarations 是 indented direct children；inline declarations 是 owner content 中的
direct marked groups：

`plumb"
 `node Head
  `@ stable
  `+ notice
  `= level 2

`plumb"
 `node{visible `@{stable} `+{notice} `={level 2}}

id/facet value 必须是一个 nonempty plain positional element。property first positional element是 nonempty plain
key，remaining elements sequence是 value。recognized declarations与 whitespace不占 owner positional indexes，
并从 rendered body/details 中过滤；
underlying core sequence 不删除、不重排。unknown marked owner 保持 ordinary content。

declarations 可以 source-order interleave。duplicate/owner-specific constraints由对应 recognizer 诊断。显式 id
是 anchor/rename authority；heading title 不生成 implicit id。

`# 3. Headings and anchors

一至六个 `"#" marker 是 heading level。level 以 marker length 决定；其他含 `"#" spelling 是 generic。

`plumb"
 `# Introduction
  `@ intro
 `## Details

outline 按 source heading levels 建树。Add explicit id operation 只作用于 syntax-valid、cursor 命中的最深
marked block 且当前没有 id；anonymous parsed/group 与 verbatim leaf 不直接提供该 operation。slug 从 nonempty
plain content 生成，fallback marker/block，并在 document 内加 numeric suffix 去重。

`# 4. Lists and definitions

`"-" 与 `"." 是 bullet/ordered item。相邻同 marker siblings 形成 list；marker 切换或 intervening body block
结束 group。nested item 形成 nested list。ordered list 固定从 1 开始；`"item" generic。

`":" 是 definition entry。没有 non-declaration children 时，first positional element是 term，remaining head
elements共同形成 inline body；multi-element term使用 anonymous group。存在 body children 时，完整 trimmed head 是 term，children
形成 block body：

`plumb"
 `: Term Inline body
 `: {Term with spaces} Another inline body

`plumb"
 `: Term with spaces
  Definition body

相邻 definition entries 形成 DefinitionList。term 必须 nonempty plain/grouped text；rich invalid key/term 发布
semantic diagnostic。
DefinitionList属于可渲染body语义，不是document metadata，也不向Event等分析提供继承上下文；其分析输出独立于metadata。

`# 5. Document metadata

document identity 是 normalized workspace-relative path。direct top-level `"=" blocks 是 metadata entries 并
可与 body interleave；消费 declarations 不拆分相邻 rendered lists。top-level `"@" unsupported，
发布 document.unsupported-identity warning，也不进入 body。根层 direct leaf `"+" 声明保留 opaque facet，
名称形态与普通 owner 相同；空值、rich name 或带 children 发布 document.invalid-facet warning。facet 不进入 body。

`plumb"
 `= title Document title
 `= created 2026-09-02T09:00:00+08:00
 `= tags
  `+ plumb
  `+ notes
 `= author
  `= name Alice

leaf property first positional element是 key，remaining elements是 scalar value；key with spaces 用 anonymous group。
有 children 时完整 head 是 key，children value shape 是：

`- zero children/null。
`- one anonymous parsed block/scalar。
`- one VerbatimBlock/literal string。
`- direct `"+" sequence，preserve source order。
`- direct `"=" nested map。

不能混合 incompatible shapes。metadata sequence 只接受 `"+"；`"-"/`"." 保持 rendered list semantics。
duplicate key 使用 first value并诊断。title 必须 scalar；created 是完整 RFC 3339，date 可 YYYY-MM-DD 或
RFC 3339，timezone 是 numeric UTC offset。

metadata insertion 只在 valid document、document 尚无 metadata 且 cursor byte offset 0 提供。title 取 filename
stem，created 取 operation local RFC 3339；通过 plumb-edit 生成 current space-separated properties。生成的
leaf property 保持 first-rest shape：key 是 first element，scalar value 是 ungrouped remaining element
sequence；filename stem 中的 spaces 不引入 anonymous Group。例如 `"Project Guide.plumb" 生成：

`plumb"
 `= title   Project Guide
 `= created 2026-09-02T09:00:00+08:00

`## 5.1 Document facets and tasks target contract

文档 facet、task、引用、编辑与适配层遵循以下统一契约。

文档根层 direct leaf `"+" declaration 接受与普通 owner 相同的 facet name 形态；未知 facet 保留为 opaque 声明，不进入 body。空值、rich name 或带 children 的 facet 发布 semantic diagnostic。根层 `"@" 仍不支持，文档身份始终来自 normalized path。

根层 `"+ task" 使文档成为唯一 Document task；重复声明不能产生重复任务。任务标题取 document title，缺省沿用 workspace 文档显示名称。根层 properties 提供现有 task 字段，body 提供详情。原有顶层 list tasks 成为文档任务的 children，深层关系保持不变。父子关系不隐含 dependency，也不传播 closure 或 focused。首版文档任务不支持 recur；该字段产生 diagnostic，相关编辑不可用。本文不启用文档 event 或任意 block task。

文档任务引用使用 source-document-relative path；带 fragment 的引用仍定位指定 anchor task，无 fragment 的引用只定位 document task。目标无 task facet 时报告 not-task，不回退到第一个子任务。depends、prev 与 event tasks 使用同一 target resolver。引用集合必须支持 grouped 单项以及结构化逐项表示，含空格或内部 .plumb 字串不能被启发式拆开；现有无歧义 anchor 引用保持兼容。

无 fragment 的 scalar reference value 整体是一个路径，例如 `"Project Plan.plumb" 或 `"archive.plumb notes.plumb"，不得按空格或内部 .plumb 拆为多个 document targets。contains Group 或 Verbatim positional element 的 leaf value 按 positional element 分项，每项 Stringify 成一个 reference；例如 `"{Project Plan.plumb} {other.plumb#review}"。全为普通 text 的 legacy anchor reference list 保持既有分项规则。空 grouped 项为 invalid target，不能静默删除。

旧 authoring 曾把多个 anchor references 包进单个 Group，例如 `"{#a #b}"。仅当该项不能解析成单个 target、且 legacy 分项全部是有效 anchor references 时，保留这一兼容分项；document paths 不适用此例外，避免把含空格文件名误拆开。

depends 与 event tasks 也接受 direct leaf `"+" children 的 sequence，每个子项的完整 content 是一个 reference，不再分项。其他 child marker、空项或嵌套 child shape 为 invalid target。结构化声明的完整 head 是 key，`"tasks other" 不等同于 tasks；显式 tasks 即使为空或非法也覆盖正文 link 推导。prev 始终是单项 scalar reference。

引用路径内部的重复 ASCII spaces 原样保留；SoftBreak 按标准 Stringify 投影为空格。分项只消费项之间的分隔，不规范化文件名本身。

focus history、状态推导、priority 和 dependency 规则复用 list task 行为。移除 task facet 只取消任务身份，保留 properties、body 和 children；已有引用随后成为 not-task。导出仍是普通文档 title/body，不额外生成包裹全文的 list task。

带 children 的 focused 声明以完整 head 为 key；focused other 是独立 opaque key，不产生 focus interval 或 task focus diagnostic。

文档 task 的根层 focused property 由 task profile 校验；其历史列表使用与 list task 相同的 direct leaf `"-" 子项，并在 metadata 中投影为 List，不产生 metadata.unsupported-value。非法历史形态由 task focus diagnostic 报告。此例外只适用于有有效 task facet 的文档根层 focused，不扩展普通 metadata 或嵌套 map 的 sequence 语法；facet 可出现在历史字段之后。

文档 task 的 created、due、wait、done、canceled、recur、priority 与 prev 必须使用带非空 scalar value 的 leaf property。空声明或带 children 的声明发布 task.invalid-field-shape，不能因 leaf attribute projection 跳过该字段而静默忽略；multiword property key 与这些 key 不同，不作为任务字段消费。

`# 6. Links

未带 resource facets 的 `"->" group 与 compact marked Verbatim 使用本节 navigation Link projection；resource-faceted Group 由第 7 节消费，但共享 positional binding。唯一 standard Link kind 是 `"->"。direct declaration与 whitespace不占 positional index。一个 non-declaration
element产生 derived-label Link：原 element是 rich label，其递归 Stringify value 是 target。两个或更多 elements
产生 explicit-label Link：first 是 rich label，所有 remaining source-ordered elements共同形成 target，保留其间
internal whitespace后递归 Stringify。两种 arity 都是同一种 Link semantic record；zero positional elements invalid：

`plumb"
 `->{guide.plumb}
 `->{`"guide.plumb"}
 `->{`!{guide.plumb}}
 `->{guide guide.plumb}
 `->{guide Project Guide.plumb}
 `->{{guide page} {Project Guide.plumb}}
 `->{{cross-file target} guide.plumb#intro `@{cross-file}}
 `->{{external target} https://example.test}

Stringify 递归遍历 Text、SpaceRun、SoftBreak、Group 与 Verbatim；envelope/marker不贡献字符，每层 direct
declaration被过滤，argument内部 whitespace run各产生一个 semantic space。empty Stringify invalid。Link 对 enclosing
plain-text projection只贡献原 rich label；target 不进入 title/details/scalar。

derived-label Link 的 multi-element target必须使用一个 anonymous Group或 compact verbatim payload，否则 first
element会成为 explicit label，remaining elements会成为 target。direct adjacency本身不建立隐式组合。

URI scheme 或 `"//" prefix 是 absolute/network target；其他是 source-document-relative raw filesystem path。
不 percent encode/decode/normalize；UTF-8、spaces、`"%"、`"?" literal。`"#" 唯一分隔 explicit anchor，path
不能含 `"#"。control、backslash、absolute filesystem path invalid。

marked inline verbatim 是 derived-label Link 的 compact spelling：

`plumb"
 `->"https://example.test/a%20b"
 `->"guide.plumb#intro"
 `->"../assets/manual draft.pdf"

payload nonempty，同时作为 label/target；上面的 one-verbatim-argument group 与 compact marked inline verbatim
spelling semantic-equivalent。Link record与 graph edge不按 derived/explicit label 或 parsed/verbatim spelling分类。
completion 在 explicit-label Link target、derived-label Link element 与 compact payload 的 explicit anchor component
提供 workspace documents/anchors；delimiter collision 使用 full quote strength，不改 raw path。

`# 7. Embedded links

只有 parsed inline Link（marker 为 `"->"）可通过 direct `"+ embed" facet 表达内嵌展示目标的意图。anonymous、transparent、generic、style、citation 或 declaration Group 上的 embed 发布 resource.invalid-owner，不获得资源 projection 或 completion。block 与 Verbatim 不获得 embed semantics。img/file facets 与 markers 不再选择资源语义；普通下载附件使用普通 Link。embed owner 上的 src 发布 resource.unexpected-source，目标只来自 positional arguments。

目标绑定沿用 Link first/rest：单 element 同时提供 derived label 与 target；多个 elements 的 first 是 label，其余 Stringify 为唯一 target。empty explicit label 合法，target 必须 nonempty。目标继续使用 URI/raw relative file path constraints；zero/empty target 发布 embed.missing-source，非法目标发布 embed.invalid-source。plain-text 只贡献 label。所有 embed 引用（包括未知类型 fallback）都不产生 navigation LinkRecord 或 document graph edge。

`plumb"
 `->{头像 static/avatar.jpg `+{embed}}
 `->{static/avatar.jpg `+{embed}}
 `->{{} static/decorative.png `+{embed}}
 `->{{Demo video} static/demo.mp4 `+{embed}}
 `->{Podcast https://example.test/stream `+{embed} `={type audio/ogg}}
 `->{Download reports/final.pdf}

共享层通过纯函数确定媒体类型，不读取文件或请求网络。优先使用 first explicit type property：MIME essence trim 并忽略大小写，允许 semicolon parameters；未知或无效 explicit type 直接返回 unknown，不能再从扩展名推断。缺少 type 时取目标 path 最后扩展名（忽略大小写）；URI/network target 仅使用 URL path，忽略 query/fragment 与 authority，local raw path 不剥离字面问号或井号。

识别的 MIME 与 extensions：image/png（png）、image/jpeg（jpg/jpeg/jpe）、image/gif（gif）、image/webp（webp）、image/svg+xml（svg）、image/avif（avif）、image/bmp（bmp）、image/tiff（tif/tiff）、image/x-icon（ico，兼容 image/vnd.microsoft.icon）；video/mp4（mp4/m4v）、video/webm（webm）、video/ogg（ogv）、video/quicktime（mov）、video/x-matroska（mkv）、video/mpeg（mpeg/mpg）；audio/mpeg（mp3）、audio/mp4（m4a）、audio/ogg（ogg/oga/opus）、audio/wav（wav，兼容 audio/x-wav）、audio/flac（flac）、audio/aac（aac）、audio/webm（仅显式 type）。其他类型回退普通 Link。识别某种媒体类型不保证浏览器支持具体 codec。

exporter 将 image 分类导出为 Pandoc Image，其他 embed 导出为 Pandoc Link fallback。两者都通过 data-plumb-facet=embed 保存内嵌意图，保留原始显式 type 与其它 attributes，不把推断结果伪装成显式 type。Importer 将 Pandoc Image 与带 embed intent 的 Link 导入为 canonical parsed Link + embed；普通 Link 不添加 facet。显式 incompatible data-plumb-marker 必须拒绝，不能重新生成 src。output writer 不支持媒体时保留 portable label/link fallback；Pandoc Image 由 writer 承担对应格式的图片 lowering。

Web 使用同一共享类型识别结果：image 渲染图片（TIFF 因浏览器支持不足保留 Link），video/audio 渲染 controls 播放器，并提供独立可点击的 Link fallback；未知类型保留普通链接。Web 不用本地资源索引、HTTP Content-Type 或 MIME sniffing 覆盖语义分类。CSP 允许同源与 HTTPS 媒体；其它外部来源仍有可点击 fallback。embed completion 使用 positional target boundary，补全任意本地文件，不提供 document anchor completion。

`# 8. Citations

首个 profile 只接受 exactly one nonempty plain positional element作为 id：

`plumb"
 See `cite{smith2004}.

bibliography metadata 是 workspace-contained CSL JSON paths：

`plumb"
 `= bibliography
  `+ static/library.json

LSP completion/hover/definition 使用 declared files。无 bibliography 的 valid citation 保持 portable export 并
发布 unresolved warning。cluster、locator、prefix/suffix 与 alternate modes deferred。

`# 9. Quotes, containers and inline styles

`">" block head 是 quoted first paragraph，body children 是 remaining quote blocks；nested quote 合法。unconsumed
declarations 通过 Pandoc inner Div 保留。

`"()" 是 transparent block/inline marker：消费自身 marker，不产生 redundant data-plumb-marker；它仍保留
ordinary owner content/children/declarations。anonymous block/group 与 explicit `"()" owner 是不同 syntax
category，但 official export 的 visible wrapper transparent。

六种 inline style marker 都是 content containers：消费 zero or more source-ordered visible elements作为完整 style
body，不建立 positional arity。direct declarations从 visible body过滤并保留为 attributes；whitespace 与 adjacency
按普通 InlineContents projection处理：

`plumb"
 `*{emphasis with spaces}
 `!{prefix`*{nested}suffix}
 `=={mark}
 `~{strikeout}
 `^{superscript}
 `_{subscript}
 `!{}

因此 style 内的 multi-element content不需要 anonymous Group；style owner自身在 parent depth已经是一个 element。
export 分别为 Emph、Strong、mark Span、Strikeout、Superscript、Subscript；presentation italic/bold 不是独立
semantics。

`# 10. Tasks

`"-"/`"." list item 通过 direct leaf `"+ task" facet 成为 task；`"task" marker 仍 generic：

`plumb"
 `- Implement parser
  `+ task
  `@ write-parser
  `= created 2026-09-02T09:00:00+08:00
  `= due 2026-09-03T09:00:00+08:00
  `= depends #design Project Plan.plumb#review

完整 item head 是 title，body children 是 details。nested task blocks 形成 display tree；只有 depends 建立
dependency edge。需要 reference 时显式 id。

defined fields：created/due/wait/done/canceled 是 RFC 3339；recur 是 positive PnD/PnW/PnM/PnY 且需 due；prev
是 one task reference；priority 是 i32；depends 是 whitespace-separated same/cross-file explicit-id references；
focused 是 focus interval 或 focus interval list（见下）。
raw cross-file spelling 是 `"path.plumb#id"，path 可以含 ASCII/UTF-8 spaces，由 `".plumb#" boundary disambiguate。

closure state：done only=done，canceled only=canceled，both=conflicted，neither=open。workflow state precedence 是
conflicted、done/canceled、future wait/waiting、open dependency/blocked、ready。time/dependency reasons 可同时存在。

focus interval 是 `"start--end" 或 open 的 `"start--"；endpoint 是带 explicit offset 的 RFC 3339 timestamp，比较按
instant 而不是字符串。`"focused" property 支持 single value 与 interval list：

`plumb"
 `- 单个开放区间
  `+ task
  `@ focus-open
  `= focused 2026-09-20T09:00:00+08:00--
 `- 单个已结束区间
  `+ task
  `@ focus-closed
  `= focused 2026-09-20T09:00:00+08:00--2026-09-20T11:00:00+08:00
 `- 区间列表
  `+ task
  `@ focus-history
  `= focused
   `- 2026-09-20T09:00:00+08:00--2026-09-20T11:00:00+08:00
   `- 2026-09-20T14:00:00+08:00--

single value 与 one-element list 语义等价。list 使用 direct `"-" children，每项恰好一个 interval，不携带其它结构或
内容。property 不能为空、不能空列表、不能混用 inline interval 与 interval children、不能重复 `"focused"。interval
按 start 升序且同一 task 内不得重叠，可以首尾相接；end 早于 start 非法，end 等于 start 允许；最多一个 open
interval 且必须位于最后。不同 task 的 interval 可以重叠。interval child 不产生 task 或 event record。

focus 派生事实：`"focused" 表示存在 valid open interval 且 task closure 未关闭；`"focused_since" 是当前 open interval
的 start，未 focused 时为空；另有完整 typed interval history 供 detail 展示。存在 `"focused" property 不等于当前
focused：已结束的历史既不让 task 退出候选，也不触发任何排序提升。ready、waiting、blocked 都可以 focused，task 变成
waiting/blocked 不自动结束 interval；done、canceled、conflicted 都不算当前 focused。focus 是 task-local 事实，不传播到
parent、child 或 dependency task；display tree 上按 subtree 聚合 focused 只影响展示排序，不改变任何 task 的语义标记。
手工编辑造成 closed task 仍有 open interval、或其它 invalid focus data 时发布 semantic diagnostic，不自动改写源文档；
focus data 无效的 task 不能作为 `"focused == false" 的 next 候选，`"next" 必须报告被跳过的 task 以便定位修复。

focus 首次写入单个 open interval；已有全部结束的历史时追加 open interval，single value 在需要时转成 list；已 focused
时 focus 幂等，不重置 start 也不写文档。unfocus 为 open interval 补当前 end time；没有 open interval 时幂等且不写
文档。focus 拒绝 closed task，waiting/blocked 的未关闭 task 允许 focus。

complete 添加 done，cancel 添加 canceled；blocked completion rejected，cancel allowed。complete 与 cancel 必须在同一次
atomic edit 中结束 open interval，关闭被拒绝时 focus history 保持不变。recurring close 保留旧 instance的 focus
history并 append next，新 instance 不继承 `"focused"，advance due/wait、unique id、prev。operations 使用 revision
guard、plumb-edit owned syntax 和 current formatter，不手写 separator/indentation；一次 operation 只取一次当前时间，
时钟回拨或 focus history 本身无效时明确失败，不写入也不猜测修复。

Task/Event construct completion 只从 list-marker prefix 提供；legacy `"t"/`"task"、`"e"/`"event" prefixes 不
提供 candidates。Task skeleton 首 child 是 `"+ task"，created 使用 current local RFC 3339。

`# 11. Events

list item 通过 direct `"+ event" facet 成为 event，与 task facet mutually exclusive：

`plumb"
 `- 14:00--15:00 Parser review
  `+ event
  `@ review
  `= date 2026-09-02
  `= timezone +08:00
  `= tasks #write-parser

first head positional element是 nonempty plain schedule，所有 remaining head elements共同形成 nonempty title；event 不再使用
kind-specific pipe parser。body children details。schedule 是 point START 或 half-open START--END。START 接受
HH/HH:MM/HH:MM:SS、YYYY-MM-DDTTIME 或 full RFC 3339；END 接受 TIME/date-time 并继承 start offset，time-only
end 早于 start 时 rollover next day。resolved end 必须 later。

nearest ancestor/document date/timezone context 继承；event property override subtree，dedent 恢复 parent。
invalid explicit override 不 fallback ancestor。tasks 创建 association，不是 dependency。UID 从 path/schedule/title
deterministically derivation；source uid/when opaque。vdir export 是 managed readonly VEVENT files。

`# 12. Tables

`"table" direct `"-" children 是 rows。nonempty row head 的每个 direct positional element是 compact inline cell；SpaceRun
width 是 formatting padding，不改变 columns，empty group 是 empty cell：

`plumb"
 `table
  `- name             age
   `+ header
  `- {Alice Smith}    10
  `- Bob              {}

empty-head row 的 direct non-declaration block children 是 expanded cells；无需额外 cell marker。cell 是
sibling block，因此与其它 block 一样用 blank line 分开：

`plumb"
 `table
  `-
   `+ header

   name

   age
  `-

   {Alice Smith}

   10

direct `"+ header" 标 leading header rows；expanded body row 的 leading header-faceted cells 是 row-header prefix。
所有 rows effective column count 一致。rowspan、colspan、alignment、widths、table foot/complex grouping unsupported。
table-consumed rows/cells 不产生 ordinary list/task/event records。export/import round-trip supported subset。

`# 13. Math and export

`"$" marked inline/block Verbatim 产生 TeX InlineMath/DisplayMath：

`plumb"
 Inline `$"x^2" math.

`plumb"
 `$"
  E = mc^2

generic marked block/group 分别 lower Div/Span，VerbatimBlock/inline Verbatim 分别 CodeBlock/Code，anonymous parsed
block lower Para。plumb export 直接产生 Pandoc JSON，只把 Pandoc 当 writer；plumb import 对 supported official
profile 生成 current strict syntax，无法表示的 figure/raw HTML/footnote/complex citation 等必须 reject，不 silent
drop。

`# 14. Version boundary

official profile 只解释 current syntax。semantic analysis 不检测历史 epoch，也不在分析前执行 source migration。
