`= title plumb diagnostics reference

`# 1. Ownership

syntax diagnostics 只报告 core syntax。official semantics 报告 marker/profile violations；workspace 报告
cross-document resolution、identity 与 dependency；protocol adapters 不重新解释错误。

每条 diagnostic 包含 stable code、severity、UTF-8 half-open source range、message 与 optional related
ranges。missing token 不作为 synthetic lossless leaf。

`# 2. Publication

每个 revision 都发布 recovered tree 的 syntax diagnostics。只有 current revision syntax-valid 时才合并同
revision semantic diagnostics。last-valid semantic outputs 可以继续服务 cross-file queries，但不能伪装成
current diagnostics。export 与 authoritative edits 只接受 ValidDocument；completion/folding 可以显式查询
recovered view。

一次 workspace diagnostic round 必须只构造一次全局 task dependency graph 与 cycle context，并在同一
workspace snapshot 的所有 document diagnostics 之间复用；不能按文档重复构造全库上下文。复用不得改变
diagnostic code、severity、message、range、related ranges 或顺序，也不能省略已关闭任务的检查。

disk generation 保存全部 syntax 与 document-local semantic
diagnostics；syntax-invalid source 只保存 syntax diagnostics，不沿用 previous semantic diagnostics。数据库与内存
诊断输入必须产生相同结果，包含没有内存 DocumentEntry 的文档。跨文件 links、embeds、task prev/dependencies、
event associations 和 cycles 仍针对当前 query snapshot 检查；外部文件存在性每轮读取。缓存命中不能跳过这些检查。

`# 3. Syntax categories

`- structural：top-level indentation、tab indentation、partial dedent 与 illegal parsed control。
`- lexical/dispatch：incomplete introducer、invalid marker、marker 后非法 whitespace、不能完成的 inline/block
 entry。
`- group：unexpected closing brace、physical line 结束前 unclosed group。
`- inline verbatim：compact/full envelope missing closer 或 quote-strength mismatch。

brackets 与 pipe 是 ordinary text，不发布 delimiter diagnostics。bare braces 永远 special；literal brace 使用
introducer escape。block raw 缺 structural margin 的 line 正常结束 payload，不是 error；该 line 随后按普通
block grammar 诊断。

历史 member-envelope 的迁移说明只保留在 history 文档中；current document diagnostics 不处理历史格式。

`# 4. Precedence and recovery

block-start error 在 current physical line 恢复。invalid block introducer 可以建立 recovered placeholder owner，
使随后 indentation/folding boundary 稳定；ordinary inline error 不建立 block owner。

`"syntax.unclosed-inline-group" 与 `"syntax.unexpected-inline-group-close" 在 current physical line 恢复；
`"syntax.unclosed-verbatim" 在 line ending/EOF 恢复。structural tab/partial dedent 优先于由错误 indentation
派生的 generic dispatch error。同一 range/root cause 只发布最具体 code；独立错误按 source/recovery order
稳定排序。

`# 5. Current syntax codes

`text"
 syntax.incomplete-introducer
 syntax.invalid-block-dispatch
 syntax.invalid-inline-dispatch
 syntax.unclosed-inline-group
 syntax.unexpected-inline-group-close
 syntax.unclosed-verbatim
 syntax.tab-indentation
 syntax.partial-indent

新增或重命名 code 是 editor/test automation interface change，必须同步 normative corpus、range/precedence 与
recovery assertions。lenient tree-sitter corpus 不能代替 strict parser corpus。

`# 6. Semantic categories

official profile 至少区分：

`- association/property：invalid key/arity/value shape。
`- heading/anchor：invalid level、duplicate explicit id。
`- metadata：duplicate/invalid key、invalid location、mixed/unsupported value、invalid created timestamp、
 unsupported document identity；document.invalid-facet 表示根层 facet 不是单个非空 plain name 的 leaf declaration。
`- link/embed/citation：missing/invalid target/source/id 与 workspace unresolved/ambiguous target。embed 的非 Link owner 发布 resource.invalid-owner；遗留 src 发布 resource.unexpected-source。未知媒体 type 是 Link fallback，不是 error。
`- task/event：invalid fields/reference/time/interval/recurrence/closure state、dependency consistency 与 mutually
 exclusive facets；task.document-recur 表示 document owner 不支持 recur；task.invalid-field-shape 表示文档任务 scalar 字段为空或包含 children，range 覆盖该 property；focus interval 的 invalid format、duplicate property、顺序、重叠、多个或非末尾的 open interval，
 以及 closed task 仍含 open interval。
`- table：invalid row child、empty row、column mismatch、header-after-body、inconsistent row-header prefix。
`- math：invalid owner 或 unsupported input language。

semantic diagnostic 指向最小 source-backed field；cross-document problem 可以携带 related locations。adapter
不得用 guessed offset 替代 syntax/semantic layer 的 precise range。
