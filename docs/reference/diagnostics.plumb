{
 `: title plumb diagnostics reference
}

`# 1. Diagnostic ownership

 syntax diagnostics 只报告 syntax。official semantics 报告 semantic profile violations；workspace

分析报告跨文档 resolution、identity 和 dependency 问题；protocol adapters 不重新解释这些错误。

每条 diagnostic 包含稳定 code、severity、message 和 source range。可修复错误可以附带 edit，
但缺失 token 不作为 synthetic syntax-tree leaf。

`# 2. Publication policy

每个 revision 都发布当前 recovered tree 的 syntax diagnostics。只有当前 revision syntax-valid 时，
才合并同 revision 的 semantic diagnostics。last-valid semantic outputs 可以继续服务跨文件查询，
但不能伪装成当前文档的 diagnostics。

export 只接受 valid authoritative view。LSP completion 等 editor assist 可以显式查询 recovered view。

`# 3. Syntax categories

syntax diagnostics 分为：

`- structural：indentation、partial dedent、非法 structural tab、verbatim margin。
`- lexical：非法 marker/kind token 或 control character。
`- delimiter/dispatch：非法 introducer entry、attached group、inline 或 verbatim。
`- ownership：旧 attribute slot spelling、unattached group 或 unexpected group close。

attached ownership 使用三个专用 codes：同一 owner 的第二个 group 使用
`"syntax.duplicate-attached-group"，primary range 指向第二个 opening brace，related range 指向第一个 group；
compact block group 后的 non-whitespace source 使用 `"syntax.trailing-after-attached-group"；文件起始的单行
`"{}" 使用 `"syntax.invalid-document-group"。duplicate recovery 保留第一个 group，并将第二个 group 标为
error region；trailing source 不重新并入 head；invalid document group 不产生 document attachment。

同一 source range 和根因只发布最具体的错误。structural error 优先于由错误结构派生的 generic
delimiter error。

Current parser 用 `"syntax.legacy-attributes" 拒绝识别出的旧 attribute slot spelling；该识别只用于
精确指出不合法范围，不产生 legacy syntax view，也不验证旧 spelling 的内部词法或 cardinality。

当前固定 precedence 包括：孤立 block introducer 的 `"syntax.incomplete-introducer" 优先于同 range
的 `"syntax.invalid-inline-dispatch"；`"syntax.invalid-marker" 优先于派生的
`"syntax.invalid-block-dispatch"；verbatim payload margin 的 `"syntax.short-verbatim-indent" 优先于
同 range 的 `"syntax.partial-indent"。独立根因继续按 source/recovery order 发布。

`# 4. Recovery boundaries

`- block-start error：当前 physical line。block column 上独占 physical line 的孤立 introducer
 保持 `"syntax.incomplete-introducer"，并在 recovered typed tree 中建立 empty-marker marked block
 placeholder；它按普通 marked block indentation rule 接管紧随的 children，使输入 marker 首字符前后的
 owner boundary 与 folding range 保持稳定。该 placeholder 不进入 valid authoritative view；ordinary inline
 position 的孤立 introducer 不建立 block owner。
`- attached block group：matching owner boundary 或 dedent；marked block 的 next-line opener 在 blank、
 owner-level dedent、children start 或不匹配的 continuation column 前停止建立 ownership；attached inline
 group：当前 enclosing inline boundary。
`- unattached group/unexpected close：当前 structural line 或 enclosing inline boundary。
`- parsed inline error：blank、dedent、block-only entry 或 EOF 前。
`- inline verbatim error：当前 physical line ending 或 EOF。
`- partial dedent：补到最接近的合法 outer column 后继续当前 line。

recovery 的目标是报告后续独立错误并保留 source，不是把 malformed spelling 当作 text。

`# 5. Semantic categories

official semantic profile 至少区分：

`- heading/anchor：非法 heading level、duplicate anchor。
`- metadata：duplicate/invalid key、invalid location、unsupported 或 mixed value shape，以及
 `"metadata.invalid-created" RFC 3339 timestamp warning。
`- link/reference：缺失或无效 target、conflicting autolink property、missing document/anchor、
 ambiguous anchor 或 unresolved file。
`- image：missing/invalid src 或 unresolved local file。
`- file：missing/invalid src 或 unresolved local attachment。
`- citation：空 id、nested 或 unsupported citation shape，无效/缺失 bibliography file、无效
 CSL JSON、duplicate id，以及无 bibliography 或已声明 bibliography 时 unresolved 或 ambiguous citation id。
`- task：`"task.invalid-datetime"、`"task.invalid-priority"、无效 recur/reference、互斥 closure state、
 self dependency、cycle、blocked completion、`"task.done-with-open-dependency" 和
 `"task.done-with-open-descendant" closure consistency warnings。
`- event：missing/invalid title/time/date/timezone、`"event.invalid-uid" empty explicit UID、invalid interval，以及
 invalid/unresolved/ambiguous/non-task reference。
`- math：invalid owner 或 unsupported input language。

semantic diagnostic 应指向最小 source-backed field range。由多个 documents 共同形成的问题可以
携带 related locations。

两条 task closure consistency warnings 的 primary range 是 parent `"done" value；同文件 open
dependency/descendant 使用 related ranges。跨文件 open dependency 计入 message 数量，但当前
protocol-neutral related range 不携带 path，因此不伪造到 source document。

`# 6. Stability

diagnostic code 是 editor、test 和 automation 的接口；重写 message 不应改变 code。新增更精确
diagnostic 时，应更新 syntax/semantic fixtures，并验证 recovery 后续 diagnostics 不回退。

strict parser normative corpus 固定 typed structural projection、code、primary/related ranges、
precedence、ordering、recovery 与 lossless token coverage；lenient tree-sitter corpus 不能代替它。

当前 core codes：

`text"
  syntax.incomplete-introducer
  syntax.invalid-block-dispatch
  syntax.invalid-inline-dispatch
  syntax.invalid-verbatim-block-dispatch
  syntax.invalid-marker
  syntax.legacy-attributes
  syntax.unclosed-attached-group
  syntax.duplicate-attached-group
  syntax.trailing-after-attached-group
  syntax.invalid-document-group
  syntax.unattached-group
  syntax.unexpected-group-close
  syntax.unclosed-inline
  syntax.unclosed-verbatim
  syntax.tab-indentation
  syntax.partial-indent
  syntax.short-verbatim-indent
