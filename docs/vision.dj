# plumb 设计愿景

> 工作名 `plumb`（strict markup）与扩展名 `.plumb` 均为占位，可改。

## 为什么要造这门语言

现有的 Djot / Markdown **容错太强**：解析器几乎从不报错，把任何输入都解析成
"某种东西"。`*加粗` 忘了闭合不会报错，而是静默变成字面量 `*加粗`；缩进差一个
空格、有序列表序号乱写、标题跳级，都被"温柔"地接受。对个人长期维护的文档来说，
这种容错意味着**错误被藏起来、格式随时间漂移**。

plumb 的目标是反过来：**严格。** 一份文档要么完全合法、语义唯一确定，要么在你写错
的当下就被明确拒绝并精确报错。

## 核心公理

- **一种意思只有一种写法，一种写法只有一种意思。**
- **特殊字符永远是特殊的。** `_ * ` [ ] ( ) < \` 等在任何位置都有语法含义；要
  字面量必须转义。没有"上下文里恰好不构成结构就降级成普通字符"这种妥协。
- **不闭合 / 不合法 = 报错**，绝不静默降级为字面文本。这是与 Djot 最大的区别。
- **reject-but-recover。** 解析器遇错报一条带字节范围的诊断，并恢复到下一个安全
  边界继续扫描，以便一次报全所有错误；只要存在任何 error，导出即拒绝出文档。

## 非目标

- 不追求表达力最大化，不追求与 Djot/Markdown 生态兼容或竞争。
- 不是给别人用的通用语言；这是个人工具，可以为"清晰、严格"牺牲便利。
- 不为了迁就某个解析技术（尤其 tree-sitter）而扭曲语言设计。

## 生态策略：靠"导出"而非"选流行语法"

对"生态小、没工具"的顾虑，答案不是去基于 Markdown（那会继承比 Djot 更严重的
容错与歧义），而是：

- **可编辑**：LSP（`plumb-ls`）提供诊断、大纲、跳转、补全、semantic tokens；
  tree-sitter（`tree-sitter-plumb`，第二阶段）提供高亮、文本对象、折叠、injection。
- **可预览 / 可输出**：`plumb-export` 让 plumb 成为自己的 pandoc *reader*——解析器
  直接产出 pandoc JSON AST，再交给 `pandoc` 当 *writer* 输出 PDF/HTML/docx/md。
- **不被锁死**：任何时候都能经由 pandoc 把内容迁出。

也就是说：**工具支持 = tree-sitter + LSP + pandoc 导出**，无需额外生态。

## 两套解析器原则（重要）

这是本项目的架构基石：

- **手写严格解析器 = 唯一权威。** 负责正确性、精确报错、导出。它严格、会拒绝。
- **tree-sitter = 有意宽松，只服务编辑器人体工学。** 永远不做严格性引擎。

为什么 tree-sitter 当不了权威，两个根本原因：

1. tree-sitter 架构上就是 **error-tolerant**：遇错插 `ERROR`/`MISSING` 节点并
   恢复，**永不拒绝**，也给不出"第 3 行的 `_` 未闭合"这种精确信息。
2. plumb 的相当一部分严格规则是**跨节点的上下文相关约束**（紧凑/松散不得混用、块间
   恰好一个空行、定义列表 key 唯一、未知角色校验……），**CFG 根本表达不了**，只能
   作为语义检查活在手写解析器里。详见 `docs/spec.dj` 的"语法 vs lint"分类。

一个反直觉但正确的推论：**你希望 tree-sitter 是宽松的。** 编辑时文档大部分时间
处于临时非法状态（你正打一半），若 tree-sitter 也严格，高亮/文本对象会一直闪断。
严格性交给手写解析器，流畅编辑体验交给宽松的 tree-sitter——这不是矛盾，而是分工。

## 小内核 + 注册式可扩展语义（架构基石）

plumb 的语言本体分两层：

- **语法内核**：一组**极小且固定**的结构原语（段落、标题、列表、代码块、引用、
  分隔线、定义列表、通用容器）+ 统一的 `{...}` 属性语法 + 行内基本元素。这一层
  **永不为新语义而增长**。
- **语义注册表**：所有额外语义（meta、note/warning、figure、citation……）都是
  **注册过的具名角色/指令**，通过 `{...}` 附加到通用容器或 span 上，如 `:::{note}`、
  `[x]{cite}`。注册表为每个角色声明可附加位置、允许/必需的属性、以及到 HTML/pandoc
  的降级。

严格性据此分工：内核语法只做结构解析，**语义的校验落在注册表**——未知角色、缺必需
属性、放错位置都报错。"非法即拒绝"依然成立，只是从"语法认识每个构造"变成"小内核 +
被校验的扩展协议"。

两个好处：(1) 加任何新语义都**不用改语法**，tree-sitter 语法因此又小又稳；(2) 你能在
配置里声明自己的角色而不碰解析器。守住一条线：内核只放"人人都要、值得专用语法"的
结构原语；领域语义一律走角色扩展，别把常用结构也塞进通用容器，否则可读性会崩。

## 与 djot-tools 的关系

plumb 脱胎于 `djot-tools`，复用其 crate 划分与 LSP 工程经验，但**不依赖 jotdown、
不与其共享依赖**（LSP 胶水从 `djot-ls` 拷贝而非抽公共 crate），保持两个项目解耦。
`docs/features.dj` 保证 djot-tools 现有的全部功能都在 plumb 的路线图内。
