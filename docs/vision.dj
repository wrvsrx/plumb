# plumb 设计愿景

> 工作名 `plumb`（strict markup）与扩展名 `.plumb` 均为占位，可改。

## 为什么要造这门语言

现有的 Djot / Markdown **容错太强**：解析器几乎从不报错，把任何输入都解析成
"某种东西"。`*加粗` 忘了闭合不会报错，而是静默变成字面量 `*加粗`；缩进差一个
空格、tab 与空格混用、行尾藏着空格，都被"温柔"地接受。对个人长期维护的文档来说，
这种容错意味着**格式错误被藏起来、随时间漂移**。

plumb 的目标是反过来：**语法严格**。一份文档要么**语法完全良构**，要么在你写错
的当下就被明确拒绝并精确报错。

## 核心公理

- **一种意思只有一种写法，一种写法只有一种意思。**
- **特殊字符永远是特殊的。** `_ * ` [ ] ( ) < \` 等在任何位置都有语法含义；要
  字面量必须转义。没有"上下文里恰好不构成结构就降级成普通字符"这种妥协。
- **不闭合 / 不合法 = 报错**，绝不静默降级为字面文本。这是与 Djot 最大的区别。
- **reject-but-recover。** 解析器遇错报一条带字节范围的诊断，并恢复到下一个安全
  边界继续扫描，以便一次报全所有错误。
- **严格只针对语法，不针对语义**（见下"语义中立的内核"）。

## 语义中立的内核 + extension（架构基石）

这是本项目最重要的架构决定，采用 **Pandoc / Docutils 的模型**：

- **core = 严格的 reader。** 把源码解析成一棵**通用的、不带任何解释的文档树**
  （块、行内、容器、`{#id .class k=v}` 作为**不透明属性**、定义列表作为通用
  scalar/list/map 数据）。它**只判定语法是否良构**，产出纯语法诊断。**core 不含
  任何语义**：不知道 `.meta`/`.note` 是什么、不解析链接目标、不校验 class 名、
  不生成 id、不做锚点/引用/大纲。
- **extension = filter / transform。** 消费这棵语义中立的树，添加**全部**语义：
  元信息折叠、链接/锚点解析与反向引用、id 生成、任务、编号、以及到 HTML/pandoc 的
  降级。**导出器本身就是一个 extension。**

一句话：**树是语义中立的，意义活在 extension 里。**

### 由此得到的性质（全部命中诉求）

- **core 极小且稳定**：加任何新语义都不碰 core、不改语法。
- **无注册表、无角色、无 class 校验**：`.class` 与属性对 core 是不透明串，自由、
  不报错。"只允许已知 class"若你想要，那是某个 extension 的策略。
- **严格 = 严格语法 + 宽松语义**：core 会欣然接受 `[x](乱码)`、`{.tpyo}`——它们
  语法合法。这类语义问题只在你运行相应 extension（链接解析器等）时才被抓。
- **extension 形态**：先做**编译期 Rust transform**（消费树、产出诊断 + 变换的
  trait）；meta / links / tasks / 导出各是一个 extension。暂不做动态插件。

## 两套解析器原则（重要）

- **手写严格解析器 = 唯一权威。** 负责语法正确性、精确报错、产出树。它严格、会拒绝。
- **tree-sitter = 有意宽松，只服务编辑器人体工学。** 永远不做严格性引擎。

为什么 tree-sitter 当不了权威：

1. tree-sitter 架构上就是 **error-tolerant**：遇错插 `ERROR`/`MISSING` 节点并
   恢复，**永不拒绝**，也给不出"第 3 行的 `_` 未闭合"这种精确信息。
2. plumb 一部分语法约束是**跨节点的上下文相关**（紧凑/松散不得混用、块间恰好一个
   空行、定义列表结构良构……），**CFG 表达不了**，只能作为语法 lint 活在手写解析
   器里。详见 `docs/spec.dj` 的"语法 vs lint"分类。

由于 core 语义中立，**core 与 tree-sitter 覆盖同一范围（纯语法）**，只差"严格 vs
宽松"——这让分工格外干净。

一个反直觉但正确的推论：**你希望 tree-sitter 是宽松的。** 编辑时文档大部分时间
处于临时非法状态（你正打一半），若 tree-sitter 也严格，高亮/文本对象会一直闪断。

## 生态策略：靠"导出"而非"选流行语法"

对"生态小、没工具"的顾虑，答案不是基于 Markdown（那会继承更严重的容错与歧义），
而是：

- **可编辑**：LSP（`plumb-ls`）提供诊断、大纲、跳转、补全、semantic tokens；
  tree-sitter（`tree-sitter-plumb`，第二阶段）提供高亮、文本对象、折叠、injection。
- **可预览 / 可输出**：`plumb-export`（一个 extension）产出 pandoc JSON AST，交给
  `pandoc` 当 writer 输出 PDF/HTML/docx/md。
- **不被锁死**：任何时候都能经由 pandoc 迁出。

## 非目标

- 不追求表达力最大化，不追求与 Djot/Markdown 生态兼容或竞争。
- 不是给别人用的通用语言；这是个人工具，可以为"清晰、严格"牺牲便利。
- 不为了迁就某个解析技术（尤其 tree-sitter）而扭曲语言设计。

## 与 djot-tools 的关系

plumb 脱胎于 `djot-tools`，复用其 crate 划分与 LSP 工程经验，但**不依赖 jotdown、
不与其共享依赖**（LSP 胶水从 `djot-ls` 拷贝而非抽公共 crate），保持两个项目解耦。
注意分层差异：djot-tools 把分析放在 `djot-core`，而 plumb 把**全部语义下沉为
extension**，core 只做严格解析。`docs/features.dj` 保证 djot-tools 现有的全部功能都
在 plumb 的路线图内（多数落在 extension 层）。
