# plumb 语法规范（MVP 草案 v0）

> 本文是设计草案，非最终规范。工作名 `plumb`、扩展名 `.plumb` 为占位。
> 全程对 Djot 做"减法 + 把容错改成报错"。

## 0. 设计公理

- 一种意思只有一种写法，一种写法只有一种意思。
- 特殊字符永远特殊：`_ * ` [ ] ( ) < \` 在任何位置都有语法含义，字面量须转义。
- 不闭合 / 不合法 = 报错，绝不静默降级成字面文本。
- 解析器 reject-but-recover：报带字节范围的诊断并恢复以继续报后续错误；有任何
  error 即拒绝出文档。
- **优先用语法让非法状态无法表达，而非事后 lint 检测**（例：有序列表用无数字标记
  `#.`，源码里无从写错序号，而不是"检测序号写错"）。

## 1. 词法与文档级规则

- 文件编码必须 UTF-8。
- 换行只允许 `LF`；出现 `CRLF` / `CR` → 错误。
- 缩进及一切空白只允许空格；出现 `Tab` → 错误（消灭 tab/空格歧义）。
- 任何行行尾不得有空格 → 错误（消灭 Markdown "两个空格换行"的隐形语法）。
- 空行 = 零字符的行。只含空格的行因上一条已是错误。
- 文件必须以恰好一个 `LF` 结尾；无结尾换行或多个结尾空行 → 错误。

## 2. 块级语法

- 块之间以恰好一个空行分隔；连续 ≥2 个空行 → 错误。
- 块类型由无歧义的行首前缀唯一决定。

### 2.1 标题

```
## 标题文本
```

- `#`×N（1–6）+ 恰好一个空格 + 非空文本，到行尾。
- 无闭合 `#`，无 setext（下划线式）标题。
- 允许层级跳级：编译目标是 HTML，`<h1>`–`<h6>` 本就可跳级，不设约束。大纲嵌套遇到
  跳级时，让深层标题直接挂在最近的上层标题下即可。
- 可选显式锚点：`## 标题 {#custom-id}`，`{` 前恰好一个空格；否则由文本自动生成
  id。

### 2.2 段落

- 一段是若干连续非空行；段内软换行渲染为空格。
- 段内硬换行用行尾反斜杠 `\`（显式，替代 Markdown 隐形的两空格）。

### 2.3 代码块（仅围栏式）

- 开栏为 N（≥3）个反引号，可紧跟单个语言标识符 `[A-Za-z0-9_-]+`（无空格）；有
  空格或多 token → 错误。
- 闭栏必须是恰好 N 个反引号独占一行；数量不匹配或到 EOF 未闭合 → 错误。
- 只允许反引号围栏（无 `~~~`），内容原样，不解析。

### 2.4 引用块

```
> 被引文本
> 第二行也必须带前缀
```

- 每一行都必须有 `> ` 前缀（`>` + 一个空格）；引用内空行写作单独的 `>`。缺前缀的
  续行 → 错误（消灭 lazy continuation）。
- 嵌套用 `> > `。

### 2.5 列表

- 无序标记只有 `- `（连字符 + 一个空格）；`*`/`+` → 错误。
- 有序标记为 `#. `（`#` + `.` + 一个空格），**不含数字**——序号由渲染器自动生成
  （HTML `<ol>`），源码里无从写错。参考 reStructuredText 的 `#.`。与标题 `# ` 无
  歧义（`#`+空格 vs `#`+`.`）。
- 续行与子块缩进必须恰好等于标记宽度（`- ` 为 2 空格，`#. ` 为 3 空格），差一个
  空格 → 错误。
- 紧凑/松散不得混用（见 §5，属 lint）。
- 子块在标记宽度缩进下套用标准块规则。MVP 是否支持项内多块内容见开放问题。

### 2.6 分隔线

- 恰好 `---`（三个连字符）独占一行；其它写法 → 错误。

### 2.7 定义列表（字段列表）

行首 `:` 标记的字段列表，用 标量 / list / map 表达结构化数据（JSON 级表达力），
编译到 HTML `<dl>`。它也是元数据的载体（见 §4）。

```
:name: 张三
:roles:
  - 作者
  - 维护者
:contact:
  :email: z@example.com
  :city: 北京
```

- 标量：`:key: 值`（`:` + key + `: ` + 值到行尾；值为字符串）。
- map：`:key:`（冒号后直接换行）+ 缩进 2 空格的子字段 `:subkey: ...`。
- list：`:key:` + 缩进 2 空格的 `- 元素`；元素可为标量 / map / list。
- 行首 `:` 是唯一入口；`:` 开头却不构成合法字段行 → 错误；正文要以字面冒号开头写
  `\:`。
- `- ` 仅在定义列表内部（缩进于某 `:key:` 下）表示 list 值；正文顶层的 `- ` 仍是
  普通无序列表——两者不撞，因为定义列表必须由 `:key:` 行进入。
- 缩进恰好 2 空格/层；`:` 后恰好一个空格。
- 标量一律字符串，**禁隐式类型**（无 `no→false`、`1.20→1.2` 这类 YAML 坑）。
- 同一 map 内 key 唯一（重复 → 错误）；同层不得混用 `- `（list）与 `:key:`（map）
  → 错误。

## 3. 行内语法

出现在段落、标题、列表项、引用文本中。

- 强调 / 加粗：`_斜体_`、`*粗体*`。
  - 必须在同一块内闭合；未闭合 → 错误（旗舰特性，绝不降级为字面量）。
  - 开定界符后须紧跟非空格、闭定界符前须紧邻非空格：`_ x_` → 错误。
  - 同类不可自嵌套；异类可嵌套：`*粗 _又斜_*`。
- 行内代码：`` `code` ``，可用 N 个反引号配对以容纳内部反引号；未闭合 → 错误；
  内容原样。
- 链接：只有行内式 `[文本](目标)`。
  - 无引用式、无隐式/快捷链接。
  - 目标为 URL、内部 `#id`、相对 `path` 或 `path#id`。
  - `[文本]` 后缺 `(...)`、括号未闭合 → 错误。
- 自动链接：`<https://…>`；裸 `<` 非法自动链接 → 错误（`<` 永远特殊）。
- 转义：`\` + 任一 ASCII 标点 = 该标点字面量；`\` 后跟不可转义字符 → 错误（无游离
  反斜杠）；`\` + 行尾 = 硬换行。

## 4. 元数据（元信息）

不引入外部格式（无 TOML/YAML 依赖）：元信息复用 §2.7 的定义列表——一个定义列表带
`{.meta}` 类时提升为文档元信息，折叠进 pandoc `Meta` 而不渲染进正文。

```
{.meta}
:title: 我的文档
:date: 2026-07-02
:tags:
  - todo
  - draft
```

- `{.meta}` 是**块属性行**，`{` 独占一行、紧接在定义列表上方（块属性的通用写法，
  见 §7-Q6）。
- **不依赖位置**：带 `{.meta}` 的定义列表出现在文档任何位置都被提升为元信息。
- 全文至多一个 `{.meta}` 定义列表；出现第二个 → 错误。
- `{.meta}` 加在非定义列表的块上 → 错误。
- 值类型映射：标量 → MetaString，list → MetaList，map → MetaMap。

## 5. 语法 vs lint 分类（写解析器/tree-sitter 前必读）

严格规则分三类，决定它们分别活在哪一层：

### 5.1 纯 CFG（tree-sitter 语法可直接表达）

- 标题 `#{1,6} `、段落、分隔线 `---`
- 链接 `[..](..)`（含嵌套括号，平衡括号是标准上下文无关语言）
- 转义序列
- 定义列表单行字段 `:key: 值` 的入口（行首 `:`）、块属性行 `{...}`

### 5.2 需外部 scanner（自定义 lexer / 栈式状态）

- 匹配长度的代码围栏（N 个反引号开 = 闭）
- 行内代码的 N 反引号配对
- 列表 / 引用块的精确缩进与块延续
- 定义列表的缩进嵌套（map/list 层级与值归属）
- 强调 / 加粗定界符的成对识别

### 5.3 CFG 表达不了 —— 只能是语义 lint，活在手写解析器

这些是跨兄弟 / 跨块的上下文相关约束，**不进 grammar.js**，只作为诊断：

- 紧凑 / 松散不得混用
- 块间恰好一个空行
- 无行尾空白 / 无 tab / 仅 LF（词法级，tree-sitter 也不会主动为它们报错）
- 定义列表同一 map 内 key 唯一；`{.meta}` 全文至多一个、且只能加在定义列表上

（注：原先的"标题不得跳级""有序序号必须连续"两条已被删除——前者放开，后者用无数字
标记 `#.` 从语法层消除。这正是 §0"用语法让非法状态无法表达"原则的体现。）

结论：手写解析器负责 §5.1–§5.3 全部并给出精确诊断；未来的 tree-sitter 只覆盖
§5.1 + §5.2，且刻意保持宽松，仅服务编辑器人体工学。

## 6. 相对 Djot 明确砍掉的容错（摘要）

setext 标题、lazy 引用续行、`*`/`+`/`~~~` 多写法、引用式/隐式链接、未闭合定界符
降级为文本、两空格硬换行、tab 缩进、任意数量空行、有序列表源码数字（改用无数字
`#.`）、行尾空白 —— 全部改为报错或删除。（注：标题跳级不在此列，已放开。）

## 7. 开放问题（待拍板）

1. 名字与扩展名（`plumb` / `.plumb` 为占位）。
2. ~~标题跳级 / 有序序号严格度~~ **已定**：标题允许跳级（HTML 目标）；有序列表改用
   无数字标记 `#. `，从语法层消除序号错误，无需 lint。（`#.` 若想换成 `. ` 可再议。）
3. ~~元数据形式~~ **已定**：复用定义列表（§2.7）+ `{.meta}` 类提升（§4），不引入
   外部格式，不依赖位置。
4. MVP 是否支持列表项内的多块内容（多段 / 代码块），还是先只支持"一行文本 + 嵌套
   子列表"。
5. 强调/加粗字符分配：`_`=斜体、`*`=粗体（与 Djot 一致）是否 OK。
6. 属性系统：MVP 已因 `{.meta}` 引入最小**块属性行** `{...}`（至少 `.class` 解析）。
   仍待定：行内 span 属性 `[文本]{.class}`、块属性完整键值，以及块属性行 `{...}` 与
   标题尾随 `{#id}` 两种放置方式是否统一。
