`= title plumb incremental transport and parsing results, 2026-09

`# Scope

本记录比较 LSP full/incremental document transport，以及 current hand-written strict parser 的 fresh 与
previous-revision incremental paths。测量基于 `"c830451" 后的 topic worktree；实现提交只改变 transport、
syntax parsing、workspace revision installation、tests 与 benchmark，不改变 document semantics。

Host 为 Intel Core i9-9980XE、NixOS、Linux 6.18.48、rustc 1.97.1。Rust benchmark 使用 release profile。
transport microbenchmark 预热 750 ms、15 samples、报告 median；Criterion 预热 1 s、measurement 约 5 s、
10 samples，并报告 confidence interval midpoint。current deterministic fixture 为 2,112,527 bytes、
170,914 lines、33,512 events 与 3,351 links；历史 5.7 MB workload 通过增加 title payload 得到相同结构数。

`# Incremental transport

2.11 MB fixture 的 full JSON serialization、stdin framing/pipe、typed deserialization 合计约 4.55 ms；
incremental payload 加 current source clone/apply 约 0.114 ms，每次 edit 节省约 4.43 ms。5.66 MB padded
fixture 从约 10.24 ms 降到 0.341 ms，每次 edit 节省约 9.90 ms。1.86 KB fixture 只从约 13.4 us 降到
11.4 us，绝对收益可忽略。

该 profile 使用 Rust serde_json 而非 Neovim encoder；pipe receiver 只读取正确 Content-Length framing 并
acknowledge body，不包含 async-lsp dispatch。结论只支持减少大文档 payload、copy 与 JSON/stdio work，不把
transport savings 归因于 parser 或 semantics。

`# Incremental strict parser

`"incremental_parse_33512" 在 start、middle、end 附近替换一个 ASCII title byte。每项 timing 外验证
incremental ParsedDocument 与 fresh parse 完全相等；reparsed owner range 分别只有 56、60、60 bytes。

`- start：fresh 79.61 ms；incremental 63.89 ms。
`- middle：fresh 84.48 ms；incremental 61.14 ms。
`- end：fresh 83.30 ms；incremental 52.59 ms。

当前实现因此减少约 16--31 ms strict parse work，但仍复制全部 reused owned blocks/tokens，并对 shifted suffix
执行 absolute-range rebase；它不是 green-tree allocation reuse。`"open_document_generation_33512" 的完整
workspace didChange parse-stage midpoint 为 83.56 ms，与此前 full-path 83.7 ms baseline 没有显著差异。
snapshot installation、owned payload lifecycle 与其它 O(document) work 已成为主要成本，因此不能宣称
end-to-end latency 按局部 owner 大小缩放。

`# Green tree prototype

Benchmark-only prototype 将每个 top-level source region 保存为 relative-range immutable shard；revision root 只
持有 source 与 `"Vec<Arc<GreenShard>>"。它不进入正式 plumb-syntax API 或 workspace path。初次 fixture 产生
36,865 shards；三个单字节 edits 都只替换一个 56--60 byte shard，并 pointer-reuse 36,864 shards。
materialize 在 timing 外验证 source、typed tree、document declarations、lossless tokens、diagnostics 与 fresh
parse 完全相等；额外 cases 覆盖 metadata、CRLF、indentation ownership、verbatim、invalid recovery、insert 与
delete revision。

从完整 old/new source 自行计算共同 prefix/suffix 时，green revision midpoint 为 4.27--4.69 ms。直接使用
incremental transport 已知的 old-byte changed range 后，该成本降为 0.665 ms、0.728 ms 与 0.788 ms。由此可见
约 3.5--4 ms 来自丢弃 edit provenance 后重新扫描 2.11 MB source，而 relative-range shard parse 与 root splice
本身低于 1 ms。

若立即 materialize 为现有 absolute-range owned ParsedDocument，排除 result destruction 的 midpoint 仍为
60.57--61.05 ms；green revision 加 materialization 为 65.83--67.54 ms。将 owned result destruction 计入同一
iteration 后为 105.86--108.16 ms。因而 green storage 不能只放在 parser 内部并继续向所有 consumers eager
materialize 旧 API；只有 typed red views 直接遍历 shared green nodes，才能保留 sub-millisecond revision cost。

prototype fresh build 先运行现有 fresh parser，再把 nodes/tokens 移入 36,865 个 individually allocated shards，
midpoint 为 143.41--158.85 ms；这不是 production green parser 的目标成本。正式实现应在一次 parse 中直接 emit
green nodes，并选择 hierarchical root/chunk allocation，避免每个顶层 region 的多组独立 Vec allocation。

`# Correctness and next boundary

普通 tests 覆盖 sequential UTF-16 changes、full replacement fallback、rangeLength validation、CRLF、surrogate
boundary、invalid notification rejection、valid/invalid recovered revisions、indentation ownership、continuation、
verbatim、文件首尾 edit 与 deep-tree full fallback。Property tests 比较 arbitrary revisions，并在结构化多 owner
documents 中强制局部 reuse；character-boundary regression 对代表 source 的每个 UTF-8 boundary 执行 insertion
与 deletion。所有结果比较 source、typed tree、lossless tokens、diagnostics 与 validity。

实验支持继续设计 production green tree，但前提是 edit byte ranges 从 transport 贯穿 workspace/syntax boundary，
并让 typed red views 按需计算 absolute range；仅替换 parser storage 后 eager materialize 没有足够收益。下一阶段
还必须分别测量 fresh parse、snapshot lifecycle 与 full document semantics。当前 semantics 仍约 168 ms，syntax
revision 降到 sub-millisecond 后会成为下一主要瓶颈；仅将 tree-sitter incremental CST 放在 strict parser 前面
不构成收益。

`# Production phase 1

后续 implementation 将 top-level relative green shards提升到 plumb-syntax API，并让 LSP 单 ranged change 的 byte
provenance贯穿 workspace。2.11 MB fixture 的 production green fresh parse midpoint 为 98.5--99.1 ms，known-edit
revision 为 0.779--0.880 ms；materialize旧 absolute ParsedDocument 仍需 62.6--63.5 ms，因此 workspace 主线程暂时
继续安装现有 incremental ParsedDocument，green cache在 didOpen 后由 blocking worker预热。

semantic component profile 的 midpoint：events 78.18 ms、lists 15.13 ms、headings 1.58 ms、metadata 2.25 ms、
citations 3.16 ms、inline styles 3.24 ms、math 4.69 ms、quotes 1.50 ms、tasks 4.80 ms、tables 1.58 ms，完整
DocumentOutput 166.10 ms。phase 1 将 events、citations/styles/math/quotes/tasks 与 lists保存为 syntax-identity keyed
relative semantic shards；event date/timezone形成 explicit context key，context变化时不复用。

完整 open-document semantic baseline 在同轮测量约 171.7 ms。edit抢在 cache warm 前到达时 absolute incremental
fallback 为 79.68 ms；warm green steady-state 为 66.27 ms，相对完整分析减少约 61%。didOpen后的独立
`"document_cache_warm" worker 为 256.94 ms，不阻塞 event loop；stale cache通过 path/revision/parsed/output
identity拒绝。green event revision自身为 4.43 ms；旧 EventOutput eager materialization 为 18.29 ms，phase 1
已让 open revision持有 lazy green event records与 list groups，不在 semantic analysis阶段执行该投影。

这些结果仍不是完整 red-view architecture。headings、metadata、tables、association与 resource records保留现有
reducers；compatibility ParsedDocument 仍在主线程构造。后续任务分别迁移 syntax consumers与剩余 semantic
reducers，并用 exported-summary equality缩小 workspace invalidation。

`# Subsequent optimization checkpoint

本节记录截至 commit `"3c51835" 的后续测量，不覆盖前面各阶段的历史边界。当前实现已删除 workspace 的
absolute ParsedDocument cache；现行 contract 见 `->{{semantic analysis} ../../architecture/semantic-analysis.plumb}。
本机当前环境为 Intel Core i9-9980XE、Linux 6.18.49、rustc 1.97.1；以下均为 release profile。
历史对照并非全部在同一 kernel 下测量，不作为跨环境保证。

Criterion 项使用 1 s warmup、约 5 s measurement、10 samples。完整响应 profile 使用 5 次 warmup、20 次
samples；task-target profile 使用 2 次 warmup、10 次 samples，cold batch 只有单次观察。

`- `"export_task_event_lookup_2000"：2,000 task 与 2,000 event 完整导出，旧线性 owned 查询约 2.1085 s，
 borrowed exact-start 查询约 95.1 ms。测量包含 parse、semantics、Pandoc JSON construction 和结果销毁。
`- `"export_table_lookup_1000"：1,000 tables 完整导出，171.6 ms 降至 53.7 ms，先定位再投影命中 records。
`- `"export_list_groups_2000"：2,000 个由 paragraph 分开的 list groups，304.4 ms 降至 31.1 ms。
`- `"open_event_title_candidates"：33,512 events，query 为 `"Event 12"，16.98 ms 降至 1.34 ms。
`- `"profile_semantic_token_response"：2,000 closed tasks、6,000 tokens、64,000 JSON bytes；旧位置转换
 943.21 ms，共享 line index 后 0.870 ms。包含 token range generation、UTF-16 encoding 和 JSON serialization，
 不包含 document analysis、transport 或 Neovim rendering。
`- `"profile_document_symbol_response"：2,000 heading/task pairs 从 7.67 ms 降至 4.28 ms；4,000 pairs 从
 24.0 ms 降至 8.32 ms。包含 symbol projection、树构造和 JSON serialization，不包含 analysis 或 client rendering。
`- `"incremental_revision_pipeline"：33,512-record fixture 曾从历史 4.13 ms 回退至 13.07 ms。分阶段测得
 首次 incremental analysis 约 2.82 ms、安装约 8.29 ms；shared segment equality 将 summary comparison 从
 2.48 ms 降至 0.191 ms，graph-input 比较复用同一路径后完整 pipeline 为 4.56 ms。安装回退大部分已消除，
 不能宣称严格恢复历史值；不同子基准缓存状态不同，不直接相减归因。
`- `"profile_cycle_membership_batch"：2,000-node chain，全节点逐次 DFS 为 297.25 ms，SCC 构建加全部成员
 查询为 0.555 ms。仅测 cycle membership，不含 graph construction、target resolution 或完整 diagnostics。
`- `"profile_open_task_target_resolution"：1,000/2,000 targets 的旧查询约 10.54/50.26 ms，anchor id 索引后
 warm 约 0.93/1.45 ms；首次整批查询含 index construction 观察到 1.73/2.16 ms。cold 数字不是中位数。

新增基准位于 `"crates/plumb-workspace/benches/semantic_store.rs"；manual profiles 位于 LSP/workspace unit tests，
使用 `"cargo test --release" 配合指定 test name 和 `"-- --ignored --nocapture" 运行。所有 profile 保留结果或
record-count 校验，普通 CI 不以 wall-clock 阈值判定正确性。

`# Production semantic stage profile

本节记录commit `"801854b3921b1ddde42f14ecf5620b3911ab26f8"，测量时间2026-09-07T19:46:55+08:00。
host为pba，Intel Core i9-9980XE（18 cores/36 threads），NixOS/Linux 6.18.49 x86_64，rustc 1.97.1
（8bab26f4f，LLVM 21.1.8），Cargo release profile。最终采样期间没有并行agent构建或benchmark。

`"profile_production_semantic_stages" 使用mixed-2000 generated fixture：314,461 bytes，2,000 Task、2,000 Event、
2,000 Link、2,100 Anchor、50 Table及50 definition lists。source SHA-256为
`"0bac372200304fdf8efc4705f22c5ac5924a79fd53162fec71caa2e9c88dfd9a"。Event样本把中间event的Event改成Later；
Equal样本把中间普通paragraph的Body改成Text，二者均等宽并传递known byte edit。该fixture有definition lists，
metadata/context preparation会走完整metadata/definition检查，不代表所有文档的metadata成本。

每种edit/lifetime组合各5次warmup、30个samples，normal/profile交替成对执行；测试总运行时间17.88 s。
normal是同一feature-enabled binary中的no-op observer入口，不读取内部阶段时钟；profile使用同一生产算法。
报告p50/p95观察值，不把它们当置信区间或跨机器保证；不同阶段分位数之和不是端到端分位数。

阶段边界见 `->{{benchmark methodology} ../../project/performance-benchmarks.plumb}。syntax包括begin revision、validity和
workspace state切换；publication仅 `"install_document_analysis_with_impact"，不含LSP通知、dependency queries或SQLite
落盘。absolute projection只collect Anchor/Link/Task/Event/root diagnostic owned records，不含metadata/table投影、
LSP position/JSON、transport或editor rendering。旧revision初次build与next source clone在计时外。

以下为profile入口的阶段耗时，单位us，每格是p50/p95。final表示没有旧revision或外部workspace/cache持有者；
shared表示保留旧revision并释放新revision，不是释放旧revision并保留新revision。

`table
 `- Stage {Event/final} {Event/shared} {Equal/final} {Equal/shared}
  `+ header
 `- Syntax 211.902/239.149 213.740/233.019 213.646/248.403 218.071/251.235
 `- Metadata/context 1037.464/1073.526 1026.445/1080.048 1022.551/1113.354 1029.550/1130.371
 `- {Local nodes/bookkeeping} 359.584/380.881 373.654/425.079 368.113/409.866 373.823/436.098
 `- {Document reduction} 274.942/296.374 301.747/345.175 1.811/2.339 1.780/2.384
 `- {Semantic total} 1660.382/1731.055 1733.091/1809.364 1394.739/1519.731 1411.805/1568.861
 `- {Workspace install} 791.241/829.356 476.318/513.988 45.447/63.683 31.551/35.654
 `- {Owned projection} 5128.341/5313.920 5031.919/5614.232 5552.584/5777.828 5429.297/5895.462
 `- {Projection destruction} 847.559/966.413 805.447/884.536 878.530/971.871 864.494/1257.059
 `- {Workspace detach} 1.716/2.243 1.713/1.954 1.718/2.112 1.754/2.352
 `- {Current root/tree/syntax destruction} 11552.264/12093.167 354.249/386.935 12046.386/12673.109 299.886/356.444
 `- {Complete pipeline} 20274.426/21094.885 8797.272/9308.941 20075.920/22628.512 8295.505/8835.039

同一完整pipeline的normal/profile对照，单位ms，每格为p50/p95：

`table
 `- Case Normal Profile
  `+ header
 `- Event/final 20.319/20.860 20.274/21.095
 `- Event/shared 8.642/9.170 8.797/9.309
 `- Equal/final 20.110/21.925 20.076/22.629
 `- Equal/shared 8.423/9.781 8.296/8.835

p50差异约-1.5%至+1.8%，不据此声称profiling提速。short-circuit确实把document reduction从约0.3 ms降为
约1.8 us，但本fixture的owned projection仍约5–6 ms；只报告local/reducer成本不能代表完整消费路径。

timed destruction前移除workspace entry、释放Pending/Prepared和其它外层holders，并验证内部SemanticRoot强引用计数为1。
独立untimed测试以Weak探针证明root/tree/syntax真正释放，及shared模式旧revision仍持有复用local nodes；先只丢弃外层
handle的反例不会被当作销毁。timed样本不保留Weak，避免allocation header延迟释放污染数据。
final约11.6–12.0 ms包含当前DocumentOutput拥有的最后syntax/local payload释放；shared约0.30–0.35 ms只释放新root及
独有部分。旧root的部分清理也可能发生在install阶段。完整pipeline在此包含当前revision teardown，不是每次按键或
普通LSP request latency；不能把final销毁数值当作每次增量编辑成本。

默认构建的semantics/workspace/LSP tests与warning gate通过；feature构建的workspace all-targets（含bench smoke）、
normal/profile结果一致性、short-circuit及生命周期测试与warning gate通过。此测量只补验收阶段边界，不引入新的性能优化。

`# Definition and metadata separation

2026-09-07/08在同一pba环境、Cargo release profile重新运行上述mixed-2000 workload，比较基线`"88cf7c0"与
`"16e014e"。fixture、SHA-256、5次warmup/30个samples、normal/profile交替采样和生命周期定义均不变；
两版依次运行，采样时没有并行构建。复跑命令仍为上述production stage profile，不用单独简化的metadata微基准代替。

Definition从`"MetadataOutput"拆出，局部relative facts按syntax shard共享，分组拓扑独立绑定current semantic tree；
文档存在definition不再禁止metadata复用。Metadata自身通过Arc共享，definition变化不参与Event上下文比较，
也不再触发metadata fold label失效。Definition局部分析计入local stage、分组计入document reduction，未从计时路径省略。

以下每格为p50/p95；metadata/context单位us、来自profile入口，其余单位ms、来自normal入口。

`table
 `- Case Stage Before After
  `+ header
 `- Event/shared Metadata/context 1030.522/1135.183 16.787/21.690
 `- Event/shared {Semantic total} 1.710/1.828 0.580/0.626
 `- Event/shared {Complete pipeline} 8.847/9.363 7.068/7.714
 `- Equal/shared Metadata/context 1025.202/1085.907 16.294/17.609
 `- Equal/shared {Semantic total} 1.403/1.479 0.360/0.402
 `- Equal/shared {Complete pipeline} 8.312/8.573 7.022/7.234
 `- Event/final Metadata/context 1024.654/1086.964 16.378/18.147
 `- Event/final {Semantic total} 1.661/1.756 0.580/0.614
 `- Event/final {Complete pipeline} 20.418/21.096 18.949/19.371
 `- Equal/final Metadata/context 1031.610/1096.120 16.336/17.624
 `- Equal/final {Semantic total} 1.402/1.462 0.356/0.382
 `- Equal/final {Complete pipeline} 19.929/20.318 18.308/18.971

Event/shared语义分析p50下降约66%，该完整消费路径下降约20%；Equal/shared分别下降约74%和16%。
这些是单机样本观察，不是保证；完整路径仍包含指定owned projection与释放新revision、保留旧revision的成本，
不是普通LSP request或按键延迟。未改变最后版本teardown策略，也未把projection提速作为本次实现的独立保证。

独立回归验证metadata Arc共享、definition局部facts和group topology共享、未变化Event node复用，及metadata日期改变
仍更新Event却保留definition局部结果。删除/分裂/合并、嵌套与跨metadata分组、Unicode/CRLF位移分别与standalone
definition analysis及fresh DocumentOutput比较；export和Folding的真实consumer对照通过。默认workspace all-targets
及bench smoke通过，另行验证新增export用例、profile parity/lifetime、默认与feature warning gates。
