`= title plumb architecture overview

`# 1. Data flow

官方工具链的数据流是：

`text

|"
  UTF-8 source
      -> plumb-syntax recovered lossless tree + syntax diagnostics
      -> typed recovered/valid views
      -> official document semantics
      -> workspace indexes
      -> queries / operations / export
      -> plumb-edit intents + canonical rendering
      -> revision-bound byte text edits
      -> LSP, CLI, Pandoc JSON export/import

一次 revision 只解析一次。semantic analyses 共享 source-backed views，不各自重新解析文本。

`# 2. Strict parser

`"plumb-syntax" 是 syntax 的唯一权威。它无文件 I/O，只使用 byte offsets，并对 valid 或 invalid
input 都产生 lossless tree 与 diagnostics。它不解释 heading、list、metadata、link、task 或 id。

parser 使用 reject-but-recover。当前 revision 有 syntax error 时，authoritative semantic analysis
和 export 必须停止；completion 等明确面向编辑中 source 的 query 可以消费 recovered view。

block 与 parsed-inline nesting 使用 explicit stacks。parser 对 source 生成完整、无空洞的 token
sequence，并从同一次 parse 暴露 typed recovered/valid views；arbitrary UTF-8 property tests 固定
reconstruction、range boundary 和 deterministic output invariants。

`"plumb-format" 消费 valid syntax tree 并提供 canonical rendering policy。formatting policy 不属于 core
syntax validity，也不依赖 official semantics；invalid recovered tree 不进入 formatter。官方 editing
consumer 不直接调用该 crate，而是把已有 `"ParsedDocument" 与 format scope 交给 `"plumb-edit"，由 edit
layer 产生 revision-bound byte edits。

`"plumb-edit" 在 valid revision 上执行 syntax-level declaration/block mutations、owned syntax insertion、
format intent、validated token rewrites 和 authoring finalization。详细 contract 见
`->[syntax editing|editing.plumb]。

`# 3. Semantics

`"plumb-semantics" 实现固定的 official semantic profile。它从 valid typed syntax views 产生
source-backed records 与 diagnostics；document analyses、workspace analyses、queries 和 operations
通过静态 Rust pipeline 显式组合，不提供动态 registration 或可替换 extension host。

官方 bundle 包括 outline、anchors/references、target resolution、lists、definitions、metadata、
citations、quotes、inline styles、tasks 和 events。semantic profile 将普通 direct declaration children 投影为
id、facets 与 named properties；core 不维护 semantic registry，也不校验 element 或 marker spelling。

`# 4. Workspace

`"plumb-workspace" 保存 document snapshots、revision、last-valid semantic outputs、dependency indexes
和 guarded edit preconditions。稳定 disk revision 的 semantic projection 使用可重建的 SQLite store
持久化；未打开 document 不要求在进程启动时恢复 source、syntax tree 或完整 Rust semantic object graph。
SQLite cache identity 至少包含 normalized path、source content hash、semantic schema version 与 producer
version；identity 不匹配的记录不能参与 query，并在 background indexing 后按 document transaction 替换。

persistent store 的 production implementation 使用 Diesel typed schema、models、transactions 与 query
composition；不得在 workspace、Web 或 protocol adapters 中散布 SQL strings、column ordinals 或 row decoding。
少数 Diesel query DSL 无法清楚表达且有独立 query-plan tests 的 SQLite operation 可以封装 raw SQL，但它仍
属于 store module，不形成第二套 repository API。Diesel schema、migration、generation transaction 与 typed
row decoding 已实现；task dependency relation、active/blocked lookup、standard task state filtering 与 Web task
query adapter 已下沉。event-task relation、通用 CEL filter/order/cursor pagination 仍未实现。
CEL reverse task dependency facts 使用 store-owned Diesel batch relation query 与 open-buffer overlay 合成，Web、
CLI 和其他 adapters 不逐 task 查询 dependents，也不接触 relation table schema。

open buffer 以 normalized document path 为单位完整遮蔽同 path 的 persisted disk projection。一个 query
中的 source document 只能来自 open-buffer snapshot 或 SQLite active generation 之一，不能逐 record
混合两个 revision。didOpen/didChange 只更新内存 snapshot；只有 host 已确认的稳定 disk revision 才能
提交 persistent store。关闭 buffer 后，workspace 必须读取与实际 disk bytes identity 一致的 persisted
generation，或先重新分析 disk source。create/change/remove 只使相关 documents 与 dependents invalidated。

Persistent query row 携带的 revision 只表示该 cached projection 的 provenance。Web 或其他 authoring adapter
向 guarded mutation 投影 revision token 时，必须使用当前 live document snapshot revision；source identity 命中而
复用旧 persistent generation 时，不得把旧 row revision 暴露为当前 mutation precondition。

open-buffer snapshot 的跨文档 records 保留在 Rust `"DocumentOutput" overlay 中；workspace query 将
persistent SQLite rows 排除 open paths 后与 overlay results 合成。不得把每次 didChange 的完整 generation
同步发布到 SQLite TEMP tables：该统一 SQL 模型没有增加第一阶段已经获得的 warm-start 与 closed-workspace
memory 收益，却会把大 open document 的 publication 和常用 query latency 带入逐键路径。query composition
必须集中在 protocol-neutral workspace layer，并以 parity tests 防止两侧 semantic rules 分歧。

syntax tree 不进入 persistent store。document analysis 仍先产生 protocol-neutral `"DocumentOutput"；
workspace 将其中跨文档 query 所需的 semantic records 作为一个 document generation 原子投影到 SQLite。
store 是 source 的 disposable derivative：transaction failure、schema migration 或 corruption 可以丢弃并
重建，不能改变 source validity 或 semantic meaning。

筛选、排序、cursor pagination、identity lookup、time overlap 与正反 relation lookup 属于 persistent
database query 职责。Task dependency 与 event-task association 必须投影为带 source/target identity 和
document generation 的 relation rows，并建立 target-side indexes；query 不得先 decode 无界 Bincode records
或 materialize 完整 workspace collection 后再执行这些集合运算。Rust semantic pipeline 继续决定 relation
事实，Diesel store 只原子保存和查询这些事实，不重新解释 task/event semantics。

workspace boundary 由显式 root 或 consumer 的 `".plumb/" discovery 决定。protocol-neutral scanner
使用 root-scoped `".ignore" 做 traversal pruning，不读取 parent 或 Git ignore sources，也不默认排除
dot paths。CLI、LSP 与 Web 共享同一 scanner；LSP root 仍由 client 的
`"workspaceFolders"/`"rootUri" 决定，server 不重新向上 discovery。ignored open document 可以作为
ephemeral buffer snapshot 参与 document features，关闭后不属于 disk index。

reference CodeLens 是 workspace reverse-reference query 的 LSP projection。`"references_to_document"
聚合 current-valid documents 中的 document/anchor links 与 task references，并保留真实 same-document
occurrences；Graph 等 consumer 可以按自己的 edge policy 隐藏 self-edge。`"references_to" 继续查询单个 explicit anchor。server 只为 current-valid metadata 与 anchors
生成 lenses，count 与 command locations 来自同一次 query，避免 UI title 与执行结果偏移。一个 document 的
CodeLens projection 必须通过一次 batched reverse-reference query 同时取得 document occurrence 与所有 direct
explicit-anchor occurrences，不得为每个 anchor 重新遍历 workspace records。
`"plumb.showReferences" 是 client-side command，不把 editor UI 行为放入 protocol-neutral workspace。

workspace identity 使用 normalized document path/URI boundary。跨文件 query 必须说明 index
completeness；不能把 partial result 表示成 complete negative result。

`# 5. Consumers

`- `"plumb" 是唯一 user-facing binary；`"lsp"、`"fmt"、`"export"、`"import"、`"note"、`"task"、`"event"、
 `"site serve"
 subcommands 分别适配 editor protocol、file/stdin、Pandoc JSON、CEL 和 task operations。
 Cargo package 与 binary 名保持为 `"plumb"；其 `"crates/plumb-app" 目录是 application composition root，
 只负责统一 command dispatch、process I/O、exit status 与 LSP protocol adapter，不以其中任一 adapter
 为整个 crate 命名。
`- `"tree-sitter-plumb" 是 lenient editor mirror，只负责 highlight、fold、indent、text object 和
 injection。

consumer 不复制 parser、semantic recognition 或 edit logic。

editor integration 可以把 core/formatter 的 structural indentation policy 映射为 filetype-local editing
options。官方 Neovim plugin 通过标准 `"ftplugin/plumb.lua" 设置 buffer-local `"expandtab"、
`"shiftwidth=1" 与跟随 `"shiftwidth" 的 `"softtabstop"，并提供 `"b:undo_ftplugin" 以在 filetype
变化时恢复先前设置。它不覆盖 `"tabstop"：该选项决定 source 中真实 tab bytes 的显示宽度，而 verbatim
payload 可以合法保留 tabs，其展示应继续服从用户配置。

LSP folding 从 recovered syntax range 投影。heading section、marked raw tail 与 anonymous verbatim raw
body 各产生 structural range。marked block 若有 children 或 raw tail，则产生延伸到最后一个 descendant
或 payload end 的 subtree range；raw tail 不增加同 start 的重复 owner range。对于允许吸收 trailing
layout 的 subtree fold，只有当前 block 与紧邻的同层 next
sibling 都是非 heading 的 parsed marked block 且 marker 完全相同时，才把两者之间一个直接相邻的空白分隔行包含在
subtree range 中；不同 marker、verbatim 或其他 block boundary 的空白分隔行保留在 fold range 外。该规则
使同 marker sibling 折叠后紧凑排列，同时保留不同 block 种类之间的可见分组边界。heading section 使用
独立的 section range，不适用该 sibling separator 规则。client
range limit 在 source order 内优先保留外层 range。

该设计区分空白分隔行的两种作用。不同 marker 之间的分隔行表达 block 种类变化形成的分组边界；即使前一
block 已折叠，这个边界仍携带结构信息，因此必须可见。相同 marker 的 block 在前一 block 有 children 时
也需要源码分隔行，但它只负责在展开状态下隔开 subtree 内容与 next sibling；subtree 折叠后这项作用随
children 一同消失，继续显示会形成没有对应内容的孤立空行，因此可以由 subtree fold 吸收。Folding
projection 由此保留折叠后仍有意义的分组信息，只移除因隐藏 subtree 而失去作用的布局间距。

支持 `"foldingRange.foldingRange.collapsedText" 时，task 与 event 的 custom fold text 保留 backtick 与完整
marker，以便与其他 block 的默认 opener 形式一致。Task 使用五种固定 ASCII workflow state slot（ready 为空白、
waiting 为波浪号、blocked 为等号、done 为 o、canceled 为 x），conflicted 使用 ox 组合；slot 预留五个 ASCII 字符，
使常见状态的 task title 对齐，但不为罕见 conflicted 状态改变其组合表达。Task text 依次包含 backtick、
"task" marker、state 和 title；event text 依次包含 backtick、"event" marker、schedule 和 title；event
保留已解析的 schedule 与 title。没有可靠 semantic facts 或 event 时间时不生成对应 custom text。

Neovim foldtext projection 对一条 task label 使用单一 highlight group，使不可行动或终态 task 的完整折叠
摘要得到一致的淡化或状态提示。Ready 不附加 highlight；waiting、blocked、done、canceled、conflicted
分别使用独立的 fold-only highlight groups。waiting、blocked、done、conflicted 保持链接到
`"DiagnosticInfo"、`"DiagnosticHint"、`"Comment"、`"DiagnosticWarn"。canceled 以 resolved `"Comment"
highlight 的 foreground 与基础样式取得弱化颜色，再叠加 `"DiagnosticDeprecated" 的 decoration attributes，
但不继承其 foreground/background；由此 canceled 会同时弱化并保留 strikethrough。fold-only groups
不复用 task semantic token groups，因此 fold text 配色
不得改变展开状态下 task body 的 semantic highlight。

Folding provider 以 document revision 为边界提供当前 snapshot 的 ranges；provider 不要求保存上一次
response，也不要求提供 old-to-new mapping。客户端 fold engine 负责保存上一个 accepted snapshot 与用户
对 provider folds 的 collapsed 状态，并在新 snapshot 到达后执行 best-effort reconciliation。匹配优先使用
provider 可选的 stable identity，其次使用编辑位置映射、父子结构、kind 与内容锚点，最后才使用起始行和
重叠关系等启发式；无法唯一匹配时必须丢弃旧状态，不得把用户状态迁移到无关 fold。用户创建的 manual
ranges 不属于 provider snapshot，另由客户端 fold engine 管理。异步 response 必须经过 revision gate，
stale snapshot 不得覆盖较新的 accepted state。

补全或 snippet 展开期间的 folding 有两个独立问题。第一个是 snapshot 应用时机：Insert、
Replace 或 Select 等编辑类模式内到达的 folding refresh 应合并，并延迟到 mode transition 后确认
稳定退出编辑态时再执行一次 `"evaluate" 与 fold recomputation。退出后才到达的 response 应立即应用，
不得再等待下一次 InsertLeave。该问题只要求不在 snippet 中间态替换当前 fold levels，不要求
provider 提供 identity，也不需要 collapse-state reconciliation。

第二个问题是新 snapshot 稳定应用后的光标可见性。新建或扩展的 fold 仍可能按 window fold settings
关闭并遮住编辑位置；延迟更新本身不保证光标可见。客户端若采用光标可见性策略，应只在实际应用
一次被延迟的 snapshot，且光标行由更新前可见变为更新后被关闭 fold 遮住时，打开包含光标的 fold
链。不得在所有 InsertLeave 时无条件执行 `"zv"，否则会打开用户原本手动关闭的 fold。该策略属于
window-local editor policy，不是 provider contract，也不代替稳定更新后对其他 open/closed state 的
best-effort reconciliation。

该光标可见性后置条件与 Neovim 核心已有的 InsertLeave folding policy 一致：核心为自动更新的
foldmethod 执行 `"foldUpdateAfterInsert()" 时，先更新 folds，再调用 `"foldOpenCursor()" 打开足以显示
光标行的 fold 链。`"expr" 与 `"syntax" 因重算成本被核心排除在该自动更新路径之外；LSP adapter
稳定安装新的 `"expr" fold levels 并主动触发 fold recomputation 时，应补齐等价的
`"foldOpenCursor()" 行为。这个补充只恢复核心原有的光标可见性 invariant，不表示所有 expr-fold
更新都应打开光标处 fold，也不扩大为 fold-state reconciliation。

`# 6. Crate boundaries

`- `"plumb-syntax"：strict syntax tree、diagnostics、typed views。
`- `"plumb-format"：syntax-valid document 的 protocol-neutral canonical formatter library。
`- `"plumb-edit"：owned syntax、valid-tree mutations、format-aware finalization 与 validated token edits。
`- `"plumb-semantics"：protocol-neutral semantic records、analyses、queries、operations、export model。
`- `"plumb-workspace"：snapshots、indexes、cross-file invalidation 和 guarded edits。
`- `"plumb"（`"crates/plumb-app"）：application composition root、统一 CLI、LSP adapter 以及各 library
 entry point 的 subcommand dispatch；crate 目录名表达整体 application 职责，而不是单个 `"lsp"
 subcommand。
`- `"plumb-export"：Pandoc JSON lowering library。
`- `"plumb-import"：受支持 Pandoc AST 到 canonical plumb 的 fallible conversion library。
`- `"plumb-notes"：notes/task query 与 operation library。
`- `"plumb-web"：workspace graph/Web document model、Pandoc Web target adaptation、本地 Web server
 与 browser assets。

crate 内部 source module 按稳定职责分界。crate root 只保留 shared data model、composition 与 public
re-export，不作为所有 domain operation 的默认落点：`"plumb-workspace" 的 task query、workflow 与
authoring mutation 由 task domain modules 持有，persistent query/schema 只由 store modules 持有；
`"plumb-web" 的 immutable snapshot/materialization、query projection、graph projection 与 task/event
mutation adapter 分属独立 modules；`"plumb" LSP server 的 workspace/index lifecycle 与各 protocol feature
adapter 分离，feature module 只投影 shared workspace/edit 结果，不取得另一份 semantic authority。
拆分不得改变 public API、revision guard、lazy materialization 或 adapter validation boundary。

`"plumb-web" 的 note renderer 使用 Pandoc citeproc 处理 official citation records。Bibliography
declaration、workspace-contained path resolution 和 CSL JSON records 由 shared semantic/workspace layers 提供；
Web adapter 只投影 document-relative writer paths。Workspace watcher 必须将被声明的 bibliography
files 视为 render dependencies，变化时清除 HTML cache 并发布新 revision。

Web server 将每个已发布 `"WebWorkspace" 作为 immutable `"Arc" snapshot。需要跨 `"await" 或进入
blocking renderer 的 request 先 clone 该 `"Arc"，随后释放 shared-state lock；不得 clone 整个 workspace、
parsed documents 或 indexes。mutation 与 watcher 在独占边界建立或 copy-on-write 更新下一个 snapshot，完成
校验后一次发布新的 `"Arc"；进行中的 request 可以继续读取旧 revision，但不能观察半更新状态。

每个 `"WebWorkspace" snapshot 只拥有一个 `"Workspace" query authority，不得为 live document access 与
persistent queries 维护两份 workspace。加载 snapshot 时保存该 revision 的 immutable source bytes，并将 disk
generations 更新到 persistent store；warm generation 不重复 parse 或 document analysis。只有 note render、
graph projection、mutation 或其它需要 lossless/typed tree 的 operation 才从保存的 source snapshot 按需
materialize document entry，并在该 Web revision 内复用。旧 snapshot 不得为了 lazy materialization 重读 disk，
因此后续 filesystem change 不能改变进行中 request 观察到的 source 或 semantic output。

统一 binary 可以依赖共享 layers，但 shared libraries 不能依赖 CLI protocol types。

Web presentation 不得取得或持有无界 event collection。Agenda 使用 server-side 有界 cursor page：没有显式
selection 时初始 page 锚定当前时间并分别取得相邻的 earlier/later records；显式 selection 位于当前 page
之外时以该 event identity 重新锚定。用户可以向前或向后替换固定大小的 page，page navigation 定位到新 page
boundary；`"Now" command 重置到当前时间 page。selection/detail mutation 只重新取得包含目标 event 的有界 page，
不能触发完整 event collection 的 transfer 或 DOM 重建。

Web task mutation 已实现下述 changed-generation replacement 与 watcher 去重；task result 与 authoring
candidate collection 已拆分，candidate 只在打开 authoring UI 时加载。event mutation 和通用 cursor query
接入仍是尚未实现的目标行为。Web task/event mutation 写入一个已索引 document 后，
server 必须以写入后的 disk bytes 原子替换该 document
snapshot，并只 invalidates 依赖该 document output 的 workspace computation；不得为了确认一次 guarded edit
而重新 scan、读取和分析未变化的 workspace documents。mutation request 可以携带当前 view query，成功响应
可以直接返回同一新 revision 上的 query result；也可以返回空 success，让 browser 发起一次有界 query。
两种协议都不得先生成一个未使用的无界 snapshot 或再重复同一 query。workspace
change notification 继续服务其他 clients；发起 mutation 的 client 必须按 revision 去重，不能因自己的
notification 再触发 graph/task/event 全量刷新。
Mutation response 必须暴露写入后的 workspace revision。browser 在 mutation 未结束时合并收到的 watcher
revision，安装 mutation 后有界 query 的同一 revision 后丢弃自己的 notification；更新的外部 revision 只重新
查询当前可见 view，连续 notifications 合并到最新 revision，其他 view 在切换到可见状态时按需查询。

Web mutation 的 CSRF boundary 使用 browser `"Origin" header。未配置 public origin 时，缺少 `"Origin" 的
non-browser request 保持允许；存在 `"Origin" 时只接受 `"http" scheme 且 authority 与直接 request `"Host"
一致。反向代理部署可以显式配置一个 public origin；它必须是只含 `"http" 或 `"https" scheme 与 authority
的 absolute origin，不得包含 userinfo、非 root path、query 或 fragment。配置后 mutation request 必须携带
可解析且规范化后与该 public origin 完全一致的 `"Origin"；不得回退到直接 `"Host"，也不得信任
`"Forwarded" 或 `"X-Forwarded-*" headers。origin comparison 规范化 scheme/host case 与默认 port，但保留
非默认显式 port 的区别。

Task authoring 不得为 parent、previous 或 dependency controls 一次创建完整 workspace option DOM。candidate
endpoint 按 document scope 或 fuzzy query 返回有界 compact identities；browser 保留已选 locator，并在输入变化
时替换候选 page。Parent/position 只查询目标 document，cross-document previous/dependency 使用显式搜索。

`# 7. Tree-sitter boundary

tree-sitter grammar 与 strict core 覆盖相同的纯 syntax surface，但容错目标不同。grammar 不能定义
validity，也不能迫使语言设计适应 CFG。

lenient mirror 的 external scanner 与 strict core 共享同一词法局部性预算（core syntax reference 第
2 节）：scanner 状态只保存 indentation stack 与 raw margin/phase。inline dispatch 不跨 physical line，
不得跟踪 inline nesting depth 或跨行匹配 delimiter；raw payload 的 blank-run classification 可以预看至
下一条 nonblank line 或 EOF，但不能引入其他 nesting context。

lenient mirror 必须保持 direct ownership 与 terminal raw-tail boundary。quote run 只能绑定 scanner
indentation stack 中同 column、仍处于 structural phase 的 marked owner，不能附着到最后一个 declaration
child，也不能在 owner 已被同列 sibling 关闭后重新接管它。raw payload 中的 blank run 必须延迟分类：
下一条 nonblank line 满足 margin 时属于 payload，缺少 margin 的 trailing run 作为 block layout 保留。

CST 使用 `"marked_block" 保存 opaque `"marker"、head、source-ordered children 与 optional `"raw_tail"；
anonymous `"verbatim_block" 只保存 quote opener 与 payload。highlight query 给 marker、parsed inline kind
与 inline verbatim kind 使用通用 `"@keyword" capture，不解释 semantic profile meaning。injection query
只对具有 raw tail 的 marked owner读取 marker 作为 `"@injection.language"；anonymous verbatim 没有 kind，
不得猜测 injection language。每条 raw payload node range 必须排除 parent column 与 structural margin，只
覆盖移除 margin 后的 payload bytes 与 line ending。injection 必须组合这些 ranges，使 injected parser
从 payload column zero 读取首层内容，同时逐 byte 保留 Python 等 indentation-sensitive language 的
payload indentation。

external scanner 中所有需要预判 backtick spelling 的入口必须复用同一个无 token-range 副作用的
dispatch classifier；classifier 只区分 escape、parsed inline、inline verbatim、marked block 与 anonymous
verbatim block，不能在 lookahead 时调用 `"mark_end" 或发射 token。strict parser normative corpus 中
所有 valid case 经 tree-sitter parser 解析后都不得包含 `"ERROR" 或 missing node；该差分检查补充而
不替代按 dispatch position 与 spelling kind 展开的 tree-sitter corpus matrix。
更强的 strict-alignment test 将 strict valid syntax 投影为 normalized tree-sitter structural CST，并
与真实 tree-sitter parse tree 比较 node kind、nesting 和稳定 byte range。投影保留 `"document"、block、
raw-tail、inline envelope、`"introducer"、`"marker"、`"inline_kind" 与 anonymous verbatim opener；忽略
layout-only wrapper、ordinary text leaf 与 anonymous delimiter。document/block container range 会因 blank
layout ownership 不同而忽略；token 与 delimiter-owned inline/raw-tail range 必须一致。recovered
invalid tree 不做同构比较，因为 strict diagnostics recovery 与 editor-oriented lenient recovery 的边界不同。

`"grammar.js" 和 `"tree-sitter.json" 是 source of truth。generated parser files 是 build/release
artifacts；修改 grammar 后必须在 locked Nix environment 中 regenerate 并测试。

`# 8. Export boundary

portability 由 export 保证。plumb 自己充当 Pandoc reader：标准 semantic structures lower 为原生
Pandoc nodes，generic structures lower 为 Div/Span/CodeBlock/Code 并保留 opaque data。Pandoc 只
负责目标 writer。

import 是 portability boundary 的反向 consumer，而不是 syntax authority。`"plumb-import" 使用 typed
Pandoc AST，拒绝没有标准 plumb 表示的 nodes 与冲突 attributes；生成 source 必须经过 strict parser
与 protocol-neutral formatter。它保证官方 exporter profile 内的 semantic round-trip，不把任意 Pandoc
presentation surface 变成 core syntax 或隐式 semantics。
