`= title plumb lossless syntax tree

`# 1. Purpose

strict parser 对任意 UTF-8 source 产生 recovered lossless tree。tree 服务 diagnostics、editor queries、
source-preserving edits 与 semantic analyses；它不是 interpreted semantic AST。

`# 2. Fidelity

所有 source-backed leaf 使用 UTF-8 half-open byte ranges，按 source order 无重叠、无空洞覆盖 input。拼接
leaf bytes 必须逐 byte 等于 source。tree 保存 indentation、spaces、line endings、introducers、escapes、
markers、group/verbatim delimiters、raw payload、layout 与 malformed bytes。

nonempty source leaf 非空；empty source 产生 range `"[0, 0)" root。missing token 不插 synthetic leaf，由
diagnostic/fix 表达。node range 从 source-backed descendants 推导。

`# 3. Public representation

ParsedDocument 同时持有 original source、LosslessTree、typed Document 与 diagnostics。LosslessTree 是 root
range 加 source-ordered SyntaxToken sequence；kind 包含 text、whitespace、indentation、line ending、
introducer、escape、marker、inline kind、delimiter、raw payload 与 error。

typed structure 统一为 optional-marker recursive owners：

`rust"
 struct Block {
     marker: Option<Marker>,
     content: BlockContent,
     children: Vec<Block>,
 }
 enum BlockContent {
     Parsed(InlineContents),
     Verbatim(RawText),
 }
 enum InlineElement {
     Text(TextRun),
     Space(SpaceRun),
     Group {
         marker: Option<Marker>,
         content: InlineContents,
     },
     Verbatim {
         marker: Option<Marker>,
         payload: RawText,
     },
 }

current Rust API 用 ParsedBlock/VerbatimBlock enum variants 表达 BlockContent，但 observable ownership 与上面
model 等价。anonymous/marked parsed blocks 共享 children；VerbatimBlock children 固定为空。

`# 4. Inline element contract

InlineContents 只保存唯一一份 source-ordered elements，包括 direct SpaceRun 与 SoftBreak；没有 InlineDatum、
segment、argument descriptor或对应的 stored range/index projection。Text 在 direct whitespace、special entry或
boundary处结束，Group/Verbatim各自是 parent depth的一个 element。SoftBreak range覆盖 physical line ending与
下一行 structural continuation indentation，typed plain-text projection视为一个 semantic space。
SpaceRun/SoftBreak不占 semantic positional index；repeated whitespace不产生 empty value，empty Group 是 explicit
empty positional element。

Group content 递归使用同一 structure。没有 InlineMember、parsed/verbatim argument variants 或 bracket/pipe
separator ranges。generic direct declaration view 可以从 direct marked owners 投影 id/facet/pair
ranges，但 underlying owner sequence 保持不变。

plain-text traversal 展开 anonymous/generic group、保留 SpaceRun，并让 semantic profile 对 Link 等 known
owners选择 visible elements。kind-specific consumer直接在 element sequence上过滤 declarations与 whitespace并
声明 first/rest、each或 exactly-one binding，不能自行按 source string split。

`# 5. Source ranges

marker 保存 introducer-inclusive owner range 与 exact marker_range。Group/Verbatim 保存完整 envelope、content/
text range；VerbatimBlock 保存 opener、quote 与 raw source span。raw typed text 已 strip exactly one structural
margin，但 source token ranges 继续覆盖 margins 与 original line endings。

projected declaration item 同时保存 full direct-owner range 与 precise key/value range。remove/move 使用 full
range；rename/selection/reference 使用 value range。consumer 不得用 `"full_start + constant" 猜测 token
boundary。

`# 6. Recovered and valid views

lossless tokens 与 typed recovered Document 始终存在。recovered_syntax 返回同一次 parse 的 recovered tree；
valid_syntax 只在没有 error diagnostic 时返回不可由 consumer 构造的 ValidDocument，同时借用 source/tree。

official semantic analysis、export 与 valid-tree authoring 只接受 ValidDocument。面向 invalid editing state 的
completion、folding 等显式接受 recovered Document。访问任一 view 不重新 parse。

`# 7. Recovery representation

malformed bytes 标为 error tokens 并继续参与 complete coverage；existing token boundaries 保留。virtual
indentation 或 missing closer 只存在于 diagnostics/fixes，不进入 source-backed leaves。recovery boundary 必须
deterministic，使 same source 的 tree、diagnostic order 与 edit ranges 稳定。

block nesting 使用 explicit work stack；typed block/inline destruction 与 traversal 也不能从 Rust call stack
获得 language depth limit。inline parser 的 arbitrary nesting implementation 必须由 deep-corpus regression
证明不会 stack overflow。

`# 8. Owned syntax

plumb-edit OwnedBlock/OwnedInline 是 syntax view 的 source-independent constructible counterpart，不是 normalized
semantic AST。它负责 insertion/replacement/move 与 canonical rendering；adapter 不手写 subtree syntax。

`# 9. Incremental strict parsing

`"plumb-syntax" 提供 fresh parse 与 previous-revision incremental parse。incremental entry 接受完整 previous
ParsedDocument 与 next source bytes，自行计算共同 prefix/suffix；caller 不传不可信的 parser boundary。实现只在
两个 revision 中都能证明 top-level owner start 是独立 line boundary 时复用其外侧结果。包含 change 的最小
top-level owner run 重新经过同一 strict block/inline parser；边界无法证明、reused token 跨越 splice，或 range
rebase 失败时必须退回 fresh parse。

复用覆盖 recovered typed blocks、syntax diagnostics 与 lossless tokens。前缀 range 不变，后缀所有 source-backed
range 以 byte delta 统一 rebase；document-level projected declarations 从合并后的 direct blocks 重新建立。最终
ParsedDocument 的 source、tree、diagnostic order、validity、token kinds/ranges 与对 next bytes 单独 fresh parse 的
结果必须相等。invalid-to-invalid、valid-to-invalid、CRLF、UTF-8、indentation、continuation、verbatim boundary 与
文件首尾 edit 都属于 differential tests 的输入范围。

该阶段允许复制 existing owned nodes，不承诺 allocation identity 或 asymptotically constant splice。benchmark
必须分别报告 reparsed byte range、incremental parse 和 fresh parse；若 edit 使整个 document 落入 reparsed range，
实现直接使用 fresh path，不把它报告为 reuse。

`## 9.1 Production green/red target

Status：尚未实现。当前 released implementation 仍使用上一段的 owned-node copy/rebase；以下 contract 是正在实现
的 replacement boundary。

每个 syntax revision 以 immutable green root 为 authoritative recovered representation。green node/token 保存
kind、children/source text 与 subtree byte length，range 相对该 subtree；不保存 parent、revision absolute offset 或
可变 identity。未变化 subtree 跨 revision 复用同一 allocation。red view 将 green node 与 revision root/path 组合，
按 preceding sibling lengths 计算 parent、siblings 与 absolute UTF-8 range；typed Block/Inline/Mark/Attributes view
不得为整个 document eager materialize 第二棵 absolute-range owned tree。

incremental entry 接受 ordered byte edits，而不只接受 final source。它验证 edits 属于 previous revision、按顺序
应用并产生 old/new affected ranges、reparsed green roots 与 reused identities。LSP UTF-16 range 只在 adapter/line
index boundary 转成 byte edits；workspace 与 syntax 不重新比较完整 old/new source 来恢复已经存在的 provenance。
无法证明 stable owner boundary 时允许扩大 reparse 或 fresh fallback，但 observable tree/diagnostics 必须继续与 fresh
parse 相等。

lossless tokens、syntax diagnostics 与 generic declaration projection 是 green tree 的 typed views，不另存一份
full-document flat mutable range graph。需要 source-ordered flat token stream 的 consumer 可以遍历 red view；该投影
可以缓存于 revision，但不得在局部 edit 后逐 token rebase unchanged suffix。

`# 10. Lenient tree and identity

tree-sitter mirror 只服务 editor ergonomics。fresh/full/incremental parses 必须在同 source 上产生等价 current
node kinds/ranges；scanner state round-trip 不引入 ERROR。它不改变 strict parser authority。

当前 contract 不承诺 cross-revision NodeId reuse。incremental identity、source-map 与 canonicalization 必须作为
同一设计问题解决；优化不能泄漏不稳定 public identity。
