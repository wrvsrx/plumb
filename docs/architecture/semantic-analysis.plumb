{
 `: title plumb semantic analysis
}

`# 1. Scope

`"plumb-semantics" 实现官方 semantic profile。它从 `"plumb-syntax" 的 immutable typed views
产生 source-backed records 与 diagnostics，并为 workspace query、operation 和 export 提供
protocol-neutral inputs。它是固定的 Rust analysis pipeline，不是动态 extension host，也不承诺
第三方 registration、可替换 semantic profiles、ABI、WASM 或 external-process plugins。

syntax/semantics 分层仍是强制边界：syntax 决定 source 是否构成合法结构；semantics 只解释
syntax-valid marker、inline kind 与 attached content。未知但词法良构的 spelling 保持 generic，
不因官方 pipeline 未识别而成为 syntax error。

`# 2. Typed views

`"plumb-syntax" 为同一 recovered tree 提供 typed recovered/valid views。view 负责安全遍历、source ranges、
marker/head/children/attached group 访问，不增加 semantic meaning。冻结的 0.17 attributes 只由显式
migration reader 暴露，不进入 current recovered/valid view。

`"Recovered" view 可用于 completion、selection 和 syntax-aware fixes。`"Valid" view 只有 syntax
diagnostics 为空时存在，用于 authoritative semantic analysis、indexes、rename 和 export。
normalized AST 只在具体 consumer 能证明 typed views 不足时 materialize。

`# 3. Static analysis pipeline

官方 pipeline 包含四类 protocol-neutral computation：

`- document analysis：从一个 revision 的 valid view 产生 semantic records 与 diagnostics。
`- workspace analysis：组合 document outputs，产生 resolution、reverse edges、cycles 等 indexes。
`- query：读取 immutable context，返回当前请求的结果，不改变 state。
`- operation：验证 preconditions，提出 guarded text/resource effects，不直接应用。

document analyses 由代码中的显式 typed inputs 与固定调用结构组合。新增或调整 analysis 必须同时更新
本页、standard semantic profile、dependency/invalidation behavior 和 tests；不得通过 registration order
形成隐式依赖。Pandoc export 是官方 semantics 的 consumer，不是 syntax 特例，也不被描述为可插拔 extension。

`# 4. Source-backed outputs

semantic record 必须携带足够 provenance：document identity、revision、owner node/range、selection
range 和从 source 提取的值。derived field 应可追溯到一个或多个 source ranges。

record identity 不能依赖 unstable allocation address。跨 revision reuse 只有在 workspace 能证明 source
identity 时才允许。Analysis output 不修改 typed syntax，也不取得 syntax node 的唯一 ownership。

同类 source-backed records 按 source range 起点保持稳定顺序。基于 owner range 查询其包含 records 时，
consumer 应利用该顺序限定候选区间，不得为每个 owner 从头扫描同一 document 的完整 record collection；
重叠或嵌套 owners 仍可引用同一 record，不引入唯一 ownership。

`# 5. Semantic composition

同一个 syntax node 可以同时产生多个正交 semantic facts。例如一个 task owner 也可以拥有 explicit
anchor 和普通 attached data。standard projection 将 direct `"-" member 投影为 facet、direct `"@"
declaration 投影为 id、direct `":" association 投影为 property；该 projection 属于 official semantic
profile，不属于 syntax grammar。

analyses 只增加自己拥有的 records/diagnostics，不能删除其他 analysis 所需的 source data。组合冲突必须
产生显式 diagnostic 或使用在 standard profile 中冻结的 rule，不能依赖 Rust module 调用顺序。generic
export 在标准 semantic lowering 后仍保留未消费的 attached elements。

`# 6. Dependencies and invalidation

analysis dependencies 通过 Rust types、函数 inputs 和固定 pipeline 明确表达。workspace 以 document
revision、configuration 和直接 dependency output identity 形成 cache/invalidation boundary。document
change 只 invalidates 直接或传递依赖其 outputs 的 computations；实现可以先采用整体 document output，
但不得声称不存在的动态 descriptor 或任意 output registry。

workspace analysis 必须声明需要 complete index，还是可以返回标明 provenance/completeness 的 partial
result。依赖 output 缺失时，应返回 dependency/completeness failure，不能猜测 negative semantic result。

`# 7. Snapshots and last-valid data

snapshot 包含 document identity、revision、source、recovered tree、syntax diagnostics 和可选 current-valid
semantic output。新 revision invalid 时，workspace 可以保留前一 revision 的 last-valid output 供其他
documents 稳定解析，但必须携带原 revision provenance。

host 在 open/disk ownership 或 protocol revision 间转换 snapshot 时，若规范化 path 与 source bytes 均未
改变，可以只把现有 ParsedDocument/current-valid output 重绑定到目标 revision，不重复 parse 或 document
analysis；invalid snapshot 继续保留原 last-valid provenance。source bytes 不同则必须建立新 revision
snapshot。该复用只省略由相同 bytes 决定的 document-local computation，不缓存 workspace diagnostics 或
resolution result。

当前 document 的 semantic diagnostics 和 authoritative query 不能使用自己的 last-valid output 冒充
current result。consumer 必须能区分 current、last-valid 与 partial workspace data。

稳定 disk snapshot 的 semantic output 可以投影到 persistent workspace store，但 persistent projection
不是 syntax cache，也不是新的 semantic authority。projection 必须保留 query 所需 provenance，并以
document generation 原子发布；query 不得观察一半属于旧 generation、一半属于新 generation 的 records。
open document 的 current-valid 或 last-valid state 保留在内存中，并以整个 document 为单位遮蔽同 path
的 disk generation。didChange 不触发 persistent write；didSave、external disk change 或 initial indexing
只有在确认 disk bytes identity 后才能发布新 generation。

`# 8. Queries

query context 至少包含 request document/revision、cursor 或 target、workspace completeness、cancellation
和 configuration。结果必须说明 provenance；candidate ordering 应 deterministic。

completion 可以读取 current recovered view 与其他 documents 的 last-valid indexes。definition、references、
hover 和 rename 默认要求当前 target 具有 valid semantic record。

LSP initial workspace indexing 不得阻塞 current open-document lifecycle 或只依赖该 document snapshot 的
formatting、range formatting、symbols、folding 与 semantic tokens requests。index 在 background 构造完整
workspace 后回到 server event loop 原子安装；安装时必须保留 indexing 期间已经 didOpen/didChange 的 current
revisions。完成前 cross-document query 明确报告 incomplete，旧 generation 的 indexing result 不得覆盖较新的
rescan 或 open-document state。

persistent workspace query 必须按需读取命中的 semantic rows，不得为了重建 process-local workspace model
而在启动时反序列化全部 document outputs。open-buffer precedence 的合并边界是 source document：persistent
query 排除所有 open paths，再与这些 paths 的内存 query result 组合并作 deterministic ordering。该规则由
protocol-neutral workspace query layer 统一实现，adapter 不自行 merge 或 deduplicate records。

task dependency relation、active/blocked lookup 与 standard task state filtering 已由 Diesel 执行；其余
尚未实现的 relation/query 下沉
完成后，persistent task/event query 必须由 Diesel 在 database 中执行 typed
filter、deterministic order、cursor pagination 与 indexed relation lookup，只 decode 命中 page 和显式 detail
targets 的完整 records。Standard task state、resolved datetime、priority、depth、source identity 与 relation
endpoints 作为 typed query columns 投影；这些 columns 是 Rust semantic output 的派生缓存，不成为新的语义
权威。Open-document overlay 可以在 Rust 中对 database page 作 document-granularity replacement，但不得改变
filter/order/limit 的 observable parity 或把完整 closed generation materialize 回内存。

CEL filter compilation 必须记录 expression 实际引用的 variables。构造 task/event context 时，普通 typed
columns 可以直接绑定；dependency、reverse relation 或其他需要 workspace lookup 的 facts 只在 expression 引用
对应 variable 时物化。该 demand-driven context 不能改变缺失 variable、类型错误或 filter result semantics。
当 task filter 引用 reverse dependency variable 时，query execution 必须在遍历 records 前一次批量取得所有 direct
dependency endpoints，并按 target identity 建立 lookup；不得为每个 candidate task 重扫 workspace 或分别发起
database query。Persistent rows 排除 open source documents 后与 open-buffer relations 合并，继续保持 document-
granularity overlay precedence 和 deterministic source ordering。

Task cursor page 绑定 normalized filter、sort keys 与 workspace revision。cursor 是 opaque continuation identity，
不得解释为 row offset；失配或 stale cursor 必须拒绝。分页先在 lightweight typed facts 上完成 filter、tree
aggregation 与 deterministic ordering，再只 decode 当前 page 的完整 records。一个 page 可以越过 requested
task count 以保留当前 document 的完整 retained tree，但不能重复或跳过相邻 pages 的 document group。

LSP adapter 将一个 revision 的 folding byte ranges 批量投影为 UTF-16 positions 时，必须复用由 source
一次线性扫描建立的 line-start index；单个 offset 只扫描它所在的 physical line。folding response 不得为
每个 range 从 document start 重复扫描 source。index 只属于 protocol projection，不进入 syntax/semantics
output，也不得跨 source revision 复用。

官方 workspace search query 的 LSP projection 使用 `"plumb/search" schema version 3。request 的可选
`"kind" 是 `"note"、`"task" 或 `"event"；result item 共享 `"kind"、`"title"、workspace-relative
`"path"、LSP `"location" 与 revision `"provenance"。Task item 还可包含 workflow fields；Event item
还可包含 `"id"、resolved `"start"/`"end" 与 associated `"tasks"。增加 record kind 或改变 typed fields
必须提升 schema version；client 必须按 initialize capability 中声明的 version 接受或明确拒绝。

`# 9. Operations and editing

operation 返回 proposal：ordered non-overlapping text edits、resource edits、expected revisions 和 required
application capabilities。proposal 本身不改变 workspace。application adapter 在应用前重新验证 revisions、
document identity、target existence 和 capability；multi-document operation 必须 atomic，或明确失败。

official semantics/workspace 将 valid-tree authoring intent 交给 `"plumb-edit"。property insert/replace/remove
必须指定 target 与 insertion position；新增 block 使用 semantics-neutral owned syntax node。edit session
绑定 revision snapshot，在内存副本应用 mutations，canonicalize 完整 owner subtree，再映射为针对原
revision 的 byte edits。

consumer 不渲染 owned nodes、不计算 subtree indentation/line-ending replacement，也不把结构删除或移动
降级为 raw token replacement。anchor/document rename 通过 `"plumb-edit" 验证 token ranges，并保持
ordered minimal edits。incomplete-source completion 属于 recovery assist，不伪装成 authoritative mutation。

Formatting 使用同一 syntax-only edit contract：consumer 传入当前 `"ParsedDocument" 和 scope，
`"plumb-edit" 返回 byte edits；LSP/CLI/Web 不直接依赖 `"plumb-format" 或重新 parse revision source。

`# 10. Diagnostics and failure

diagnostic 是可定位的 source problem；query failure 是请求无法完成；cancellation 不是 failure；
runtime failure 不应伪装成 source diagnostic。

syntax diagnostics 由 `"plumb-syntax" 拥有；semantic profile diagnostics 由 `"plumb-semantics" 拥有；
跨文档 resolution、identity 与 dependency diagnostics 由 `"plumb-workspace" 拥有。adapter 不重新解释
或复制这些 rules。

`# 11. Implementation policy

当前 release 只有一套 official semantic profile 和一个静态 Rust pipeline。若未来出现第二个 concrete
profile、第三方 integration 或不同 host，必须以独立 proposal 证明需求并重新冻结 compatibility contract；
当前 crate names、records 与 pipeline 不构成 plugin API。
