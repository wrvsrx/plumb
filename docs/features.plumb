`# plumb 工具需求与路线图

本文从 `"[docs/requirements.plumb]" 的目标推导工具需求。block structure 已在
`"[docs/spec.plumb]" 冻结，inline envelope 记录在 `"[docs/inline.plumb]"；两者仍有实现前
细节需要拍板。

阶段：`"[MVP]" = 首个可用版本；`"[v1]" = 稳定版；`"[later]" = 之后。

`## A. 解析内核（`"[plumb-core]"）

`- [plumb] MVP 手写严格解析器：输入 UTF-8 字符串，输出
  `"[(Document, Vec<Diagnostic>)]"。
`- [plumb] MVP reject-but-recover：遇到语法错误后恢复到安全边界，尽量继续报告后续
  错误。
`- [plumb] MVP 精确语法诊断：每条诊断包含字节范围、错误码和简短说明。
`- [plumb] MVP 无文件 I/O，只使用 byte offsets；位置换算交给上层。
`- [plumb] MVP 语义中立 AST：core 不解释属性、链接目标、任务、元信息、id 唯一性或
  HTML 语义。
`- [plumb] MVP 通用属性解析：属性形状良构即可，内部 token opaque。
`- [plumb] MVP 最小高频语法：paragraph、generic marked block、代码块、inline envelope、
  属性和 introducer escape；标题、列表、引用等 marker 语义由 extension 提供。
`- [plumb] v1 raw text、字段列表、通用容器等根据语法拍板结果纳入。

`## B. 语义扩展（extensions）

extension 是语言无关的语义 analysis / transform contract，消费 core AST，产出结构化结果、
语义 diagnostics 或 text edits。导出器也是 extension。官方工具链可以使用 Rust module
实现并静态组合它们，但 Rust trait、WASM 或外部进程协议都属于 host implementation，
不属于 extension 的语义定义。完整契约见 `"[docs/extensions.plumb]"。

`- [plumb] MVP 语言无关的 extension contract、诊断合并模型和官方 host API。
`- [djot-tools] MVP outline extension：解释约定的 marker 并构建 document symbol 所需的大纲。
`- [djot-tools] MVP anchors / links extension：收集显式锚点、自动 heading id、链接
  引用和目标分类。
`- [djot-tools] MVP target resolution extension：解析内部和相对跨文件目标；URL 不解析。
`- [djot-tools] MVP workspace extension：按规范化路径存 AST / analysis 结果，支持打开
  buffer、磁盘文档和跨文件引用查询。
`- [plumb] MVP html / pandoc export extension：core AST 到 Pandoc JSON AST，再交给
  pandoc writer。
`- [plumb] v1 meta extension：识别约定的元信息结构并折叠到导出 metadata。
`- [plumb] v1 tasks extension：解释 marked block attrs 中的任务状态、日期和查询字段，
  并提供状态变更所需的文本编辑。

`## C. 语言服务器（`"[plumb-ls]"）

`"[plumb-ls]" 装配 core + extensions，把结果映射到 LSP。

`- [plumb] MVP `"[publishDiagnostics]"：core 语法诊断 + extension 语义诊断。
`- [djot-tools] MVP `"[textDocument/documentSymbol]"：经 outline extension。
`- [djot-tools] MVP `"[textDocument/definition]"：命中链接后解析目标并返回位置；URL 返回空。
`- [djot-tools] MVP `"[textDocument/references]"：经 workspace / links extension。
`- [djot-tools] MVP `"[textDocument/hover]"：目标类型、id、路径和源码预览。
`- [djot-tools] MVP FULL 文本同步；打开 buffer 优先于磁盘版本。
`- [djot-tools] MVP 初始化时索引根下文档；无根时仅索引已打开 buffer 和惰性目标。
`- [djot-tools] MVP byte offset ↔ LSP UTF-16 Position 转换。
`- [djot-tools] MVP async-lsp advertised notification 必须显式实现 handler，即使只是
  no-op。
`- [plumb] v1 semantic tokens、completion、folding、selection range、rename。

`## D. 导出（`"[plumb-export]"）

`- [plumb] MVP plumb 作为 Pandoc reader：AST → `"[pandoc_types]" JSON → pandoc writer。
`- [plumb] MVP core 有语法 error 时拒绝导出。
`- [plumb] MVP HTML 语义由 export extension 落地，而不是 core 落地。
`- [plumb] v1 meta、tasks、links 等 extension 结果参与导出。
`- [plumb] later 项目特定导出语义作为独立 extension 添加。

`## E. 笔记 CLI（`"[plumb-notes]"）

`- [djot-tools] v1 递归扫描根下 plumb 文档，载入 workspace，打印匹配路径。
`- [djot-tools] v1 查询语言：路径、元信息、链接关系、任务字段。
`- [djot-tools] v1 interactive picker：路径显示/输出、全文匹配、预览、`"[$EDITOR]" 打开、
  从 query 建档。
`- [djot-tools] v1 task 查询和状态变更：只产出文本编辑，是否落盘由 CLI 层决定。

`## F. tree-sitter（`"[tree-sitter-plumb]"）

`- [plumb] later 宽松镜像语法，用于编辑器高亮、文本对象、折叠、injection 和缩进。
`- [plumb] later 不做严格性引擎，不决定文档是否有效。
`- [plumb] later 语法冻结后再开始，避免跟随草案反复重写。

`## G. 打包与开发环境

`- [plumb] later Cargo workspace 和 crate 划分：`"[plumb-core]"、extensions、
  `"[plumb-ls]"、`"[plumb-export]"、`"[plumb-notes]"。
`- [djot-tools] later Nix flake 打包二进制。
`- [plumb] later warning gate：`"[cargo check --workspace --all-targets]"。

`## 建议实现顺序

`- 1. 冻结 MVP 语法和 AST。
`- 2. 实现 `"[plumb-core]"：lexer、block scanner、inline parser、diagnostics 和 focused tests。
`- 3. 冻结 extension contract，实现官方 host API、outline、anchors、links、workspace。
`- 4. 实现 `"[plumb-ls]" 的 diagnostics、documentSymbol、definition、references、hover。
`- 5. 实现 `"[plumb-export]" 和最小 HTML / Pandoc 导出。
`- 6. 增加 meta、tasks、completion、semantic tokens。
`- 7. 实现 `"[plumb-notes]"。
`- 8. 语法稳定后实现 `"[tree-sitter-plumb]"。
