{
  `: title plumb 文档
}

plumb 是一门严格、语义中立的 markup language。手写 parser 是语法有效性的唯一权威；
heading、list、metadata、link、citation、task 和 event 等含义由official semantic profile 提供。

`# 阅读路径

`- 第一次使用 plumb：从 `->[语法指南][guide/syntax.plumb] 开始，再阅读
   `->[标准扩展指南][guide/semantics.plumb]。
`- 管理 notes workspace：阅读 `->[workspace 指南][guide/workspace.plumb]。
`- 配置 editor：阅读 `->[editor integration 指南][guide/editor-integration.plumb]；当前实现
   覆盖 Neovim 0.12。
`- 使用 build、check、format 与 import/export：阅读
   `->[工具链指南][guide/toolchain.plumb]。
`- 查询精确规则：阅读 `->[core syntax reference][reference/core-syntax.plumb]、
   `->[standard semantics reference][reference/standard-semantics.plumb] 和
   `->[diagnostics reference][reference/diagnostics.plumb]。
`- 开发 plumb：阅读 `->[架构总览][architecture/overview.plumb]、
   `->[semantic analysis][architecture/semantic-analysis.plumb] 和
   `->[syntax tree][architecture/syntax-tree.plumb]；涉及 authoring operation 时再阅读
   `->[syntax editing][architecture/editing.plumb]。
`- 了解项目方向：阅读 `->[愿景][project/vision.plumb]、
   `->[路线图][project/roadmap.plumb]、`->[当前任务][project/tasks.plumb] 和
   `->[已完成任务][project/completed-tasks.plumb]；下一版 attached data 方向见
   `->[attached declarations proposal][project/attached-declarations.plumb]。
`- 追溯设计理由：阅读 `->[语法决策记录][history/syntax-decisions.plumb] 和
   `->[djot-tools 能力审计][history/djot-tools-audit.plumb]。

`# 权威顺序

文档发生冲突时，按以下顺序判断：

`. `"`reference/core-syntax.plumb" 决定 core syntax validity。
`. `"`reference/standard-semantics.plumb" 决定官方 semantic profile。
`. `"`architecture/" 决定官方工具链的实现边界。
`. `"`guide/" 解释如何使用当前 release。
`. `"`project/" 与 `"`history/" 记录方向和背景，不覆盖当前规范。

`# 状态归属

`"`guide/" 只描述当前 release 已提供的行为，不复制实现进度或 TODO。尚未完成、需要继续验证和
deferred 的工作统一记录在 `->[路线图][project/roadmap.plumb]；可直接实施和验收的工作统一
记录在 `->[当前任务][project/tasks.plumb]。标准 profile 明确排除的语义仍由
`->[standard semantics reference][reference/standard-semantics.plumb] 定义。
