{
 `: title plumb performance benchmark points
}

`# 1. Purpose

本页记录 2026-08-11 大 workspace 调查中已解决的 performance problems、可复现 workload 和后续 backend
comparison points。数值是特定机器上的 observation，不是跨机器 SLA；benchmark 必须记录 release commit、
host、input identity、sample count 与统计量。

`# 2. Reference workload

主要 stress document 是 `"migrated-from-khal.plumb"：5,682,790 bytes、134,054 lines、33,512 events，
其 folding response 包含 33,512 ranges，JSON payload 约 3.18 MB。仓库 benchmark 不复制个人 notebook
内容，而是 deterministic 生成相同数量级并覆盖 nested events、events with links、unmatched links 与大量
explicit anchors 的 fixture。

`# 3. Resolved performance problems

`- Syntax source offset/line lookup 使用预建 line starts 二分查找，避免 position-heavy consumers 对每个 offset
 从文档开头线性扫描。
`- Workspace 的 current/last-valid semantic generation 使用共享 ownership，合法 revision 不再复制整份
 DocumentOutput；notes/events/tasks command paths 也直接消费 workspace-owned source，避免再次复制大文档。
`- Whole-document formatting 的 changed-line projection 使用 unique anchors 分段 diff，避免重复 marker lines 令
 sequence diff 退化；CLI 直接消费 formatter 已持有的 source，`"fmt --check" 不再构造并应用无用 edits。
`- Agenda browser 不再一次 materialize 无界 event DOM；初始 render window 锚定 now，并使用 bounded paging。
`- LSP initial workspace indexing 移到 background，不再阻塞 open-document lifecycle；generation install
 保留 indexing 期间产生的 buffer revisions。
`- Folding byte offset 到 UTF-16 position 的 projection 复用 line-start index，避免每个 range 从 source
 开头重复扫描。
`- 一个 document 的 reference CodeLens 使用 batched reverse-reference query，避免按每个 anchor 重复
 遍历 workspace records。
`- event-contained links 利用 source-ordered link records 二分限定候选 suffix，不再为每个 event 从头扫描
 全部 links；后续仍计划建立 explicit containment index。
`- unchanged didOpen/full-text update 在 normalized path 与 source bytes 相同期间重绑 revision，复用
 ParsedDocument 与 DocumentOutput，不重复 parse 和 document semantics。

`# 4. Observed baseline

在上述真实 stress document 与当时 release build 上，server-side observation 为：plumb unchanged update
约 2 ms，CodeLens 约 2--6 ms，semantic tokens 约 0 ms，folding range generation 约 55 ms。

patched Neovim 0.12.4 中，JSON decode 约 18--47 ms，fold state evaluation 约 23--27 ms，
`"vim._foldupdate" 约 291--293 ms，完整 folding handler 约 314--320 ms。end-to-end fold-ready 约
2.50--4.52 s；instrumentation 显示 3.18 MB folding body 开始到接收完成约 4--42 ms，主要等待发生在 server
response 开始之前。

绕过 Neovim 的 minimal synchronous LSP client 中，didOpen write 约 8 ms，CodeLens/semantic data 约
343 ms 可用，fold response header 约 422 ms，body 约 798 ms 完成，parsed result 约 851 ms 可用。
Neovim instrumentation 中，5.82 MB didOpen 先进入 asynchronous stdin write queue；后续请求保持 protocol
ordering 并排在该 write 后。该 transport wait 不能归因于 plumb parse、fold generation、JSON decode 或
fold installation，backend benchmark 必须单独报告 server 与 editor end-to-end boundary。

`# 5. Backend comparison matrix

原 process-local backend 与 SQLite persistent semantic backend 必须在同一 release binary、同一 fixture、
同一 host 上分别测量：

`- cold build：空 cache 扫描、parse、document semantics、workspace index/store commit。
`- warm startup：cache identity 全部命中，到 complete workspace query 可用。
`- reverse references：document target、single anchor 与 batched CodeLens anchors。
`- event query：时间范围 agenda、event-task association 与 contained-link lookup。
`- unchanged reopen：disk snapshot 转 open revision，不重复 document-local computation。
`- open overlay：一个或多个 open documents 遮蔽 disk generation 后的 query latency 与 result parity。
`- save replacement：1K、10K 与 33,512-event document generation 的 transaction latency。
`- resources：steady-state resident memory、peak build memory、database bytes 与 write amplification。

每个 point 同时报告 absolute value 和相对 process-local baseline。wall-clock benchmark 不进入普通 correctness
CI 的硬阈值；普通 CI 固定 result parity、cache identity、generation atomicity、document shadowing 和已知
algorithmic work bounds。

`# 6. SQLite backend comparison on 2026-08-11

commit `"43b339e" 后的 benchmark harness 使用 33,512 events、3,351 references、2,038,805 source bytes
的 deterministic workload。host 为 Intel Core i9-9980XE、Linux 6.18.39、rustc 1.97.1；Criterion 使用
release profile。下列数值取 reported interval 的 midpoint：

`- cold build：process-local memory backend 176.70 ms；SQLite backend 654.63 ms，SQLite 为 3.70x，
 即回退约 270%。该路径同时执行 parse、document semantics 与首次 persistent transaction。
`- warm startup：SQLite content-hash hit 7.91 ms；原 backend 必须重新执行约 176.70 ms cold build，warm
 SQLite 快约 22.3x。独立 process 的首次 page-cache observation 为 23.94 ms，因此 cold OS cache 会缩小
 但不会消除该收益。
`- batched reverse references：memory 3.98 ms，SQLite 3.32 ms，SQLite 快约 16.7%。
`- agenda range：memory 25.26 ms，SQLite 65.12 ms，SQLite 慢约 2.58x。SQL time index 限定候选后，
 命中 EventRecord 的 Bincode decode 仍是独立成本；该回退不能描述为性能提升。
`- open overlay：SQLite disk generation 被一个完整 open document 遮蔽后，reverse-reference query 为
 2.35 ms，结果 parity 由普通 tests 固定。
`- full document replacement：33,512-event generation transaction 为 701.52 ms；它只在稳定 disk
 revision 发布时发生，不在 didChange/逐键路径执行。

Linux process profile 中，memory backend cold build 的 peak/resident 为 217,584 KiB。SQLite cold build
peak 为 207,672 KiB，事务完成后 resident 为 165,992 KiB；新 process warm startup 的 peak 为
23,428 KiB、resident 为 12,628 KiB，相比原 backend resident 降低约 94.2%。该值包含 allocator、SQLite
page cache 与 process runtime，不等同于 semantic records 的精确 retained bytes。

Bincode semantic records 下，首次 transaction 尚有 WAL 时 database、WAL 与 shared-memory files 合计
45,562,232 bytes；重新打开并 checkpoint 后合计 22,618,112 bytes。此前 JSON record prototype 为
75,262,976 bytes，且同一 agenda query 约 84.66 ms；binary record encoding 同时降低 size 与 decode
latency，但没有消除 agenda 相对 memory backend 的回退。

`# 7. SQLite TEMP open-document overlay experiment on 2026-08-11

`"semantic_overlay" release harness 在同一个 SQLite connection 中保留 main closed generations，并用
`"temp_store=MEMORY" 的 `"open_documents/anchors/links/semantic_references/tasks/events" 保存 open generation。
`"effective_events" 与 `"effective_references" TEMP views 使用 `"UNION ALL"，且 main branch 通过
`"NOT EXISTS open_documents" 实现 complete-document shadow。每类 record 在 generation transaction 中只
prepare 一次 insert statement；agenda records 和 direct-link references 与当前 Rust overlay 做 exact parity
assertion 后才进入 measurement。

同一 host、相同 deterministic workload 和 Criterion release profile 的 publication midpoint 为：

`- 1,000 events：当前 Rust parse、semantics 与 generation replacement 为 4.79 ms；pre-analyzed TEMP
 publication 为 3.28 ms；parse、semantics 与 TEMP publication 合计 8.32 ms，是当前路径的 1.74x。
`- 10,000 events：当前路径 85.13 ms；pre-analyzed TEMP publication 37.45 ms；完整 TEMP path
 138.95 ms，是当前路径的 1.63x。
`- 33,512 events：当前路径 314.37 ms；pre-analyzed TEMP publication 130.99 ms；完整 TEMP path
 498.10 ms，是当前路径的 1.58x。
`- 十次连续 33,512-event updates：当前路径 3.31 s；pre-analyzed TEMP publication 1.32 s；完整 TEMP
 path 5.04 s，是当前路径的 1.52x。连续 replacement 没有观察到超线性累积。

Prepared-statement reuse 是结果成立的必要条件。prototype 每行重新 prepare 的早期 observation 中，33,512
events 的 TEMP-only/full publication 分别约 291/654 ms；transaction 内按 record kind 复用 statement 后降为
约 131/498 ms。后续实现和 benchmark 不得退回 per-row prepare。

33,512-event open generation 上的统一查询 midpoint 为：

`- agenda range：当前 Rust overlay 25.49 ms；SQL effective view 34.79 ms，SQL 慢约 36.5%。
`- reverse references：当前 Rust overlay 4.02 ms；SQL effective view 2.24 ms，SQL 快约 44.3%。

结论不是统一 SQL 不可行：document shadow 可以完整封装在 TEMP views 中，且 reference-style indexed query
已有收益。阻碍是 publication 而非 overlay correctness；在当前 full-document parse/semantics 下，每次
didChange 同步发布 TEMP generation 会给 33,512-event document 额外增加约 184 ms，并把已有约 314 ms 的
更新扩大到约 498 ms。不能用 document-size threshold 隐藏该成本。生产重构应先获得 incremental semantic
generation/record diff，或定义 asynchronous generation publication 与 revision consistency；在此之前保留
Rust open-document output 与 document-granularity query composition 的总体 latency 更低。

`# 8. Unified TEMP SQL production rollback on 2026-08-11

commit `"2f85c76" 曾将 prototype 扩展为生产实现：closed generations 位于 SQLite main tables，open
generations 位于 connection-local TEMP tables，所有跨文档 query 通过 `"effective_*" views 统一。该实现
随后被撤回；以下数据作为 rejected design 的实测记录保留，不描述当前 backend。

相同 33,512-event release workload 相对第一阶段 SQLite plus Rust overlay baseline 的 midpoint 为：

`- 单次 open update：314.37 ms 增至 491.57 ms，回退 56.4%。
`- 十次连续 open updates：3.31 s 增至 5.59 s，回退 69.0%。
`- open agenda：25.49 ms 增至 47.59 ms，回退 86.7%。
`- persistent/site agenda：25.49 ms 增至 30.80 ms，回退 20.8%。
`- open reverse references：4.02 ms 增至 6.58 ms，回退 63.7%。
`- warm startup：7.91 ms 增至 8.67 ms，回退 9.6%。
`- warm persistent resident memory：12.6 MiB 增至 13.5 MiB，回退约 6.7%；open in-memory resident
 memory 从 217.6 MiB 降至 215.8 MiB，差异不显著。

第二阶段删除 Rust/SQL dual query composition，工程收益是单一 relational query path；但第一阶段已经通过
persistent SQLite 获得约 22.3x warm startup 和约 94.2% warm resident memory reduction。第二阶段没有重复
获得这些核心收益，反而使 full-generation TEMP publication 进入 didChange，并让 SQLite VM、effective view、
row conversion 与 record decode 进入 open-document query。因此当前实现恢复 persistent SQLite closed
generations plus Rust open overlay。只有 incremental semantic record diff 或异步 publication 能先移除逐键
full-generation cost，且完整 production parity benchmark 不再显示明显常用 query regression 时，才重新评估
统一 SQL open generations；不能以 document-size threshold 规避该设计成本。

`# 9. Web mutation workload

Web task mutation benchmark 使用 deterministic 生成的 176 documents、约 1,300 tasks workspace，并固定
包含 recurring task 的 status operation。measurement 分开记录 guarded edit、changed-document snapshot
replacement、当前 task query 和 JSON response；不得把 loopback connect time 当作 server latency。

mutation hot path 不得重新 scan 或分析未变化 documents，也不得在成功响应后由同一 browser revision
立即重复 task query 或触发 graph/task/event workspace refresh。integration test 以 instrumentation 证明
一次 mutation 只读取并替换 target document，并证明 response revision/result 可由 browser 直接安装。
真实 notebook smoke benchmark 记录 response size、time to first byte 与 total time；性能验收以优化前同一
commit build 的 paired measurement 为基线，不把依赖 host 的绝对 wall-clock 数字写成跨机器 SLA。
