`= title|plumb tasks

本页只记录可以直接实施和验收的工作。方向、约束和 deferred design 继续放在
`->[roadmap|roadmap.plumb]；已经结束的任务及其验收记录保存在
`->[completed tasks|completed-tasks.plumb]。

`# Core syntax

`- 实现 recursive owner syntax 与 member-envelope-v1 migration

 `+ task

 `@ recursive-owner-syntax

 `= created|2026-09-02T00:00:00+08:00

 `note strict parser、lossless/typed tree、formatter、plumb-edit、semantic profile、export/import、LSP、
 tree-sitter 与 repository documents 统一采用 optional-marker recursive owners；block 用 physical line 与
 indentation 组织，inline 用 direct ASCII SpaceRun 分隔 positional data，anonymous/marked groups 分别使用
 braces 与 marker-prefixed braces。

 `note inline/block verbatim 支持 anonymous 与 optional opaque marker；compact inline quote 是 strengthened
 brace envelope 的 sugar，block raw 使用独占 opener 与单 structural-space margin，不保留 raw tail。

 `note 新增 isolated member-envelope-v1 parser 与显式 migration，覆盖旧 brackets/pipe members、paragraph
 continuation、raw tail、quote-count block margin、delimiter escapes 和 current repository documents；migration
 输出新 syntax、保持 semantic projection 与 raw payload、可重复执行且拒绝 invalid legacy source。

 `note 验收包含 strict normative corpus、migration golden/idempotence、formatter/edit tests、semantic/export
 parity、tree-sitter corpus、project document graph、workspace all-target check 与 Clippy warning gate。
