`= title|plumb tasks

本页只记录可以直接实施和验收的工作。方向、约束和 deferred design 继续放在
`->[roadmap|roadmap.plumb]；已经结束的任务及其验收记录保存在
`->[completed tasks|completed-tasks.plumb]。

`- 按 capability 拆分 plumb-workspace implementation

 `+ task

 `@ split-workspace-capabilities

 `= created|2026-08-29T03:28:53+08:00

 `note 保留 Workspace 作为公共 facade，沿 document lifecycle/overlay、navigation/references、completion、rename、

 task 与 event operations 等已有 ownership boundary 拆分当前大型 implementation module。

 `note 以 batch indexer 的共享抽取作为首个边界，不引入仅转发调用的抽象层，也不改变 query、editing、revision、

 cache 或 adapter observable behavior。

 `note 分拆后保持 public API compatibility，并通过 workspace tests、adapter integration tests 与 warning gate；记录

 新模块的责任和 dependency direction，防止 adapter-specific types 进入 protocol-neutral crates。

`- 控制本地 Cargo build artifacts 增长

 `+ task

 `@ control-cargo-build-artifacts

 `= created|2026-08-29T03:28:53+08:00

 `note 测量 target 中 incremental、deps、binary 与重复 build variants 的空间和 rebuild-time contribution，记录

 可重复的 baseline，而不把单次本机 target size 当成仓库保证。

 `note 评估 documented periodic cleanup、cargo-sweep 与 dev profile debug-info policy；选择方案前测量 clean/incremental

 build time、debugger usability 和 disk usage，不能在普通 build、test 或 release workflow 中自动删除用户 cache。

 `note 将采用的维护命令和风险写入 contributor-facing documentation，并验证 root target 与其他 generated artifacts

 继续被 ignore；不提交 build outputs。

`- 让 semantic-store profiling fixture 使用当前语法

 `+ task

 `@ current-semantic-store-profile-fixture

 `= created|2026-08-29T03:28:53+08:00

 `note 移除 semantic_store_profile example 生成的 legacy brace ownership，改用当前 list facet、direct declaration

 与 positional-argument syntax，使生成 records、titles 和 source structure 与 production documents 一致。

 `note Criterion benchmark 与 manual profiler 复用一份 canonical fixture generator，或删除不再提供独立价值的重复

 example，避免两个 workload 再次漂移。

 `note strict-parse generated fixture，并验证预期 event/task counts、cache hit/miss 路径和 benchmark measurements

 仍覆盖其声明的 workload。

`- 减少 notes interactive mode 的旧 dependency generations

 `+ task

 `@ deduplicate-notes-interactive-dependencies

 `= created|2026-08-29T03:28:53+08:00

 `note 评估升级或替换 notes interactive mode 使用的 skim 0.10，减少与 workspace 当前 clap、syn、bitflags、nix

 和 unicode-width 并存的旧 dependency generations。

 `note 保持 fuzzy selection、preview、terminal input/output、single-binary packaging 与 Nix build behavior；补充 notes

 interactive tests 或等价的 protocol-neutral selection tests。

 `note 比较 clean build time、target size 和 release binary size；只有收益足以覆盖迁移与维护成本时才替换依赖，

 否则记录测量结果并关闭任务。
