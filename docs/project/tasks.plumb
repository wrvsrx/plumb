`= title plumb tasks

本页只记录可以直接实施和验收的工作。方向、约束和 deferred design 继续放在 `->{roadmap roadmap.plumb}；已经结束的任务及其验收记录保存在 `->{{completed tasks} completed-tasks.plumb}。

`- Finish semantic reducer deltas and equality invalidation
 `+ task
 `@ green-semantic-reducers
 `= created 2026-09-05T03:24:13+08:00

 Tree-based DocumentOutput、relative semantic shards、typed segmented collections、linear node splice与list span
 reducer已完成。Stable record topology现共享Arc node-index segments并O(1) rebind current SemanticTree；projection、
 anchor-id与list delta在node construction中单次累计。33,512-record incremental semantic tree为2.48 ms，完整
 revision pipeline为4.13 ms。继续把 nonempty heading hierarchy/section ends及带 diagnostics revision的 reducer state
 改为 persistent/chunked delta，并把 query hot path中剩余的 owned single-record projections替换为borrowed typed views。
 发布 added/changed/removed exported summaries，使纯
 layout或 semantic-equal edits不 invalidate无关 workspace dependency computations；differential tests覆盖
 semantic-analysis中尚未验收的 reducer delta规则。
