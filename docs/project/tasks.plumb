`= title plumb tasks

本页只记录可以直接实施和验收的工作。方向、约束和 deferred design 继续放在
`->[roadmap|roadmap.plumb]；已经结束的任务及其验收记录保存在
`->[completed tasks|completed-tasks.plumb]。

`task 用 Diesel 建立关系化 workspace query backend

 `@ diesel-workspace-query-backend

 `note 先将现有 SqliteSemanticStore schema、migration、generation transaction、CRUD 与 typed row decoding
  迁移到 Diesel，保持 memory/persistent result parity、non-UTF-8 path round-trip、cache invalidation 与现有
  semantic-store benchmark，不在第一步改变 query behavior。
 `note 再把 task dependencies、event-task associations 及 task/event 常用 query facts 投影为带 document
  generation 的 typed relation rows，建立 identity、target、state、time 与 ordering indexes；Rust semantics
  仍是事实权威，database 不解析 source 或重新推导 semantic meaning。
 `note 最后让 protocol-neutral workspace query 使用 Diesel 完成 filter、sort、cursor pagination 与 reverse
  lookup，Web mutation 只发布 changed document generation 并返回有界结果；覆盖 query-plan、open-overlay
  parity、adapter tests 与真实 notebook paired latency benchmark，移除被替代的 rusqlite plumbing。
 `note 当前 Diesel migration、task dependency relation、standard state query、Web changed-generation mutation
  与 lazy authoring candidates 已完成；通用 CEL filter/order/cursor pagination、event-task relation 和 rusqlite
  benchmark plumbing 清理仍待完成，因此本任务保持 open。
 `note 下一阶段只实施已有基线支持的边界：done 全集约 1.55 s/658 KB，因此增加 stable task cursor page；简单
  title CEL 约 12.84 s，因此用 CEL reference analysis 跳过未引用 relation facts；New task 打开约 209 ms 且
  创建约 920 个 cross-workspace options，因此改为 bounded candidate search。并发 ready 在 done 查询期间仍约
  121 ms，mutation 约 92 ms，暂不增加 async driver、connection pool 或增量 title/resource bookkeeping。
 `note demand-driven CEL facts、stable document-group cursor page 与 bounded candidate search 已完成。真实 notebook
  中 title CEL 降至约 0.172 s，done 首页约 0.279 s/150 KB、下一页约 0.360 s/89 KB，candidate search 约
  0.041 s/16 KB；authoring reference options 从约 920 降至约 40。显式 directly_blocking CEL 原约 12.5 s，
  改为 Diesel batch relation 与 open overlay lookup 后，冷启动首轮约 2.45 s，热查询约 0.139--0.161 s。

 当前仍没有证据支持先引入 async driver 或 connection pool。

 `note Web Agenda 已改为 Diesel stable-key cursor page：初始 page 围绕当前时间分别取得前后 records，earlier/
  later page 在 database 中按 resolved time、source path、source start 执行 hard limit，NULL time 位于有效时间之后
  且仍可到达；selected event 可以重新锚定有界 page，open documents 按 document generation overlay。真实 notebook
  的初始 `"/api/events" 从约 1.21 s/16.7 MB 降至约 0.103 s/71.5 KB（120 records），earlier 240-record page
  约 0.192 s/126 KB。browser 同时按 workspace revision 合并 watcher notifications，task mutation 不再因自己的
  notification 重复查询 Tasks 或刷新隐藏的 Graph/Agenda。

`task 建立 event containment 索引

 `@ event-containment-index

 `note 在 document analysis output 中显式表示 event 到 contained links 的关系，优先采用共享 link storage
  上可重叠的 index ranges，保持嵌套 events 可以关联同一 link，避免复制 source-backed records。
 `note workspace 的 event-task association、diagnostics、navigation、reverse references 与 event queries
  统一通过 indexed containment query，移除各 consumer 的范围 join 和重复 resolution plumbing。
 `note 用包含大量 events、links 与 nested events 的基准比较当前有序 links 二分查询，验证 build/query
  complexity、revision invalidation、memory overhead 与现有 association semantics 后再替换。

`task 让 folding 在 workspace 索引期间保持可用

 `@ folding-before-index-complete

 `note `"textDocument/foldingRange" 不等待也不要求 `"index_complete"；首次 workspace scan 尚未完成或
  永久 incomplete 时，server 仍立即返回只依赖当前 recovered syntax 的 structural ranges，以及可由当前
  document 独立确定的 metadata、event 与 task labels，不得以 `"workspace query result is incomplete" 拒绝
  整个请求。
 `note Task workflow state 只有在完整 query result 或本地事实足以唯一确定时才进入 `"collapsedText"。
  Closure、future wait、无 dependencies 的 ready 以及已发现 open dependency 的 blocked 均可直接确定；
  存在尚未完整解析的 dependencies 且仍可能在 ready/blocked 之间变化时，只省略该 task 的 custom label，
  不得省略 fold 或猜测 ready。
 `note 索引完成及后续 dependency facts 改变时，对声明 support 的 client 使用标准
  `"workspace/foldingRange/refresh" 请求更新 labels。Neovim adapter 只有在实现并声明该 capability 后才接收
  refresh；不支持 refresh 的 client 仍必须保留首次返回的 structural folds。
 `note Protocol tests 使用 non-null workspace root 和可控的未完成 initial index，覆盖首次请求不返回
  `"-32603"、本地与不确定 task labels 的降级、metadata/event labels 不受 task query 影响，以及索引完成后的
  capability-gated refresh。Neovim integration test 覆盖 initial request race，并把 client refresh support 留在
  Neovim fork 或正式 adapter boundary 内实现。
