`= title|plumb tasks

本页只记录可以直接实施和验收的工作。方向、约束和 deferred design 继续放在
`->[roadmap|roadmap.plumb]；已经结束的任务及其验收记录保存在
`->[completed tasks|completed-tasks.plumb]。

`task 定义并实现 block table 语义

 `@ block-table-semantics

 `note 使用可选 inline caption 的 `"table" block 表示表格；direct `"-" children 表示 rows。非空 row
  head 的 ordered arguments 是 compact inline cells；空 head row 的 direct `"-" children 是 expanded
  rich cells。两种形态不在同一 row 混用，连续 separators 表示 compact empty cell。
 `note `"+ header" facet 标记开头的 header rows，并允许在 expanded body row 中标记独立的 row-header
  cell。Cell head 投影为首段，cell children 继续容纳普通 blocks；空 cell 可表达。Table recognizer 拥有
  row/cell subtree，结构性的 `"-" blocks 不得同时进入普通 list recognition；cell children 内恢复普通
  block semantics，direct declarations 不占 row/cell position。
 `note 对非 cell 的 row children、table 中混入其它 direct children、header row 出现在
  body row 之后以及 effective column count 不一致提供 semantic diagnostics。首版支持 caption、empty/rich
  cells、连续 header rows、row-header cells、attributes 与一致列数，不实现 rowspan、colspan、column
  widths 或复杂的 Pandoc head/body/foot 分组。
 `note 先在 protocol-neutral semantics 中测试识别、subtree ownership、projection 与 diagnostics，再实现
  Pandoc Table 的 lowering/import round trip；随后按共享 semantic output 补充 exporter/importer tests，
  并在确有编辑器需求时加入 LSP folding 与 completion integration。

`task 建立 event containment 索引

 `@ event-containment-index

 `note 在 document analysis output 中显式表示 event 到 contained links 的关系，优先采用共享 link storage
  上可重叠的 index ranges，保持嵌套 events 可以关联同一 link，避免复制 source-backed records。
 `note workspace 的 event-task association、diagnostics、navigation、reverse references 与 event queries
  统一通过 indexed containment query，移除各 consumer 的范围 join 和重复 resolution plumbing。
 `note 用包含大量 events、links 与 nested events 的基准比较当前有序 links 二分查询，验证 build/query
  complexity、revision invalidation、memory overhead 与现有 association semantics 后再替换。

`task 在任意 document metadata entry 上提供文档 hover

 `@ document-metadata-hover

 `note 保留 document byte offset 0 作为 path-defined document declaration 的 hover 入口；光标位于
  任意 top-level direct `"=" metadata entry 的整个 subtree 时，也显示该文档投影后的完整
  metadata summary。普通 block 的 nested `"=" property 不得命中 document metadata hover。
 `note Link、citation 等更具体的 semantic target 优先于 metadata hover。Hover range 使用当前
  命中的 top-level metadata entry；不得用 metadata 从首个 entry 到末个 entry 的聚合
  range 直接命中，因为 document declarations 可以与 body blocks 任意交错。
 `note 在 protocol-neutral workspace query 中提供明确的 metadata target lookup，LSP 只负责
  position/range 投影和 Markdown rendering。测试覆盖文件开头、分散的 top-level entries、
  entry children、与正文交错的 declarations、nested owner properties 以及具体 target 的优先级。

`task 让 folding 在 workspace 索引期间保持可用

 `@ folding-before-index-complete

 `note `"textDocument/foldingRange" 不等待也不要求 `"index_complete"；首次 workspace scan 尚未完成或
  永久 incomplete 时，server 仍立即返回只依赖当前 recovered syntax 的 structural ranges，以及可由当前
  document 独立确定的 metadata、event 与 task labels，不得以 `"workspace query result is incomplete" 拒绝
  整个请求。
 `note Task workflow state 只有在完整 query result 或本地事实足以唯一确定时才进入 `"collapsedText"。
  Closure、future wait、无 dependencies 的 ready 以及已发现 open dependency 的 blocked 均可直接确定；
  存在尚未完整解析的 dependencies 且仍可能在 ready/blocked 之间变化时，只省略该 task 的 custom label，
  不得省略 fold 或猜测 ready。
 `note 索引完成及后续 dependency facts 改变时，对声明 support 的 client 使用标准
  `"workspace/foldingRange/refresh" 请求更新 labels。Neovim adapter 只有在实现并声明该 capability 后才接收
  refresh；不支持 refresh 的 client 仍必须保留首次返回的 structural folds。
 `note Protocol tests 使用 non-null workspace root 和可控的未完成 initial index，覆盖首次请求不返回
  `"-32603"、本地与不确定 task labels 的降级、metadata/event labels 不受 task query 影响，以及索引完成后的
  capability-gated refresh。Neovim integration test 覆盖 initial request race，并把 client refresh support 留在
  Neovim fork 或正式 adapter boundary 内实现。
