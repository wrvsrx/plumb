`= title plumb 标准语义指南

core 只识别结构；本页介绍官方工具链赋予常用 marker、inline kind、direct declarations 与 children 的语义。 本文保留 happy path 和组合示例；field validation、target classification、diagnostics、export lowering 与明确排除项 统一见 `->{{standard semantics reference} ../reference/standard-semantics.plumb}。

`# Heading

一至六个 `"#" 表示 heading level：

`plumb"
  `# 一级标题
  `## 二级标题

heading 默认只有 outline identity。需要成为 link 或 rename target 时才添加显式 id：

`plumb"
  `# 可引用的标题
   `@ intro

cursor 位于尚无 id 的 heading/generic marked block 时，Add explicit id 根据 content 生成 unique id。verbatim leaf
不能拥有 declarations；需要 raw 与 id 时使用 transparent wrapper + marked raw child。

`# List 与 definition

`"-" 是 bullet item，`"." 是 ordered item。相邻、同层且 marker 相同的 items 聚合为一个 list：

`plumb"
  `- First
  `- Second
    `- Nested
  `. One
  `. Two

ordered list 固定从 1 开始。

`":" 是 definition entry。没有 children 时 first positional element是 term、remaining head elements是 inline body；有 children 时
完整 head 是 term、children 是 body：

`plumb"
  `: Term Inline body.
  `: Term
    Definition body.

`# Table

`"table" direct `"-" children 是 rows。compact row 每个 direct positional element是 cell；SpaceRun 可以 padding 对齐但
不改变 arity：

`plumb"
  `table
   `- name    age
    `+ header
   `- Alice   10
   `- Bob     20

direct `"+ header" 标 leading header rows。`"{}" 是 empty cell。expanded row 使用 direct anonymous block
children。

`plumb"
  `table People
   `-
    `+ header
    `- name
    `- age
   `-
    `- Alice
     `+ header
    `- 10
      `note Approximate.

同一 row 不混用 compact 与 expanded cells，所有 rows 的 effective column count 必须一致。expanded cell 的 head 是可选首段，ordinary children 是后续 blocks；空 head 且没有 body 表示 empty cell。首版不支持 rowspan、colspan、column widths、alignment、table foot 或复杂 head/body grouping。

`# Metadata

document 是 implicit root owner。顶层 direct `"=" block 是 metadata key/value association，可以与正文 blocks 交错，且不会拆分投影后相邻的正文 list。document identity 来自 normalized workspace-relative path，document 也没有 attribute class container，因此顶层 direct `"@" 与 `"+" 都不受支持：

`plumb"
  `= title Document title
  `= created 2026-07-20T09:00:00+08:00
  `= tags
   `+ plumb
   `+ notes
  `= author
   `= name Alice

value 可以是 null、一个 paragraph scalar、`"+" sequence、nested `"=" map 或一个 verbatim block。metadata sequence 只使用 `"+" 并保留 source order。`":"/`"-" 是可渲染的 definition/list structure，`"="/`"+" 是不进入 rendered body 的 map/sequence structure。

`"created" 使用带 timezone 的完整 RFC 3339 timestamp。非法值会产生 `"metadata.invalid-created" warning，但不使 core syntax 无效。空文档也可以通过 `"Insert document metadata" code action 创建带默认 filename title 与当前 created 的 metadata； 该 action 只在 cursor 精确位于 document start（byte offset 0）时提供，并在 byte 0 插入 direct `"title" 与 `"created" declarations。两个 scalar 保持同行，并按最长 key 对齐 separator；filename title 含 spaces 时仍作为 ungrouped first-rest value 输出：

`plumb"
  `= title   Project Guide
  `= created 2026-08-30T00:00:00+08:00

`# Link

`"->" 使用 one or more non-declaration positional elements：one element产生 derived-label Link，原 rich element是 label且
Stringify value 是 target；two or more elements产生 explicit-label Link，first 是 label，所有 remaining elements共同
形成 target：

`plumb"
  `->{guide.plumb}
  `->{`!{guide.plumb}}
  `->{{same document} #intro}
  `->{{another document} guide.plumb}
  `->{guide Project Guide.plumb}
  `->{{another anchor} guide.plumb#intro `@{guide}}
  `->{website https://example.test}

本地 anchor 必须显式存在。跨文件 target 使用相对 `"..plumb" path。explicit-label Link 的 rest target 是 raw
filesystem path；原 source 有 whitespace时才产生 semantic space，adjacent elements直接拼接。中文、spaces、
parentheses 与 percent characters 都按字面保留，不进行 URL encode/decode。derived-label Link 的 multi-element
target需要一个 anonymous Group或 verbatim spelling。

旧 consecutive slots 与 inline attached group 不属于 current syntax，formatter 与其他工具均不读取这些历史写法。

derived-label Link 可以使用 compact verbatim spelling：

`plumb"
  `->"https://example.test/a%20b"
  `->"{https://[2001:db8::1]/}"
  `->"guide.plumb#intro"
  `->"../assets/manual draft.pdf"

payload 同时作为 label 与 target，并保留原始 spelling。parsed one-argument 与 compact Link 使用相同 target
classification：absolute/network target 按 URI 检查，relative target 是当前 document 目录下的 raw filesystem
path。`"#" 分隔 explicit anchor；payload 含 double quote 或 closing-like sequence 时增加 inline verbatim quote
strength。`"..plumb" path 与 explicit anchor 可以由 LSP 补全。自定义 label 使用 first/rest explicit-label
`"->"。

ordinary inline `"-"/`"->" prefix 只提供 canonical parsed Link template；`"->{" 保持该候选，`"->\"" 提供
compact verbatim Link template。完成后的 one-element/two-or-more-element forms分别使用 derived/explicit label。

`# 内嵌链接

给 parsed Link 添加 direct `"embed" facet，表达内嵌展示链接目标的意图。单参数从目标派生 label，多参数的 first 是 label，其余共同构成 target；多词或富文本 label 使用一个 anonymous Group，空图片 alt 使用 empty Group：

`plumb"
  Text with `->{{status icon} static/status.png `+{embed}} inline.
  `->{static/status.png `+{embed}}
  `->{{} static/decorative.svg `+{embed}}
  `->{{Demo video} static/demo.mp4 `+{embed}}
  `->{Podcast https://example.test/stream `+{embed} `={type audio/ogg}}

显式 `"type" 属性优先指定 MIME；否则按目标扩展名识别，忽略大小写，并对 URL 排除 query/fragment。显式类型未知时直接保留链接，不再猜扩展名。识别不读取文件或请求网络。exporter 将图片导出为 Pandoc Image，其余保留 Link；Web 按类型呈现图片、视频或音频，播放器旁保留可点击 fallback。目标类型或输出格式不支持内嵌时使用链接 fallback。

普通下载附件直接使用普通 Link，不需要 facet：

`plumb"
  `->{{Download report} reports/final.pdf}

embed 引用不产生笔记导航图边，包括未知类型 fallback。LSP 在 positional target 中补全本地文件，并提供 hover、go-to-definition 与 unresolved-file diagnostic。具体类型表与诊断规则见 `->{{standard semantics reference} ../reference/standard-semantics.plumb}。

`# Citation

标准 citation 接受一个 plain id：

`plumb"
  `= bibliography
   `+ static/library.json
  See `cite{smith2004}.

`"bibliography" metadata 接受一个或多个 workspace 内的 CSL JSON relative paths。LSP 从 这些 files 补全 citation id，并提供 hover 和 definition；Web viewer 通过 citeproc 渲染 inline citation 与文末 bibliography。Bibliography file 改变时 Web watcher 自动刷新页面。

其他 citation shape 的 profile 边界见 `->{{standard semantics reference} ../reference/standard-semantics.plumb}。

`# 透明 containers

anonymous/marked parsed blocks 都可拥有 children；`"()" 是 official export transparent container：

`plumb"
  `() Warning
   `@ notice
   `warning

   More details.
  This is `(){important `+{warning}}.

它们直接导出为 Pandoc Div/Span；其他 generic marker/kind 仍保留 `"data-plumb-marker"。

`# Quote

`">" block 的 head 是引用中的第一个 paragraph，children 是后续引用内容：

`plumb"
  `> A quoted opening.
    A second quoted paragraph.
    `> A nested quote.

export 产生 Pandoc BlockQuote。quote 上未消费的 declarations 通过内部 Div 保留；`"quote" 不是 alias，仍按 generic block 导出。quote 不隐式解释 attribution 或 citation。

`# Inline styles

常用 inline styles 使用简短 kind，并消费全部 visible content：

`plumb"
  `*{emphasis with spaces}
  `!{prefix`*{nested}suffix}
  `=={mark}
  `~{strikeout}
  `^{superscript}
  `_{subscript}
  `!{}

style 接受 zero or more Text/Space/Group/Verbatim elements，不做 positional arity validation；direct declarations
从 visible body过滤并作为 attributes保留。anonymous Group只在 style owner作为父级 positional value时用于组合，
style body自身不需要额外分组。除 mark 使用 Pandoc `"Span.mark" 外，其余分别产生 Pandoc Emph、Strong、
Strikeout、Superscript 与 Subscript。`"**"、`"em"、`"strong"、`"mark" 不是 aliases；italic/bold 是
emphasis/strong 的常见渲染，不需要独立 spelling。

`# Math

使用 opaque `"$" verbatim kind：

`plumb"
  Inline `$"\pi r^2".
  `()
   `@ energy
   `$"
    E = mc^2

inline 与 display 由 envelope 决定，input 默认是 TeX。内容出现 closing bracket 时使用现有 quote strength。普通 dollar characters 和 dollar-delimited math 不具有特殊含义。

`# Event

bullet 或 ordered list item 添加 direct leaf `"+ event" facet 后成为 event：

`plumb"
  `- 14:00--15:30 Parser review
   `+ event
   `@ parser-review
   `= date 2026-07-30
   `= timezone +08:00
   `= tasks #write-parser

first positional element是 schedule，remaining head elements是 nonempty title。schedule 使用 `"HH"、`"HH:MM"、`"HH:MM:SS"
point 或 `"START--END" interval，并继承 date/timezone context。tasks 关联文档任务路径或 explicit task ids；event/task facets
mutually exclusive。完整边界见 standard semantics reference。

`"date"/`"timezone" 沿 marked-block subtree 继承，最近的 override 优先，dedent 后恢复 parent context：

`plumb"
  `()
   `= date 2026-07-31
   `- 09:00 使用 7 月 31 日
    `+ event
  `- 09:00 恢复 document metadata date
   `+ event

快速创建 event 时，在 list item head 开头输入 shorthand，再在该行执行 `"Convert to event" CodeAction； title 可以包含 inline markup：

`text"
  `- 11 relax: phone
  `- 11:10:24 precise reminder
  `- 11:00--11:20 relax: phone
  `- 2026-05-21T11:10--11:20 parser review
  `- 2026-05-21T11:10--2026-05-23T09:00 conference
  `- 00:05--00:15 wheel: distinguish `"nix develop" and normal shell
  `- 18:00-- event 1
  `- 18:30-- event 2
  `- 19:00--20:00 event 3

time 使用两位 `"HH"、`"HH:MM" 或 `"HH:MM:SS"；省略分钟/秒时补零，省略 date/timezone 时优先继承 document metadata，缺少时使用执行操作的本地日期/offset。显式 date 使用 `"YYYY-MM-DDT" prefix。 end 可写 time 或显式 date-time，但不能写 offset 或 `"Z"。time-only end 早于 start 时解释为次日； dated end 不 rollover，且所有 interval 的 end 必须严格晚于 start。 转换会在该 list item 上原位追加 `"event" facet，并把规范化 schedule 留在 head 开头；title 中的 parsed/verbatim inline 及其 ordered members 原样保留。它不生成 explicit id 或 UID metadata；只有需要引用 event 时才用通用操作添加 id。event 可用 direct `"uid" scalar/literal property 固定 iCalendar UID；省略时 vdir exporter 从 workspace-relative path、schedule 与 title 确定性派生 UID，因此同一 workspace 在不同 设备上产生相同的 calendar files。重复的有效 UID 会使完整 export 失败；`".ics" filename 直接使用 UID。

显式 date 写入 event override；batch shorthand conversion只转换匹配 items。schedule/title 与 properties使用
SpaceRun boundaries，连续 properties可按 Unicode width对齐。trailing `"--" 从 next sibling推断 end。

`"plumb event --root DIR" 列出 events，并支持使用 `"--query" 对 path、id、title、uid、date、timezone、when、 解析后的 at/start/end、tasks 与 now 执行 CEL。`"plumb event --root DIR export-vdir --output DIR" 生成受管理的只读 vdir；输出目录 可以作为 khal 的 `"type = calendar"、`"readonly = true" calendar。每次导出原子替换旧 projection 并 清理 stale items；缺少有效 time 的 event 不能 export，不要在该目录手工维护 events。

Web Agenda 提供在线创建、编辑和删除；Calendar/khal 只读取 projection。首版不支持 recurrence、全天 event、reminder、外部 iCalendar 回写或 CalDAV client。

`# Task

文档根层添加 direct leaf `"+ task" facet 后，整个文件成为一个任务：

`plumb"
 `= title Release plan
 `+ task
 `= priority 10

 Release details.

 `- Verify release
  `+ task
  `@ verify

文档任务标题取 title，缺省使用文件名；正文是详情，顶层列表任务成为它的子任务。父子关系不自动传播 focused、完成状态或依赖。文档任务不支持 recur；移除 task facet 保留正文、字段、focus 历史和子任务。文件身份使用相对路径，不需要也不支持根层 id。

引用 `"Release Plan.plumb" 指向文档任务，`"Release Plan.plumb#verify" 指向列表任务；目标没有 task facet 时不会退回第一个子任务。单个无 fragment 的值整体作为一个路径。多个引用可逐项书写，路径内的空格不会拆项：

`plumb"
 `+ task
 `= depends
  `+ Release Plan.plumb
  `+ other.plumb#review

bullet 或 ordered list item 添加 direct leaf `"+ task" facet 后成为 task：

`plumb"
  `- Implement parser
   `+ task
   `@ write-parser
   `= priority 10
   `= created 2026-07-20T09:00:00+08:00
   `= due 2026-07-21T09:00:00+08:00

head 是 title，未被声明消费的 children 是 details。nested task items 形成 display tree；另一个 task 需要引用它时才添加 id。table recognizer 消费的 row/cell 不产生 task/event record。

标准字段包括 `"created"、`"due"、`"wait"、`"done"、`"canceled"、`"recur"、`"prev"、 `"priority"、`"depends" 和 `"focused"。priority 是 32-bit signed integer，数值越大越优先；缺失时排序权重 默认为 `"0"，因此正数提前、负数延后。effective priority 除了从 children 提升 ancestors，也沿 `"depends" 提升仍 open 的 dependencies；多级 dependency chain 继续传播，closed dependency 不传播。 时间 property 使用 RFC 3339。 Task reference 使用 raw relative path，不做 URL encode/decode；`".plumb#" 分隔 path 与 explicit id，path 本身不能包含 `"#"。`"depends" 中每条 reference 在 id 后由 whitespace 分隔，因此 path 可以直接包含 spaces：

`plumb"
  `- Review
   `+ task
   `= depends #local Project A.plumb#build Project B.plumb#test

LSP 在 depends 与 prev 的引用项内补全 workspace tasks：没有 fragment 时可选择文档任务路径，或以 same-file `"#"、cross-file `"path.plumb#" 继续选择列表任务 id；fragment 后只替换 id。根层声明、grouped 项和 direct `"+" sequence 复用相同引用上下文。候选排除自身、已有引用和 non-task anchors。

`"focused" 把一个 task 标记为已列入当前工作线，值为一个时间区间或 direct `"-" 子项区间列表；endpoint 是带 explicit offset 的 RFC 3339 timestamp，按 instant 而不是字符串比较：

`plumb"
 `- Review
  `+ task
  `= focused 2026-09-20T09:00:00+08:00--2026-09-20T11:00:00+08:00
 `- Second pass
  `+ task
  `= focused
   `- 2026-09-20T09:00:00+08:00--2026-09-20T11:00:00+08:00
   `- 2026-09-20T14:00:00+08:00--

single value 与 one-element list 语义等价。区间按 start 升序且同一 task 内不得重叠，可以首尾相接；end 不得早于 start，最多一个 open interval（`"start--"）且必须位于最后；不同 task 的区间可以重叠。 只有存在有效 open interval 且 task 未关闭时才算当前 focused；已结束的历史不会让 task 退出候选，也不会提高它在 Tasks 树中的排序。 focus 与 workflow state 正交：ready、waiting、blocked 都可以 focused，state 变化不会自动结束区间。focus 是 task-local 事实， 不传播到 parent、child 或 dependency task。

`"recur" 接受正整数 `"`PnD"、`"`PnW"、`"`PnM" 或 `"`PnY"，并要求 `"due"。

document-local closure state 由 closure timestamps 推导：没有 `"done"/`"canceled" 是 open；只有 `"done" 是 done；只有 `"canceled" 是 canceled；两者同时存在是 conflicted。

workspace 工具在 closure state 上推导互斥 workflow state：同时关闭是 conflicted；仅完成是 done； 仅取消是 canceled；open task 依次按 future `"wait"、open dependency 判断为 waiting、blocked，否则为 ready。future wait 与 open dependency 同时存在时 state 是 waiting，但 blocked fact 仍为 true，waiting reasons 同时包含 time 与 dependency。

关闭 recurring task 会保留当前 instance，并紧接着创建下一个 sibling instance。新 instance 使用 canonical formatter layout，不在两个 task 之间添加空行，并保留当前 nesting 与 LF/CRLF style。

Convert to task 保留 list marker并添加 direct `"+ task" facet 与 created property。Task mutations 可以按 Unicode
width对齐连续 property SpaceRuns；properties使用 `"key value"，不再写 pipe。

`"created"、`"due"、`"wait"、`"done" 与 `"canceled" 必须是 RFC 3339 timestamp property。非法值发布 `"task.invalid-datetime" warning，不参与 state 或 operations。完成或取消 nested task 时只格式化该 task subtree，不改变 following sibling 的层级。

`"focused" 的非法格式、重复属性、顺序、重叠、多个或非末尾的 open interval 分别发布 `"task.invalid-focus"、 `"task.duplicate-focus"、`"task.unordered-focus"、`"task.overlapping-focus" 与 `"task.multiple-open-focus" warning；closed task 仍含 open interval 时发布 `"task.focus-on-closed"。这些 diagnostics 都不改写源文档，focus 数据无效的 task 也不会被当作"未 focused"参与候选。

done task 仍有 open dependency 时发布 `"task.done-with-open-dependency" warning；仍有 open descendant 时发布 `"task.done-with-open-descendant" warning。同一个 child 同时是 descendant 与显式 dependency 时 只报告 dependency inconsistency。warnings 不阻止 operation，canceled parent 不受此检查。

精确 recognition、diagnostics 和 export semantics 见 `->{{standard semantics reference} ../reference/standard-semantics.plumb}。
