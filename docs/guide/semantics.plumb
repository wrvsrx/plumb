{
 `: title plumb 标准语义指南
}

core 只识别结构；本页介绍官方工具链赋予常用 marker、inline kind 和 attached elements 的语义。

`# Heading

一至六个 `"#" 表示 heading level：

`plumb"
  `# 一级标题
  `## 二级标题

heading 默认只有 outline identity。需要成为 link 或 rename target 时才添加显式 id：

`plumb"
  `# 可引用的标题 {
    `@ intro
  }

cursor 位于尚无 id 的 heading、generic marked block 或 verbatim block 时，`"Add explicit id" code
action 根据 head 生成 document-unique id。空 head 使用 marker，verbatim block 使用 `"block"；已有
attached elements 会保留。

`# List 与 definition

`"-" 是 bullet item，`"." 是 ordered item。相邻、同层且 marker 相同的 items 聚合为一个 list：

`plumb"
  `- First
  `- Second
    `- Nested

  `. One
  `. Two

ordered list 固定从 1 开始。

`":" 是 definition entry，head 是 term，children 是 definition body：

`plumb"
  `: Term

    Definition body.

`# Metadata

文档 metadata 是文件开头的 document attached group，其中每个 marked block 的 marker 是 key：

`plumb"
  {
    `title Document title
    `: created 2026-07-20T09:00:00+08:00
    `tags
      `- plumb
      `- notes
    `author
      `name Alice
  }

value 可以是 null、一个 paragraph scalar、`"-" sequence、nested `":" map 或一个 verbatim
block。metadata sequence 只使用 `"-"；它表示数据顺序，不带正文 list style。

`"created" 使用带 timezone 的完整 RFC 3339 timestamp。非法值会产生
`"metadata.invalid-created" warning，但不使 core syntax 无效。空文档也可以通过
`"Insert document metadata" code action 创建带默认 filename title 与当前 created 的 metadata；
该 action 只在 cursor 位于 document start（第一个非空白字节之前）时提供。

`# Link

自定义 label、相对文档与 anchor 使用 `"->"，target 放在 attached `"to" property：

`plumb"
  `->[same document]{`:[to #intro]}
  `->[another document]{`:[to guide.plumb]}
  `->[another anchor]{`:[to guide.plumb#intro]}
  `->[website]{`:[to https://example.test]}

本地 anchor 必须显式存在。跨文件 target 使用相对 `"..plumb" path。named Link 的相对 `"to" value
是 raw filesystem path；中文、spaces、parentheses 与 percent characters 都按字面保留，不进行 URL
encode/decode。

label 与 target 相同时，可以改用 Autolink：

`plumb"
  `->"https://example.test/a%20b"
  `->"[https://[2001:db8::1]/]"
  `->"guide.plumb#intro"
  `->"../assets/manual draft.pdf"

payload 同时作为 label 与 target，并保留原始 spelling。它与 named Link 使用相同 target
classification：absolute/network target 按 URI 检查，relative target 是当前 document 目录下的 raw
filesystem path。
`"#" 分隔 explicit anchor；payload 含 double quote 或 closing-like sequence 时增加 inline verbatim quote strength。`"..plumb"
path 与 explicit anchor 可以由 LSP 补全，补全原样插入 path，并在需要时自动增强 delimiter。自定义
label 继续使用显式 `"->"。
opaque `"->" kind 只用于 verbatim inline，不能与 `"to" property 组合。

创建 Autolink 时，在 ordinary inline context 输入 `"`-" 或 `"`->" prefix，并从 construct completion
选择 Autolink；两者同时提供 Link 与 Autolink，继续输入 `"`->[" 时只保留 Link，输入 `"`->\"" 时只
保留 Autolink。生成 `"->" kind 后，LSP 才在 payload 中提供 path 与 explicit-anchor completion；
bare backtick、单独 `"`[" 或 unclosed inline verbatim 不提供 construct candidates。

`# Image

`"img" content 是 alt text，`"src" 是 absolute URI 或相对当前 document 的 file reference：

`plumb"
  Text with `img[status icon]{`:[src static/status.png]} inline.

  `img[]{`:[src https://example.test/decorative.svg]}

empty alt 用于 decorative image。image 单独占一个 paragraph 时不需要另一种 block syntax。
相对 src 是 raw filesystem path，中文、spaces、parentheses 和 percent characters 都按字面保留，
不做 URL encode/decode。LSP 在 src 中补全常见本地图片文件，并为存在的文件提供 hover 和
go-to-definition。image 的
标准 profile 边界见 `->[standard semantics reference]{`:[to ../reference/standard-semantics.plumb]}。

`# File attachment

`"file" content 是始终可用的 fallback label，`"src" 指向 absolute URI 或相对当前 document 的文件：

`plumb"
  `file[Demo video]{`:[src static/demo.mp4]}
  `file[Download report]{`:[src reports/final.pdf]}

普通 exporter 把 attachment 写成可点击 Link。Web viewer 会把 workspace 内能识别为 video 的本地文件
显示为带 controls 的播放器，同时保留下载链接作为浏览器不支持该格式时的 fallback；PDF、archive 或
未知类型继续显示为普通附件链接。MIME 不改变 `"file" 语义，普通 `"->" Link 也不会自动变成播放器。
LSP 在 src 中补全任意本地文件，并提供 hover、go-to-definition 和 unresolved-file diagnostic。

`# Citation

标准 citation 接受一个 plain id：

`plumb"
  {
   `: bibliography
    `- static/library.json
  }

  See `cite[smith2004].

`"bibliography" metadata 接受一个或多个 workspace 内的 CSL JSON relative paths。LSP 从
这些 files 补全 citation id，并提供 hover 和 definition；Web viewer 通过 citeproc 渲染
inline citation 与文末 bibliography。Bibliography file 改变时 Web watcher 自动刷新页面。

其他 citation shape 的 profile 边界见
`->[standard semantics reference]{`:[to ../reference/standard-semantics.plumb]}。

`# Div 与 span

`"div" 和 `"span" 是没有隐式 presentation semantics 的透明 containers：

`plumb"
  `div Warning {
    `@ notice
    `warning
  }

    More details.

  This is `span[important]{`-[warning]}.

它们直接导出为 Pandoc Div/Span；其他 generic marker/kind 仍保留 `"data-plumb-marker"。

`# Quote

`">" block 的 head 是引用中的第一个 paragraph，children 是后续引用内容：

`plumb"
  `> A quoted opening.

    A second quoted paragraph.

    `> A nested quote.

export 产生 Pandoc BlockQuote。quote 上未消费的 attached elements 通过内部 Div 保留；`"quote" 不是 alias，仍按
generic block 导出。quote 不隐式解释 attribution 或 citation。

`# Inline styles

常用 inline styles 都使用单字符 kind：

`plumb"
  `*[emphasis]
  `![strong]
  `=[mark]
  `~[strikeout]
  `^[superscript]
  `_[subscript]

除 mark 使用 Pandoc `"Span.mark" 外，其余分别产生 Pandoc Emph、Strong、Strikeout、Superscript 与
Subscript。未消费的 attached elements 通过 Span 保存。`"**"、`"em"、`"strong"、`"mark" 不是 aliases；italic/bold
是 emphasis/strong 的常见渲染，不需要独立 spelling。

`# Math

使用 opaque `"$" verbatim kind：

`plumb"
  Inline `$"\pi r^2".

  `$" {`@[energy]}
    E = mc^2

inline 与 display 由 envelope 决定，input 默认是 TeX。内容出现 closing bracket 时使用现有 quote
strength。普通 dollar characters 和 dollar-delimited math 不具有特殊含义。

`# Event

`"event" marker 是 specialized bullet list item：

`plumb"
  `event 14:00--15:30 Parser review {
    `@ parser-review
    `: date 2026-07-30
    `: timezone +08:00
    `: tasks #write-parser
  }

head 的第一个 positional argument 是 schedule，后接 whitespace 与 title。schedule 使用 `"HH"、`"HH:MM"、`"HH:MM:SS" point 或 `"START--END" interval。start 也可写
`"YYYY-MM-DDTTIME" 来覆盖 date 并继承 timezone；若指定 offset 或 `"Z"，则必须写包含秒的完整 RFC 3339
timestamp，例如 `"2026-05-02T08:22:31+08:00--09:20"。end 可写 `"TIME" 或
`"YYYY-MM-DDTTIME"；两者都从 start 继承 offset，end 不允许携带 timezone。仅含 time 的 end 默认使用
start date 并在更早时解释为次日；带 date 的 end 可表达多日 interval。
event 默认继承 metadata `"date" 与 numeric-offset `"timezone"；RFC 3339 metadata date 也提供自身 offset。
同名 event pairs 可逐条覆盖。解析后的 end 必须严格晚于 start。
旧 `"at"、`"start"、`"end" 不具有 event semantics。`"tasks" 显式关联同文件或跨文件 task id，不产生 dependency，也不从 task
timestamps 猜测 event。`"event" 与 `"task" 是互斥的 specialized list-item markers；同名 attached
facets 只是 opaque data。

`"date"/`"timezone" 沿 marked-block subtree 继承，最近的 override 优先，dedent 后恢复 parent context：

`plumb"
  `div {
    `: date 2026-07-31
  }
    `event 09:00 使用 7 月 31 日
  `event 09:00 恢复 document metadata date

快速创建 event 时，在 list item head 开头输入 shorthand，再在该行执行 `"Convert to event" CodeAction；
title 可以包含 inline markup：

`text"
  `- 11 relax: phone
  `- 11:10:24 precise reminder
  `- 11:00--11:20 relax: phone
  `- 2026-05-21T11:10--11:20 parser review
  `- 2026-05-21T11:10--2026-05-23T09:00 conference
  `- 00:05--00:15 wheel: distinguish `"nix develop" and normal shell
  `- 18:00-- event 1
  `- 18:30-- event 2
  `- 19:00--20:00 event 3

time 使用两位 `"HH"、`"HH:MM" 或 `"HH:MM:SS"；省略分钟/秒时补零，省略 date/timezone 时优先继承
document metadata，缺少时使用执行操作的本地日期/offset。显式 date 使用 `"YYYY-MM-DDT" prefix。
end 可写 time 或显式 date-time，但不能写 offset 或 `"Z"。time-only end 早于 start 时解释为次日；
dated end 不 rollover，且所有 interval 的 end 必须严格晚于 start。
转换会在该 list item 上原位追加 `"event" facet，并把规范化 schedule 留在 head 开头；title 中的
parsed/verbatim inline 及其 attached data 原样保留。它不生成 explicit id 或 UID metadata；只有需要引用
event 时才用通用操作添加 id。event 可用 direct `"uid" scalar/literal property 固定 iCalendar UID；省略时
vdir exporter 从 workspace-relative path、schedule 与 title 确定性派生 UID，因此同一 workspace 在不同
设备上产生相同的 calendar files。重复的有效 UID 会使完整 export 失败；`".ics" filename 直接使用 UID。

显式 date 写入 event override；省略 date/timezone 时优先继承 document metadata，缺少对应 metadata 时
才使用 operation 当前本地日期/offset。选中多行时可执行 `"Convert selected items to events"
批量转换范围内匹配 shorthand 的 list items；不匹配或已是 event 的 item 保持不变。
只写 trailing `"--" 时，结束时间取紧邻的下一条 sibling shorthand start；转换会把结果固化为完整
schedule。没有下一条的 chain 末项暂不转换，普通 item 或其他 block 会中断推断；给末项写显式 end 后即可
一次转换整条 chain。

`"plumb event --root DIR" 列出 events，并支持使用 `"--query" 对 path、id、title、uid、date、timezone、when、
解析后的 at/start/end、tasks
与 now 执行 CEL。`"plumb event --root DIR export-vdir --output DIR" 生成受管理的只读 vdir；输出目录
可以作为 khal 的 `"type = calendar"、`"readonly = true" calendar。每次导出原子替换旧 projection 并
清理 stale items；缺少有效 time 的 event 不能 export，不要在该目录手工维护 events。

Web Agenda 提供在线创建、编辑和删除；Calendar/khal 只读取 projection。首版不支持 recurrence、全天
event、reminder、外部 iCalendar 回写或 CalDAV client。

`# Task

`"task" marker 是 specialized bullet list item：

`plumb"
  `task Implement parser {
    `@ write-parser
    `: priority 10
    `: created 2026-07-20T09:00:00+08:00
    `: due 2026-07-21T09:00:00+08:00
  }

head 是 title，children 是 details。另一个 task 需要引用它时才添加 id。

标准字段包括 `"created"、`"due"、`"wait"、`"done"、`"canceled"、`"recur"、`"prev"、
`"priority" 和 `"depends"。priority 是 32-bit signed integer，数值越大越优先；缺失时排序权重
默认为 `"0"，因此正数提前、负数延后。effective priority 除了从 children 提升 ancestors，也沿
`"depends" 提升仍 open 的 dependencies；多级 dependency chain 继续传播，closed dependency 不传播。
时间 property 使用 RFC 3339。
Task reference 使用 raw relative path，不做 URL
encode/decode；`".plumb#" 分隔 path 与 explicit id，path 本身不能包含 `"#"。`"depends" 中每条
reference 在 id 后由 whitespace 分隔，因此 path 可以直接包含 spaces：

`plumb"
  `task Review {
    `: depends #local Project A.plumb#build Project B.plumb#test
  }

LSP 在 `"depends" scalar value 内分两阶段补全 workspace tasks：当前 reference 尚无 `"#" 时只列出含有
可选 task 的 document，选择后插入 same-file `"#" 或 cross-file `"`path.plumb#"；`"#" 后只列出已选
document 中的 explicit task ids，并仅替换 id fragment。task id 候选显示 title、relative document path 和
derived workflow state。补全保留同一 value 中其他 references，并排除当前 task、已经列出的 dependencies
与 non-task anchors。

`"recur" 接受正整数 `"`PnD"、`"`PnW"、`"`PnM" 或 `"`PnY"，并要求 `"due"。

document-local closure state 由 closure timestamps 推导：没有 `"done"/`"[canceled]" 是 open；只有 `"done" 是
done；只有 `"canceled" 是 canceled；两者同时存在是 conflicted。

workspace 工具在 closure state 上推导互斥 workflow state：同时关闭是 conflicted；仅完成是 done；
仅取消是 canceled；open task 依次按 future `"wait"、open dependency 判断为 waiting、blocked，否则为
ready。future wait 与 open dependency 同时存在时 state 是 waiting，但 blocked fact 仍为 true，waiting
reasons 同时包含 time 与 dependency。

关闭 recurring task 会保留当前 instance，并紧接着创建下一个 sibling instance。新 instance 使用
canonical formatter layout，不在两个 task 之间添加空行，并保留当前 nesting 与 LF/CRLF style。

cursor 位于普通 list item 时，`"Convert to task" code action 将 marker 改为 `"task" 并添加 `"created" property；
已有 task 缺少 `"created" 时，`"Add task created timestamp" 可以补录。两者使用执行时的本地
RFC 3339 timestamp。

`"created"、`"due"、`"wait"、`"done" 与 `"canceled" 必须是 RFC 3339 timestamp property；legacy pair
必须 quoted。非法值发布
`"task.invalid-datetime" warning，不参与 state 或 operations。完成或取消 nested task 时只格式化该
task subtree，不改变 following sibling 的层级。

done task 仍有 open dependency 时发布 `"task.done-with-open-dependency" warning；仍有 open descendant
时发布 `"task.done-with-open-descendant" warning。同一个 child 同时是 descendant 与显式 dependency 时
只报告 dependency inconsistency。warnings 不阻止 operation，canceled parent 不受此检查。

精确 recognition、diagnostics 和 export semantics 见
`->[standard semantics reference]{`:[to ../reference/standard-semantics.plumb]}。
