`= title plumb syntax guide

本文介绍 current recursive-owner syntax。精确 validity/recovery 以
`->{{core syntax reference} ../reference/core-syntax.plumb} 为准；旧 brackets/pipe epoch 只由 migration reader
处理。

`# Blocks and indentation

每条 nonblank physical line 开始一个 block，或作为 preceding parsed owner 的 plain continuation。普通 line 是
anonymous parsed block；反引号加 marker 是 marked block：

`plumb"
 ordinary paragraph
 `note Head content

newline 分隔 blocks，indentation 建立 children。anonymous/marked parsed block 都可以拥有 children；canonical
indentation 是一格 ASCII space：

`plumb"
 `parent
  `child

`plumb"
 `parent Head
  `child One
  `child Two

立即缩进且没有前置 blank 的 plain line继续 owner inline content；typed projection把 boundary视为一个 space。
blank line结束 continuation，之后的缩进 plain line成为 anonymous child。显式 empty anonymous block 写作 `"{}"。

block marker 后必须是 ASCII space 或 line ending。marker 紧邻 brace/quote 时是 inline owner，因此：

`plumb"
 `note content
 `note{content}

前者是 marked block，后者是 anonymous block 内的 marked inline group。

`# Inline data and groups

inline sequence losslessly 保存 Text 与 ASCII SpaceRun。direct one-or-more spaces 在当前 depth 分隔 positional
data；空格数量是 formatting，不产生 empty datum。一个 datum 也可以由多个 adjacent terms 组成：

`plumb"
 prefix`!{strong}suffix
 `row Alice   10   {}

anonymous group `"{...}" 在 parent 中占一个 term，用于 multiword positional value；marked group 把 marker
放在 braces 外：

`plumb"
 {guide with spaces}
 `!{strong}
 `->{{guide page} {Project Guide.plumb}}
 `node{visible `@{stable} `={key value}}

group 可以 nesting，但不跨 physical line。empty group `"{}" 是 explicit empty datum。brackets 与 pipe 是
ordinary text。

`# Escapes

parsed text 只有三种 introducer escape：

`plumb"
 `` literal backtick
 `{ literal opening brace
 `} literal closing brace

不要使用 Markdown backslash escape。tab/control 在 parsed content 非法；verbatim payload 可以保留任意 bytes。

`# Inline verbatim

常见 raw 使用 compact quote pair，可带 opaque marker且允许 empty：

`plumb"
 `"cargo test"
 `$"x^2"
 `$""

payload 含 quote 或以 brace 开头时使用 full envelope；closing-like collision 通过增加 quote strength 解决：

`plumb"
 `"{raw containing "quotes"}"
 `""{raw containing }" safely}""

两种 spelling 产生同一个 Verbatim node，且只能占一条 physical line。

`# Block verbatim

own-line quote opener 进入 indentation-terminated raw。anonymous/marked forms：

`plumb"
 `"
  anonymous raw

`plumb"
 `rust"
  fn main() {}

payload 每行携带 opener indentation 加一 structural space；只 strip 这一格，其余 bytes 完整保留。raw blank
line 也必须携带 margin。没有 closing fence、quote-count margin 或 raw tail。

verbatim block 不能拥有 parsed children。需要 id/properties 时使用 ordinary wrapper 与 marked raw child：

`plumb"
 `()
  `@ example
  `= language rust
  `rust"
   fn main() {}

`# Direct ownership

block direct children 与 inline group direct content 使用同一个 recursive model。official profile 把 direct
leaf `"@"、`"+"、`"=" owners 投影为 id/facet/property；core 仍保留 generic source order：

`plumb"
 `node Head
  `@ stable
  `+ notice
  `= level 2

`plumb"
 `node{visible `@{stable} `+{notice} `={level 2}}

property/definition 没有 children 时 first datum 是 key/term，remaining data sequence 是 value/body；有
children 时完整 head 是 key/term，children 是 value/body：

`plumb"
 `= title Document title
 `: {Term with spaces} Inline body

`plumb"
 `= author
  `= name Alice

`plumb"
 `: Term with spaces
  `() Block definition body

`# Migration

former brackets member envelope、pipe separators、quote-count block margin 和 raw tail 使用：

`plumb"
 plumb migrate --from member-envelope-v1 FILES...

CLI 先用 frozen legacy parser strict-parse，再通过 current owned syntax输出并用 current parser验证。`"--check"
只报告需要迁移的 files；path replacement atomic。不要用 global string replacement 模拟 converter。
