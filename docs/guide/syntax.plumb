`= title|plumb 语法指南

plumb 使用反引号引入特殊结构。特殊 spelling 一旦开始就必须完整合法；错误不会静默退化为
普通文本。

`# Paragraph 与 marked block

普通文本组成 paragraph。marked block 由一个反引号、非空 marker 和可选
head 构成：

`plumb

|"
  普通 paragraph。

  `note 一条有 head 的 block
  `note 带 direct children
   `@ stable
   `+ warning
   `= level|2

block 可以拥有缩进 children。第一个 child 建立 sibling column；formatter 无论 head 是否为空，
都把 children 放在 owner column 加一：

`plumb

|"
  `- Parent

     Child paragraph.

     `- Nested block

空行后的缩进 paragraph 是 child；没有空行的缩进普通文本会继续前一行的 head。

paragraph 与 block head 都包含 ordered parsed arguments。first argument 没有前导 separator，后续
arguments 用当前 depth 的 bare `"|" 分隔；紧邻 argument boundary 的 ASCII spaces 是 padding，typed
argument view 会从两端移除，但 lossless source 与 formatter 原样保留。连续 pipes 或只有 padding 的区域会产生
empty arguments。普通 ASCII space 始终产生 `"Inline::Space"，不会建立 argument boundary；literal pipe
写成 `` `|"：

`plumb

|"
  `event 14:00--15:30|Parser review
  `row Language server|Waiting||Optional note
  ordinary first argument|second argument

marker 后第一个 ASCII space 同时是 head 的首个 `"Inline::Space"；typed first argument 会把它作为 boundary
padding 移除。tab 或其他 whitespace 不能打开 head。block children 位于完整 head argument sequence 之后。
每层 nested parsed argument 都独立通过 typed view trim，因此 `"`()[ value ]" 投影为 `"value"。确实需要
argument 以 ASCII space 开头或结尾时，用 backtick-space 把该 space 写成 literal text：

`plumb

|"
  `row ` Alice` |10
  `kind[` value` ]

空 head 的 first child 也可以与 parent 写在同一行；child introducer column 决定后续对齐：

`plumb

|"
  `- `- a
     `- b

`# Inline element

具名 inline element 使用 `"`kind[content]"，content 会继续解析 nested inline：

`plumb

|"
  See `term[outer `term[inner] text].

同一个 owner 在一对 brackets 内拥有 `"|" 分隔的 ordered members。ordinary content 是 argument，
省略 introducer 且独占 member 的具名 element 是 child：

`plumb

|"
  `pair[first|second]
  `pair[`*[rich first]|second with spaces|note[child]]
  `pair[|"[raw]"]

first member 固定为 parsed argument 且可以为空；后续 argument 与 child 可以交错。只有当前 depth 的 pipe
建立 member boundary；member 两端的 direct ASCII spaces 是 typed padding。每个 nested parsed argument
也独立 trim；padding 不会把 full verbatim argument 或 introducer-elided child 改判为普通 argument。
verbatim argument 也只能出现在 pipe 后，如上面的第三行。core 保存统一的 source order，不解释 kind、
argument 含义或数量。

element 可以跨属于同一 paragraph/head 的 continuation lines：

`plumb

|"
  See `term[first line
  second line].

id、facet、property 或普通 structural child 也放在同一 envelope：

`plumb

|"
  `term[value|@[stable]|+[warning]|=[level|2]]

inline element 即使 first argument 为空也必须写出 `"[]" delimiter；不存在 zero-member element 或裸
`"warning" shorthand：

`plumb

|"
  `warning[]

需要字面 closing bracket 时写两个反引号再跟 closing bracket。

`# Verbatim

inline verbatim 适合 code、路径和其他不需要 inline parsing 的内容：

`plumb

|"
  Run `"cargo test".
  A literal quote: `"[say "hello"]".

引号数量决定 closing delimiter 的强度。只有 payload 中出现较短 closing spelling 时才增加
引号。

anonymous verbatim block 由 introducer 与一个或多个 quotes 开始；quote count 声明 raw payload 的
ASCII-space structural margin。它是没有 head 或 children 的 raw leaf：

`plumb

|"
  `"
   anonymous raw

具名 raw 使用普通 marked block 的 terminal raw tail。owner 可以先拥有任意 direct children，再由与
owner column 对齐、独占一行的 quote run 进入 raw phase：

`plumb

|"
  `rust
   `@ example

  |"
   fn main() {
       println!("hello");
   }

quote count 声明 raw payload 的 ASCII-space structural margin；没有 children 时 boundary 紧接 head，存在
children 时最后一个 child 与 boundary 之间保留空行。结构缩进后的 payload bytes 会原样保留。内部空行
可以完全空白；尾部空行只有带完整 structural margin
才属于 payload，否则它是 block layout。verbatim block 没有 closing fence。

`# Document root

document 是 implicit root owner，保存 source-ordered top-level blocks。brace 是 ordinary parsed text，
不建立 ownership structure。顶层 declaration 是普通 marked block；例如 official semantic profile
使用 direct `"=" 表达 document metadata：

`plumb

|"
  `= title|Document title

  Body paragraph.

`# Direct ownership 与 raw tail

document 与 marked block 都用同一个 source-ordered direct children sequence 表达 ownership。declaration
与普通 structural children 可以任意交错；core 不根据 marker spelling 改变结构：

`plumb

|"
  `node Head
   `@ intro
   `+ note
   `= level|2
   `note ordinary child

  `task A long head that
   wraps across lines

   `= created|2026-08-07T09:00:00+08:00

core syntax 不解释 `"+"、`"@" 或 `"="。official semantic profile 将 direct `"+" child 投影为 facet，
将 direct `"@" declaration 投影为 identity，将 direct `"=" association 投影为 property，并从 owner
的普通 body projection 中过滤已消费 declarations。

raw tail 是 marked owner 的 optional singular field，不是 child。quote run 之后永久进入 raw phase，
所以 raw tail 必须位于全部 children 后，结构上也不能出现第二个 payload。匿名 raw 需要 declarations
或普通 children 时，使用 `"()" explicit owner 与同一个 raw-tail form。

旧 postfix `"{}" group 与 `"{#id .class key=value}" slot 都不属于 current syntax；brace 只作为
ordinary text 保留。legacy 文件必须显式使用 `"plumb migrate --from attached-v1"，formatter 不读取旧语法。
已经移除 attached groups、但 block association、compact definition 或 event 仍用旧 space boundary 的
文件使用 `"plumb migrate --from head-space-v1"；该 converter 只把旧 recognizer 确认的 separator token
最小替换为 `"|"，不会重新格式化其余 source。

`# Escape

两个连续反引号产生一个字面反引号：

`plumb

|"
  ``note is text, not a block.

单个反引号加 ASCII space、`"`["、`"]" 或 `"|" 是 delimiter 的字面转义，在任何 parsed position 无条件成立；
backtick-space 产生 literal `"Inline::Text" space，因此不会被 boundary trim 移除。
裸 brackets 与 inline member boundary 上的 pipe 是结构拼写。brace 是 ordinary text，不需要 escape。
不要使用 Markdown 的 backslash escape，也不要假设 punctuation 在所有位置都有特殊含义。

完整 grammar、indentation 与 recovery 规则见
`->[core syntax reference|../reference/core-syntax.plumb]。
