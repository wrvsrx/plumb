`= title plumb editor integration 指南

`# LSP boundary

`"plumb lsp" 提供 diagnostics、document symbols、definition、references、hover、completion、rename、
CodeAction、CodeLens、semantic tokens、folding ranges、formatting、range formatting 和 workspace search。

LSP protocol 由 editor client 选择 `"rootUri"/`"workspaceFolders"。plumb server 不向上查找 marker，也
不向 client 声明 marker；client integration 应优先使用 `".plumb"，再 fallback 到 editor 自己的
project marker。workspace 与 ignore 行为见 `->[workspace 指南|workspace.plumb]。

当前官方 editor integration 只覆盖 Neovim 0.12。未来其它 editor 应复用相同的 LSP 与 workspace
contract，并在本文增加同级章节。

`# Neovim setup

确保 `"plumb" binary 可从 `"PATH" 启动，并将 packaged `"contrib/nvim" 或 source checkout 中的同名
directory 加入 `"runtimepath"。在打开首个 plumb buffer 前显式调用 setup：

`lua"
  require('plumb').setup({
    command = 'plumb',
    lsp = { root_markers = { '.plumb', '.git' } },
    codelens = { picker = 'quickfix' },
    search = { picker = 'native', task_filter = 'state == "ready"' },
  })

setup 默认启用 LSP、CodeLens 与 search，不创建 keymaps；重复 setup 不会重复 commands、autocmds 或
clients，并可直接接受 function-valued options：

`lua"
  require('plumb').setup({
    lsp = { on_attach = function(client, bufnr) end },
  })

packaged runtime 的标准 `"ftplugin/plumb.lua" 会对每个 plumb buffer 启用 `"expandtab"，设置
`"shiftwidth=1"，并让 `"softtabstop" 跟随它。这样 Tab/indent commands 产生 formatter 使用的单个
structural space；buffer 改为其它 filetype 时这些 local options 会恢复。plugin 不修改 `"tabstop"，因此
verbatim payload 中真实 tab bytes 的显示宽度继续服从用户配置。

底层等价的 Neovim native LSP 配置是：

`lua"
  vim.filetype.add({ extension = { plumb = 'plumb' } })

  local capabilities = vim.lsp.protocol.make_client_capabilities()
  capabilities.workspace.workspaceEdit.documentChanges = true
  capabilities.workspace.workspaceEdit.resourceOperations = { 'rename' }
  capabilities.workspace.didChangeWatchedFiles =
    capabilities.workspace.didChangeWatchedFiles or {}
  capabilities.workspace.didChangeWatchedFiles.dynamicRegistration = true
  capabilities.workspace.didChangeWatchedFiles.relativePatternSupport = true

  vim.lsp.config.plumb = {
    cmd = { 'plumb', 'lsp' },
    filetypes = { 'plumb' },
    root_markers = { '.plumb', '.git' },
    capabilities = capabilities,
  }

  require('plumb.codelens').setup({ picker = 'quickfix' })
  vim.lsp.enable('plumb')

Linux 上 Neovim 默认可能不启用 dynamic file watching；上述 capability 允许 client 注册
`"..plumb" 与 `".ignore" watchers，系统还需要可用的 watch backend。

`# Tree-sitter

tree-sitter parser 只负责 highlight、fold、indent、text object 和 injection，不负责 strict
diagnostics。parser 与 queries 独立于 `"plumb.nvim" 安装；plugin 不注册 parser、不携带 queries，也不调用
`"vim.treesitter.start()"。editor configuration 可以统一启动已注册 parser：

`lua"
  vim.api.nvim_create_autocmd('FileType', {
    pattern = 'plumb',
    callback = function(args)
      vim.treesitter.start(args.buf, 'plumb')
    end,
  })

repository development 使用 `"dev/nvim.lua" 加载当前 checkout 的 parser 与 queries。先运行：

`text"
  ./tree-sitter-plumb/build-parser.sh
  nvim -u dev/nvim.lua document.plumb

`# Folding

`"textDocument/foldingRange" 使用 recovered strict syntax，并返回两类整行 fold：heading fold 覆盖完整
section，verbatim block fold 覆盖 raw body。带 multiline owned group 的 marked block 先提供 head 加
owned group 的 owner fold；存在 children 时再提供覆盖整个 node 与 descendants 的 subtree fold。两层从
同一行开始，外层 subtree range 排在前面；没有 children 时不重复相同 range。没有 owned group 的 marked
block 只提供 subtree fold。普通多行 paragraph 与 inline element 不单独折叠。即使 document 存在 syntax
error，未受错误影响的 recovered blocks 仍可折叠；server 遵守 client 声明的 `"rangeLimit"，按 source
order 保留外层范围。

客户端声明 `"foldingRange.foldingRange.collapsedText" support 时，task 与 event fold text 保留 backtick
与完整 marker。Task 使用固定五字符 state slot：ready 使用空白槽，waiting 使用波浪号，blocked 使用等号，
done 使用 o，canceled 使用 x；conflicted 组合为 ox。常见状态 slot 后补齐空格，
使同级 task title 对齐，但不为 conflicted 状态改变组合表达，并保留 task block opener 的原始结构缩进。
Task text 保留 backtick 与完整 "task" marker，后接 state 和 title。event fold 把 date、timezone、when 属性
解析出的时间（省秒、省偏移）与 title 拼成保留 backtick 与完整 "event" marker 的摘要；point 使用精确到分钟
的时间与 title，interval 追加两个短横线和结束时间。顶层有效 metadata 使用 `"METADATA  Document title"，缺少可用 title
时回退为 `"METADATA"；第一层 metadata entry fold 使用 `"key  scalar value"，list、map、null 或不可展示
value 只使用 `"key"。label 规范为空格分隔的单行并在 80 characters 后截断。三者都保留原始结构
缩进。没有该 capability、无可靠 valid task/metadata facts 或无可用时间的 event fold 不携带 custom
label。

Task subtree 与紧邻的同层 task sibling marker 相同时，把两者之间一个直接相邻的空白分隔行包含在 subtree
fold range 中，使同类 task 关闭后不遗留孤立空行；只消费一个直接相邻的空白行，不跨过下一条非空内容。
next sibling 使用不同 marker、verbatim 或其他 block boundary 时，空白分隔行保留在 fold range 外，继续
显示 block 种类变化形成的分组边界。Task owner fold 与其他 block、heading fold 继续遵守自身的 source
range，不扩张到 trailing layout。

Task/event 只有一条 physical line 时，server 也会向声明 `"lineFoldingOnly = true" 的 client
返回 `"startLine == endLine" 的 semantic fold，且省略 character boundaries。该 capability 只要求
range 使用整行 boundary，不排除单行 range。支持这类 range 的 editor 还需要类似
`"foldminlines = 0" 的设置，才能关闭只占一个 screen line 的 fold。上游 Neovim 0.12 的
LSP fold adapter 会忽略这类 range；plugin 不补充 folding-range adapter。

当前官方 Neovim integration 使用 `"~/Documents/nur-packages" 提供的 patched Neovim 0.12.4，以及原生
`"vim.lsp.foldexpr()" 和 `"vim.lsp.foldtext()"。patch 修复多个相邻 nested folding ranges 在同一行结束时
被 foldexpr 合并的问题，使 owner/subtree 两层 range 不会吞并后续 sibling；它不增加 single-physical-line
fold support，也不恢复 folding topology 重建前的手动 open/closed state。原生 adapter 仍按
`"foldlevel"、`"foldmethod=expr" 和自身的 fold engine 规则重建 folding tree；编辑造成 topology 变化时，
用户通过 `"zo"、`"zc" 选择的局部状态不保证跨越所有结构重建继续保留。plugin 不在 Lua 层复制 fold
engine，也不使用 range overlap、cursor visibility 或延迟 `"foldopen"/`"foldclose" 猜测并恢复用户状态。

这是当前的兼容性取舍，不是最终设计。长期愿景是让 Neovim 拥有现代的 identity-aware folding：fold
source 以稳定 identity 和可随编辑移动的 anchor 表示，状态仍按 window 保存，更新按受影响的局部
结构增量完成，并能通过正式 API 区分不同 namespace、读取实际 fold tree 与保留手动 open/closed
override。LSP 的无 identity `"FoldingRange" 仍需采用明确的保守匹配策略；不能确认对应关系时，应
遵守 window 的 `"foldlevel"，而不是强行改变用户状态。

plugin 不修改 window-local folding 设置。editor configuration 可以在 `"LspAttach" 中仅对支持该 method
的 client 启用：

`lua"
  vim.api.nvim_create_autocmd('LspAttach', {
    callback = function(args)
      local client = vim.lsp.get_client_by_id(args.data.client_id)
      if client and client:supports_method('textDocument/foldingRange') then
        vim.wo.foldmethod = 'expr'
        vim.wo.foldexpr = 'v:lua.vim.lsp.foldexpr()'
        vim.wo.foldtext = "v:lua.require'plumb'.foldtext()"
        vim.wo.foldminlines = 0
        vim.wo.foldlevel = 99
      end
    end,
  })

独立 tree-sitter package 的 `"folds.scm" 可以作为没有 LSP client 时的 lenient fallback；editor 可以在
plumb LSP 支持 folding 时优先使用 LSP ranges。

`"require('plumb').foldtext()" 包装原生 `"vim.lsp.foldtext()"，并为 waiting、blocked、done、canceled、
conflicted task 的整条 fold label 分别使用 PlumbTaskFoldWaiting、PlumbTaskFoldBlocked、PlumbTaskFoldDone、
PlumbTaskFoldCanceled、PlumbTaskFoldConflicted。它们默认链接到 DiagnosticInfo、DiagnosticHint、Comment、
DiagnosticDeprecated、DiagnosticWarn；ready、event、metadata 与 fallback labels 保持原样。fold-only groups
不改变展开 task body 的 semantic highlight。LSP `"collapsedText" 本身只携带文字，highlight chunks 由
plugin 在 editor 端补充。

`# Closed task highlighting

LSP semantic tokens 将 done task 的完整 block 标为 `"task + completed"，将 canceled task 标为
`"task + canceled"；同时具有两个 closure fields 的 conflicted task 携带两个 modifiers。父 task 的
range 会扣除 nested task blocks，child task 始终按自身状态着色。空行不产生 token。

完整 plugin 默认把两种 token 链接到 `"Comment" highlight，因此 closed task 保持可读但降低对比度。
不使用 plugin 时可以直接配置 Neovim 0.12：

`lua"
  vim.api.nvim_set_hl(0, '@lsp.typemod.task.completed.plumb', {
    link = 'Comment',
  })
  vim.api.nvim_set_hl(0, '@lsp.typemod.task.canceled.plumb', {
    link = 'Comment',
  })

颜色方案或用户配置可以把这两个 groups 改成任意颜色、italic 或 strikethrough；semantic token 只提供
状态与 source ranges，不 conceal 文本，也不自动关闭 fold。

`# Authoring workflow

普通 Neovim LSP mappings 可以直接调用 plumb 标准功能：

`lua"
  vim.keymap.set('n', 'gd', vim.lsp.buf.definition)
  vim.keymap.set('n', 'gr', vim.lsp.buf.references)
  vim.keymap.set('n', '<leader>rn', vim.lsp.buf.rename)
  vim.keymap.set({ 'n', 'x' }, '<leader>ca', vim.lsp.buf.code_action)
  vim.keymap.set('n', '<leader>f', function()
    vim.lsp.buf.format({ name = 'plumb' })
  end)

construct completion 按 marker prefix 与 syntax owner 触发：裸 introducer 不提供候选；line start 的反引号加
`"t" prefix 提供 Task，`"e" prefix 提供 Event，并在继续输入完整 marker 时保持对应候选。line start 与
ordinary inline context 都以反引号加连字符或箭头 prefix 同时提供 Link 与 Autolink；继续输入 `"->[" 时只
提供 Link，输入 `"->\"" 时只提供 Autolink。ordinary inline context 中的 `"c" 至 `"cite" prefix
提供 Citation snippet；在 `"cite[...]" content 中，从 document `"bibliography" metadata 声明的
CSL JSON files 补全 ids。单独 opening bracket prefix 不提供候选。snippet-capable client
使用 tab stops。Task completion 根据 client 的 multiline insertion mode 投影缩进，因此在顶层或嵌套位置
都生成相同的 canonical expanded-group layout。
CodeAction 包括 metadata insertion、explicit id、event shorthand conversion、task conversion、
created insertion 和 task status operations。metadata insertion 只在 cursor 精确位于 document start（byte offset 0）
时提供；event shorthand 的具体格式见
`->[Event|semantics.plumb]。whole-document 与 range formatting 使用同一个 canonical formatter。
Whole-document formatting 由 protocol-neutral formatter 产生 non-overlapping line-diff edits，LSP 只做
position mapping，并只替换实际变化的行；Neovim 使用
`"vim.lsp.foldexpr()" 时，这会减少未变化 source 被整篇 replacement 触发 fold 重算的机会；但原生
fold engine 在 folding topology 确实变化时仍可能重新决定局部状态，plugin 不承诺跨越这类重建恢复
手动 fold。Format-on-save 通过同一 `"textDocument/formatting" handler 获得相同行为。

光标位于顶层 `"meta" marker 时，`"vim.lsp.buf.rename()" 重命名当前 document file，并更新 workspace
内指向它的 links、Autolinks 和 task references。rename input 只显示当前 filename stem；输入同目录的新
stem 时不写 `".plumb" extension，server 会自动保留它。operation 不修改 metadata `"title"。client 必须
支持 `"workspaceEdit.documentChanges" 与 `"RenameFile" resource operation。

`# Note and task search

`"plumb.search" 使用 `"plumb/search" custom request。native picker 基于 `"vim.ui.input/select"：

`lua"
  vim.keymap.set('n', '<leader>pn', function()
    require('plumb.search').search_notes()
  end)

  vim.keymap.set('n', '<leader>pt', function()
    require('plumb.search').search_tasks({ filter = 'state == "ready"' })
  end)

`"live_search" 允许 picker adapter 提供 debounce、cancellation、preview 和 incomplete-result UI；Lua API
见 `"contrib/nvim/README.plumb"。

`"plumb.search.search_notes(opts)" 与 `"search_tasks(opts)" 使用当前 buffer，或者显式的 `"opts.bufnr"，
选择 attached clients 中声明 `"experimental.plumb.search"、`"[method = "plumb/search"]" 与
`"schemaVersion = 3" 的一个。没有兼容 client、schema 不匹配、request error 与 malformed response 都通过
`"vim.notify" 报告；空结果交给 `"vim.ui.select"，选择结果后使用对应 client 的 `"offset_encoding"
调用 `"vim.lsp.util.show_document" 跳转到 `"Location"。

可以直接执行一行命令：

`text"
  :lua require('plumb.search').search_notes()
  :lua require('plumb.search').search_tasks({ filter = 'state == "ready"' })

也可以注册稳定的 user commands 与 keymaps：

`lua"
  vim.api.nvim_create_user_command('PlumbNotes', function()
    require('plumb.search').search_notes()
  end, {})
  vim.api.nvim_create_user_command('PlumbTasks', function()
    require('plumb.search').search_tasks({ filter = 'state == "ready"' })
  end, {})
  vim.keymap.set('n', '<leader>pn', '<cmd>PlumbNotes<cr>')
  vim.keymap.set('n', '<leader>pt', '<cmd>PlumbTasks<cr>')

底层调用不经过 `"workspace/executeCommand"；它直接发送 `"plumb/search"：

`lua"
  local client = assert(vim.lsp.get_clients({ bufnr = 0, name = 'plumb' })[1])
  client:request('plumb/search', {
    kind = 'task',
    query = 'review',
    filter = 'state == "ready" && due < now',
    limit = 100,
  }, function(err, result)
    if err then
      vim.notify(err.message, vim.log.levels.ERROR)
      return
    end
    if vim.tbl_isempty(result.items) then
      vim.notify('plumb: no matching tasks', vim.log.levels.INFO)
      return
    end
    vim.lsp.util.show_document(result.items[1].location, client.offset_encoding, { focus = true })
  end, 0)

params 的 `"kind" 是 `"note"、`"task" 或 `"event"；`"query" 是 fuzzy text，`"filter" 是 CEL，`"limit" 最大
200。result 是 `"schemaVersion = 3"、`"complete" 与 `"items"。每个 item 都有 `"kind"、`"title"、
workspace-relative `"path"、LSP `"location" 和 `"provenance = { source, revision }"；task item 还可有
`"id"、`"state"、`"waitReasons"、`"due"、`"blocked" 与 `"actionable"；event item 还可有 `"id"、
`"start"、`"end" 与 `"tasks"。`"complete = false" 表示 index
或 limit 导致结果不完整。推荐 wrapper 已处理 capability、buffer/client selection、空结果、错误、Location
jump 与 cancellation；自选 picker 应复用同一 schema，而不是解析 display label。

`# Reference CodeLens

metadata 与 explicit anchors 的 CodeLens 返回 `"plumb.showReferences" client command。
`"plumb.codelens.setup()" 将 locations 显示在 quickfix；`"picker = 'snacks'" 使用 Snacks picker，并在
Snacks 不可用时回退 quickfix。

该 command 不是 server-side `"workspace/executeCommand"。CodeLens arguments 固定为 target URI、target
position 与完整 `"Location list"；wrapper 直接使用第三个 argument，不再发送 `"textDocument/references"。
光标位于 metadata marker 或 explicit anchor block 时可以运行：

`text"
  :lua vim.lsp.codelens.run()

推荐直接注册 keymap：

`lua"
  vim.keymap.set('n', '<leader>pr', vim.lsp.codelens.run)

这要求当前 buffer attached 到提供 CodeLens 的 plumb client，并且已调用
`"require('plumb.codelens').setup()" 注册 `"plumb.showReferences"。0 references 显示 info notification；
client 已退出、payload 无效或 picker 失败时显示明确错误。quickfix 与 Snacks 都使用 Location 跳转，
Snacks additionally 提供 preview、tagstack 与 single-result auto-confirm。

`# Workspace Web app

Web viewer 独立于 LSP process 与 Neovim plugin，通过统一 CLI 启动：

`text"
  plumb site serve --root /path/to/workspace

Note preview 对 `"cite" 运行 Pandoc citeproc。Bibliography relative path 从各 note 所在目录
解析，必须保持在 workspace root 内；CSL JSON 改变会触发 preview cache invalidation。

`# Troubleshooting

`- `":checkhealth vim.treesitter" 检查 parser 是否注册；highlight 正常不代表 strict parser 接受文档。
`- `":LspInfo" 检查 `"plumb" client、root directory 与 attached buffers。
`- nested workspace 未生效时，确认 marker 是 `".plumb/" directory，并在重新打开 buffer 后检查 root。
`- ignored note 意外出现在 search 或 graph 时，检查生效 workspace root 及其 root-scoped `".ignore"。
`- repository-local parser 或 LSP 需要调试时，以 `"dev/nvim.lua" 作为可运行参考配置。
