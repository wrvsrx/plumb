`= title plumb 工具链指南

本页记录当前 release 的 commands、inputs、outputs 与 failure behavior。未来方向只进入 roadmap， 内部 crate/store contracts 只进入 architecture。

`# Agenda 核算与完整覆盖检查

`sh"
 plumb event summary --root DIR --from 2026-09-22T00:00:00+08:00 --to 2026-09-23T00:00:00+08:00
 plumb event summary --root DIR --from 2026-09-22T00:00:00+08:00 --to 2026-09-23T00:00:00+08:00 --group-by task --json
 plumb event check-timeline --root DIR --from 2026-09-22T00:00:00+08:00 --to 2026-09-23T00:00:00+08:00

from/to 使用带 offset 的 RFC 3339，结束必须晚于开始。summary 可按 category（默认）、item 或 task 显示秒数； JSON 同时提供各类汇总、逐事件份额、分类来源、source byte ranges、point events、空档、重叠与 completeness。 跨范围事件裁剪后计时，多事项先去重再均分；task 汇总不重新分配普通活动份额。 累计时长包含重叠，covered_seconds 只算时间轴的实际覆盖。

check-timeline 要求 half-open 范围内每个时刻恰好有一个 event 覆盖，包括首尾边界。
空集合报告整个范围缺失；point event 不提供覆盖，分类分摊不会生成额外事件。
两种查询都支持现有 event CEL 的 --query，并在响应中标明筛选条件。
退出码 0 表示成功，check-timeline 的 1 表示覆盖不合格，2 表示查询失败或结果不完整；
summary 的空档与重叠不单独改变成功退出码。无效文档或无法确定时间的所选事件使检查不完整，不能假定其在范围外。
这些检查只由主动查询执行，不改变文档语法合法性。

`# Build and test

`text"
  nix develop
  cargo test --workspace
  cargo check --workspace --all-targets

tree-sitter grammar 使用独立 shell 生成和测试：

`text"
  nix develop .#tree-sitter-plumb -c tree-sitter generate
  nix develop .#tree-sitter-plumb -c tree-sitter test

generated parser sources 不属于 repository source，不应提交。

workspace discovery、ignore、note/task、graph 与 site 见 `->{{workspace 指南} workspace.plumb}；editor setup 与交互工作流见 `->{{editor integration 指南} editor-integration.plumb}。

`# Check

`"plumb check --root DIR" 检查 workspace index 中的 `"..plumb" files；省略 `"--root" 时使用 `->{{workspace marker discovery} workspace.plumb}。它复用与 LSP 相同的 strict syntax、semantic 和 cross-document diagnostics。

diagnostics 按 relative path 和 source range 稳定排序，输出 `"path:line:column"、severity、code 与 message。`"--level error|warning|hint" 选择最低显示 severity；默认 `"warning"，因此显示 Error 与 Warning 并省略 Hint。只有 Error 令 command 返回 nonzero，Warning 与 Hint 不改变退出状态。筛选后无 diagnostic 时不输出内容并返回成功。

`# Cache

check、note（含 interactive）、task 查询、next 和任务操作，以及 event 查询与 export-vdir 默认复用持久化语义
缓存。每次运行仍扫描并读取文件，只有内容变化的文档重新解析；没有保存到磁盘的编辑器内容不进入 CLI 查询。
跨文件诊断、附件存在性和当前时间相关的任务状态每次重新检查。

这些命令接受 `"--no-cache"，从磁盘完整分析，用于排错和结果对照；`"--cache-stats" 在 stderr 显示本轮索引的
documents、hits、实际 parsed 数量及阶段耗时，不改变正常 stdout。任务操作只为目标文件建立编辑所需语法树，
拒绝覆盖索引后发生的源文件变化，成功后刷新缓存。示例：

`text"
  plumb check --root notes --cache-stats
  plumb check --root notes --no-cache
  plumb task --root notes --query 'state == "ready"' --cache-stats

加载工作区时，缓存无法打开或索引失败会报告 warning 并改用无缓存分析；源文件扫描或读取失败仍返回失败，不输出旧结果。
读取缓存记录时发现损坏也会报告错误，可使用 `"--no-cache" 排错并通过下述 prune 清理。fmt、export 和 import
直接处理单文件或 stdin，不为缓存扫描整个 workspace。任务源文件保存后若缓存刷新失败，会明确报错，说明源文件已保存。

`"plumb cache status" 显示 semantic cache root，并按 workspace/site 与 binary version 报告 active state、 database/file 数量和 bytes。没有 lease 的旧 layout 数据显示为 unmanaged。

`"plumb cache prune" 删除非当前 binary version 中未被进程使用的可重建 cache；当前版本默认保留。显式 `"plumb cache prune --all" 也会考虑当前版本。LSP、Web 或其它进程仍持有的 active namespace，以及无法用 lease 证明未被使用的 unmanaged 数据，始终只报告而不删除。server startup 不自动清理其它版本。

`# Playground

`"examples/index.plumb" 及同目录文档覆盖 core syntax 和 official semantics，可用于 editor、 diagnostics 和 export smoke test。

`# LSP

`"plumb lsp" 提供 strict syntax 与 semantic diagnostics、document symbols、definition、references、 hover、completion、anchor/document rename、metadata insertion、task conversion/created/status actions、 metadata document-reference 与 explicit-anchor CodeLens、task semantic tokens 和 whole-document formatting。server 会扫描 workspace，并在 editor 支持时 监听 `"..plumb" 与 `".ignore" file changes。ignore rule 变化会 resynchronize disk index；ignored document 若作为 open buffer 存在，仍由 open-buffer snapshot 覆盖 disk index。

标准 `"workspace/symbol" 支持 `"note QUERY"、`"task QUERY"、`"event QUERY" 和无 prefix 的混合搜索， 结果包含可跳转 Location。`"plumb/search" custom request 提供 schema version 3 的 typed records，`"plumb/next" custom request 提供 schema version 1 的 shortlist：

`json"
  {
    "kind": "task",
    "query": "review",
    "filter": "state == 'ready' && due < now",
    "limit": 100
  }

`"kind"、`"query" 和 `"filter" 可省略；kind 可以是 `"note"、`"task" 或 `"event"，filter 使用 shared CEL facts。task result 的 `"state" 是 ready、waiting、blocked、done、canceled 或 conflicted，并用 `"waitReasons" 报告 time/dependency reasons；event result 可携带 `"start"、`"end" 与 `"tasks"。result 还包含 items、Location、revision provenance 和 complete；limit 上限为 200。invalid CEL 使用 InvalidParams，initial index 前使用 ServerNotInitialized，workspace scan 不完整时 complete 为 false， Location 等内部 invariant 失败使用 InternalError，cancellation 使用 RequestCancelled。initialize response 的 `"experimental.plumb.search" 声明 method 与 schemaVersion。

client configuration、completion、CodeAction、CodeLens、search 与 Web app integration 见 `->{{editor integration 指南} editor-integration.plumb}。

direct declaration completion 由 recovered syntax owner 决定：task/event properties、Link target、Image/File 的
`"src" 与 math language 按上下文提供。block owner 生成 marked child，inline owner 生成 direct marked group；
已存在的 unique property 不重复建议。Link/Image path/anchor 使用 workspace-aware candidates。

`# Format

`"plumb fmt" 对 syntax-valid document canonicalize recursive owners。anonymous/marked parsed owner 的 children
统一深一列；owner 与 first child 之间 one line ending，different sibling kinds 可保留一个 blank separator。
已有 block InlineContents 的 source spelling 保持不变，包括 element间 SpaceRun width、group boundary padding、escape spelling
与 inline verbatim delimiter strength。formatter 保留 continuation physical wrapping 与 structural indentation，只移除
line-end padding；marker 与同一行首个 content element之间规范化为 one ASCII space。
explicit Align arguments 可以扩大 direct SpaceRun，formatter 不主动对齐或撤销对齐。

新建或替换 syntax 由 plumb-edit 选择 canonical group/escape 与 minimal compact/full inline verbatim quote strength。
已有 block verbatim 使用 optional marker + one quote 与 one structural-space payload margin；formatter 保持 typed raw
bytes与 final newline，不存在 raw tail。

formatter 不重排普通 text/direct owners，也不解释 marker semantics；syntax error document 被拒绝。

不带 path 时从 stdin 读取并写到 stdout；带 path 时原地更新：

`text"
  plumb fmt document.plumb
  cat document.plumb | plumb fmt > formatted.plumb
  plumb fmt --check docs/*.plumb

`"--check" 不写文件；任一输入需要重新格式化时返回非零状态。LSP 使用同一个 formatter 实现 `"textDocument/formatting" 与 `"textDocument/rangeFormatting"，editor 的 tab size 不改变 explicit Align arguments 的 display-column policy。protocol-neutral formatter 对 whole-document canonical output 做 line diff， 使用两侧均 unique 且顺序一致的 unchanged lines 分隔局部差异，并返回按 source order 排列、互不重叠的 最小 changed-line edits；LSP 只转换 byte ranges，不把 unchanged blocks 包含在 replacement 中。

range formatting 不把 selection 扩展到只被部分命中的 structure：空 selection 或只包含 block 局部内容时返回空 edits；完整包含一个 owner block 时格式化其完整 subtree；完整包含多个 blocks 时 只选择其中 maximal blocks，并把相邻同级 blocks 合并为连续 group。每个 group 只替换 block content 及 group 内部 layout，不改变 selection 外的前后分隔空白；syntax-invalid document 返回 `"null"。

`# Export

`"plumb export" 直接产生 Pandoc JSON AST。Pandoc 只作为 writer：

`text"
  plumb export document.plumb | pandoc -f json -t html -o document.html
  cat document.plumb | plumb export | pandoc -f json -o document.pdf

syntax error、无效 metadata 或其他阻止 authoritative semantics 的错误会让 export 失败。

`# Import

`"plumb import" 读取 Pandoc JSON AST，并在 stdout 产生 syntax-valid、canonical formatted plumb。 省略 path 时从 stdin 读取：

`text"
  pandoc source.md -t json | plumb import > document.plumb
  plumb import document.json > document.plumb

importer 覆盖 `"plumb export" 当前产生的标准 profile，并保证受支持 AST 的 `"export -> import -> export" semantic round-trip。包括 metadata、paragraph、heading、bullet/decimal ordered list、definition、table、quote、Div、CodeBlock，以及 text、inline styles、Code、Link、File、Image、 simple citation、inline math 和 Span。Table 支持 inline/rich/empty cells、caption、attributes、连续 header rows 与一致的 row-header prefix。

Pandoc 中不存在标准 plumb 表示的 node 会明确失败，不会静默丢失内容。例如 figure、raw HTML、horizontal rule、line block、footnote、复杂 citation、非 decimal 或非 1 起始 ordered list 当前均不受支持。Table 的 rowspan、colspan、alignment、column widths、foot 与复杂 body grouping 也不可表示。 Pandoc attribute name 不能表示为 declaration name，或 Link/Image property 与 target tuple 冲突时也会失败。 importer 不读取 Pandoc Markdown source；需要先让 Pandoc 输出 JSON。

项目方向与尚需验证的工作统一记录在 `->{路线图 ../project/roadmap.plumb}；可直接实施的 工作见 `->{当前任务 ../project/tasks.plumb}。本指南不复制实现进度。

`## 检查事件分类

`sh"
 plumb event check-category --root ~/notes --json
 plumb event check-category --root ~/notes --explicit --json

检查全部选中的 events（支持全局 query），无需指定日期范围。默认考虑引用事项的分类；explicit 只检查 event 自身声明，适合迁移检查。
报告 checked 数量、missing 位置与 issues。退出码 0 表示全部有分类，1 表示缺分类，2 表示非法分类或检查不完整。
